# 样式开发规范（极简黑白灰）

> **令牌唯一定义在 `quiz-web/src/assets/base.css`**。禁止在 `<style scoped>` 里再次声明 `:root` 或重定义 `--color-*` / `--radius-*` / `--text-*`；所有颜色/圆角/字号一律通过全局变量引用。
>
> **padding / margin / gap 直接写 px，不用抽象 scale**（详见第三章）。

---

## 一、色彩系统

极简黑白灰唯一色系，除功能状态外**不引入品牌 accent**（不再用蓝/紫/绿/靛/mint 等）。

### 1.1 中性色阶

| 变量                    | 值        | 用途                                    |
| ----------------------- | --------- | --------------------------------------- |
| `--color-bg`            | `#F8F8F8` | 页面底色                                |
| `--color-surface`       | `#FFFFFF` | 卡片 / 浮层                             |
| `--color-surface-alt`   | `#FAFAFA` | 斑马纹、次级面                          |
| `--color-ink`           | `#1A1A1A` | **主文字 & 主色**（按钮 / 强调）        |
| `--color-ink-soft`      | `#4A4A4A` | 次要文字                                |
| `--color-ink-muted`     | `#8A8A8A` | 辅助 / 占位 / 说明                      |
| `--color-ink-inverse`   | `#FFFFFF` | 深色背景上的文字                        |
| `--color-line`          | `#EAEAEA` | 常规边框                                |
| `--color-line-soft`     | `#F0F0F0` | 浅边框 / 分割线                         |
| `--color-hover`         | `#F5F5F5` | hover 底色                              |
| `--color-active`        | `#E8E8E8` | pressed 底色                            |
| `--color-charcoal`      | `#333333` | 深块底（Hero CTA / Pro 卡 / 反色区域）  |
| `--color-black`         | `#000000` | 极端强调（按钮 active）                 |

### 1.2 功能色（仅用于状态提示，不用作大面积视觉）

| 变量                | 值        | 用途                        |
| ------------------- | --------- | --------------------------- |
| `--color-success`   | `#2A9D8F` | Message / Tag 成功状态      |
| `--color-warning`   | `#E9A83B` | Message / Tag 警告状态      |
| `--color-danger`    | `#D9534F` | Message / Tag 危险 / 退出   |
| `--color-info`      | `#6C757D` | Message / Tag 中性提示      |

每个功能色都配了对应的 `--color-*-bg` 浅底版本供 Tag / Message 组件使用。

### 1.3 使用规则

```css
/* ✅ 正确 */
.button { background: var(--color-ink); color: var(--color-ink-inverse); }

/* ❌ 错误：硬编码 */
.button { background: #1a1a1a; color: white; }

/* ❌ 错误：组件内重定义 */
.pricing { --color-primary: #4f46e5; ... }
```

---

## 二、排版系统

### 2.1 字体栈

```css
--font-sans: 'Inter', 'PingFang SC', 'Microsoft YaHei', system-ui, -apple-system,
             BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
--font-mono: 'SF Mono', 'Fira Code', 'JetBrains Mono', Consolas, monospace;
```

body 已在 `base.css` 中默认使用 `--font-sans`。等宽字体用于**数据 tag、编号（如 `01 / 模考评估`）**。

### 2.2 字号 & 行高

| 变量           | 值        | 场景                    |
| -------------- | --------- | ----------------------- |
| `--text-xs`    | 12px      | 小 tag、辅助说明        |
| `--text-sm`    | 14px      | 按钮、导航链接、卡片正文 |
| `--text-base`  | 16px      | 正文                    |
| `--text-lg`    | 18px      | 引导小标                |
| `--text-xl`    | 20px      | Hero 副标、小卡标题     |
| `--text-2xl`   | 24px      | 卡片标题、Plan title    |
| `--text-3xl`   | 30px      |                         |
| `--text-4xl`   | 36px      | Section H2（移动）      |
| `--text-5xl`   | 48px      | Section H2（桌面）      |
| `--text-6xl`   | 60px      | Hero H1（桌面）         |

行高：`--leading-tight 1.1` / `--leading-snug 1.25` / `--leading-normal 1.5` / `--leading-relaxed 1.7`。

字重：`400 / 500 / 600 / 700`。

### 2.3 排版规则

- **标题**（h1-h3）统一 `font-weight: 700` + `letter-spacing: -0.02em`
- Hero H1 桌面 `60px`，移动 `44px`
- Section H2 桌面 `48px`，移动 `36px`
- Eyebrow 小标：`12px`、`font-weight: 600`、`letter-spacing: 0.15em`、`text-transform: uppercase`
- 正文用 `--text-base` + `--leading-relaxed`，颜色 `--color-ink-soft`

---

## 三、间距系统

**原则**：局部 padding / margin / gap **直接写 px**，不搞抽象 scale。跨页面复用的"页面级布局尺寸"才用 token。

### 3.1 页面级布局 token（7 个语义化尺寸）

| 变量                     | 值      | 用途                                              |
| ------------------------ | ------- | ------------------------------------------------- |
| `--layout-min-width`     | 1200px  | **全站最小布局宽度**（web-only，见 3.2）          |
| `--container-max`        | 1280px  | 页面级最大宽度（Hero / 落地页外层容器）           |
| `--container-px-desktop` | 40px    | 页面容器桌面左右内边距                            |
| `--container-px-mobile`  | 24px    | 页面容器移动左右内边距                            |
| `--section-py-desktop`   | 96px    | section 桌面上下 padding                          |
| `--section-py-mobile`    | 64px    | section 移动上下 padding                          |
| `--nav-height`           | 72px    | 全站导航栏高度（Hero 顶部要预留这个值）           |

### 3.2 布局宽度规范 ⭐

**本项目仅面向 Web 桌面浏览器**，不做移动端响应式挤压。全站布局锁定最小宽度 `1200px`：

- **视口 = 1200px**：内容刚好占满，正常显示
- **视口 > 1200px**：内容自然拓展占用更大宽度（不硬顶到 1200 中间留白）
- **视口 < 1200px**：**不缩不叠**，底部出现横向滚动条，用户滚动即可看到完整布局

实现方式：在 `base.css` 里对 `body` 强制 `min-width: var(--layout-min-width)`。任何模块只要挂在 body 下就自动享受这个规则，不需要每页各写。

```css
/* base.css */
body {
  min-width: var(--layout-min-width);   /* 1200px 硬底线 */
  ...
}
```

模块内容通常放在一个可选的**内容外壳**（class 用 `.shell` / `.profile-shell` / `.exam-shell` 等）里，负责水平居中 + 左右呼吸：

```css
.shell {
  max-width: 1600px;                      /* 大屏上限，避免超宽屏文字行长失控 */
  margin: 0 auto;
  padding: 0 var(--container-px-desktop); /* 40px 左右内边距 */
}
```

`max-width` 可根据模块特点调整（表格密集型可放宽到 1600，文本阅读型 1200），但**下限必须由 body 的 `min-width` 保证**，不在 `.shell` 上再设 min。

**例外**：`Hero` / 首页深色 CTA 大块等**通铺型区块**可以用外层 `--container-max` (1280px) 让视觉有呼吸感，但**内容区**（文字、卡片、表格）仍要对齐到统一的 shell 基线，不越界。

### 3.3 常用 px 参考值（写代码时对照挑，不做成变量）

- **卡片内边距**：常规 32px，紧凑 24px，宽松 40px 或 48px
- **Section 顶底**：桌面 96px，移动 64px（走 `--section-py-*` token）
- **列表 gap**：紧密 8-12px，常规 16-24px，宽松 32px
- **表单项间距** `margin-bottom`：紧密 16px，常规 20px，宽松 24px
- **按钮内 icon 与文字 gap**：8px
- **卡片之间 gap**：24px（网格布局）

---

## 四、圆角与阴影

### 4.1 圆角

| 变量             | 值    | 用途                            |
| ---------------- | ----- | ------------------------------- |
| `--radius-sm`    | 6px   | 小 tag、标签                    |
| `--radius-md`    | 8px   | 按钮、输入框                    |
| `--radius-lg`    | 12px  | 小卡片、下拉                    |
| `--radius-xl`    | 16px  | 常规卡片                        |
| `--radius-2xl`   | 20px  | Hero 浮动预览卡、Pricing 卡     |
| `--radius-3xl`   | 24px  | Hero / Modal                    |
| `--radius-pill`  | 999px | eyebrow chip、胶囊 badge        |

### 4.2 阴影（纯灰度，不加色相）

```css
--shadow-sm:  0 1px 2px  rgba(0, 0, 0, 0.04);
--shadow-md:  0 4px 12px rgba(0, 0, 0, 0.06);
--shadow-lg:  0 12px 32px rgba(0, 0, 0, 0.08);
--shadow-xl:  0 20px 40px rgba(0, 0, 0, 0.10);
```

### 4.3 组件圆角/阴影匹配表

| 组件            | 圆角              | 阴影           |
| --------------- | ----------------- | -------------- |
| 按钮 / 输入框   | `--radius-md`     | 无             |
| 常规卡片        | `--radius-xl`     | `--shadow-sm`  |
| Hero 浮动预览卡 | `--radius-2xl`    | `--shadow-md`  |
| Modal / 大卡    | `--radius-2xl`    | `--shadow-xl`  |
| 弹出下拉        | `--radius-lg`     | `--shadow-lg`  |
| Tag             | `--radius-sm`     | 无             |

---

## 五、组件规范总表 ⭐

**这是全站视觉一致性的权威表**。以后新写任何组件、复审 PR，都先对照这张表。缺失的组件在实现时补充回这里。

### 5.1 高度 / 尺寸

| 组件             | 高度   | Token                    | 说明                              |
| ---------------- | ------ | ------------------------ | --------------------------------- |
| 输入框（常规）   | 48px   | `--height-input`         | 登录 / 表单 / 大屏                |
| 输入框（紧凑）   | 36px   | `--height-input-sm`      | 表格搜索栏 / 工具栏               |
| 按钮 md（默认）  | 40px   | `--height-button`        | 导航 CTA / 卡片按钮               |
| 按钮 lg          | 48px   | `--height-button-lg`     | Hero CTA / 表单主提交（`.auth-submit`） |
| 按钮 sm          | 32px   | `--height-button-sm`     | 表格行内操作                      |
| 表格行           | 56px   | `--height-table-row`     | `el-table` 每行                   |
| 导航栏           | 72px   | `--nav-height`           | 全站顶部导航                      |

### 5.2 圆角

| 组件                        | 圆角 | Token           |
| --------------------------- | ---- | --------------- |
| 输入框（`el-input`）        | 8px  | `--radius-md`   |
| 按钮（`el-button`）         | 8px  | `--radius-md`   |
| Tag（`el-tag`）             | 6px  | `--radius-sm`   |
| 表格（`el-table`）          | 12px | `--radius-lg`   |
| 下拉菜单（dropdown）        | 12px | `--radius-lg`   |
| 常规卡片                    | 16px | `--radius-xl`   |
| Hero 浮动预览卡 / Pricing 卡 | 20px | `--radius-2xl`  |
| 弹窗（`el-dialog`）         | 16px | `--radius-xl`   |
| 头像                        | 50%  | 直接写          |
| 胶囊 chip / badge           | 999px| `--radius-pill` |

### 5.3 边框

| 状态              | 值                                             |
| ----------------- | ---------------------------------------------- |
| 默认              | `1px solid var(--color-line)` (#EAEAEA)        |
| Hover             | `1px solid #B8B8B8`                            |
| Focus             | `1px solid var(--color-ink)` (#1A1A1A)         |
| 错误              | `1px solid var(--color-danger)`                |
| 深色反色块        | 无边框（靠 `--color-charcoal` 底色区分）       |

### 5.4 内边距（组件级）

| 组件                     | Padding                             |
| ------------------------ | ----------------------------------- |
| 输入框（常规）           | `0 14px`（wrapper）                 |
| 按钮 md                  | `10px 18px`                         |
| 按钮 lg                  | `14px 28px`                         |
| 卡片（常规）             | 32px                                |
| 卡片（宽松，Pricing 等） | 40px                                |
| 弹窗内容                 | 24px                                |
| 表格单元格               | `12px 16px`                         |

### 5.5 阴影

| 组件                | 静态          | Hover          |
| ------------------- | ------------- | -------------- |
| 卡片（常规）        | `--shadow-sm` | `--shadow-md`  |
| Hero 浮动预览卡     | `--shadow-md` | -              |
| 下拉菜单            | `--shadow-lg` | -              |
| 弹窗                | `--shadow-xl` | -              |
| 按钮 / 输入框       | 无            | 无（用边框变化） |

---

## 六、按钮规范

### 5.1 三种变体

| 变体          | 用途                          | 底色                  | 字色                   | 边框                        |
| ------------- | ----------------------------- | --------------------- | ---------------------- | --------------------------- |
| **primary**   | 主 CTA（提交 / 跳转 / Hero）  | `--color-ink`         | `--color-ink-inverse`  | `--color-ink`               |
| **secondary** | 次要（取消 / 返回 / 副 CTA）  | `--color-surface`     | `--color-ink`          | `--color-line`              |
| **ghost**     | 无背景（导航链接类）          | 透明                  | `--color-ink-soft`     | 无                          |

hover：

- primary → 底色变 `--color-charcoal`，`translateY(-2px)`
- secondary → 边框变 `--color-ink`
- ghost → 文字变 `--color-ink`

### 5.2 尺寸

| 尺寸            | padding    | 字号          | 用途                           |
| --------------- | ---------- | ------------- | ------------------------------ |
| **sm**          | `8px 14px` | `--text-sm`   | 表格内操作、导航次按钮         |
| **md**（默认）  | `10px 18px`| `--text-sm`   | 导航主 CTA、卡片操作           |
| **lg**          | `14px 28px`| `15px`        | Hero CTA、Pricing CTA          |
| **xl**          | `16px 32px`| `15px`        | 底部深色 CTA 区                |

### 5.3 反色 CTA（深色区）

在深色区（Hero CTA 大块、Pro 定价卡）内的主按钮**反过来**：

- 底 `--color-ink-inverse`（白）、字 `--color-ink`（黑）；副按钮描边白色透明。

---

## 七、响应式断点

```css
@media (max-width: 640px)   { /* sm - 手机竖屏 */ }
@media (max-width: 768px)   { /* md - 平板竖屏 */ }
@media (max-width: 1024px)  { /* lg - 平板横屏 */ }
@media (min-width: 1280px)  { /* xl - 桌面 */ }
```

**规则**：

- 导航栏：`<768px` 隐藏 logo 文字与用户 chip 名称，链接横向可滚
- Section H2：`48px → 36px`
- Hero H1：`64px → 56px → 44px`
- 卡片网格：桌面 3 列 → 平板 2 列 → 手机 1 列
- 页面内边距：40px → 24px

---

## 八、过渡动画

```css
--duration-fast: 0.15s;
--duration-base: 0.2s;
--duration-slow: 0.3s;
--ease-out: cubic-bezier(0.16, 1, 0.3, 1);
```

- 常规交互 `transition: all var(--duration-base) ease`
- 卡片 hover 用 `var(--duration-slow)`
- 弹窗 / 折叠用 `--duration-slow` + `--ease-out`
- 卡片 hover 统一 `translateY(-2px)`（按钮）或 `translateY(-4px)`（卡片）

---

## 九、Element Plus 主题覆盖

覆盖在 [element-overrides.css](quiz-web/src/assets/element-overrides.css)。

### 12.1 全局变量映射

| Element Plus 变量               | 项目令牌                                 |
| -------------------------------- | ---------------------------------------- |
| `--el-color-primary`             | `var(--color-ink)` = #1A1A1A             |
| `--el-color-primary-light-3..9`  | 灰阶派生（#4A/#8A/#C4/#DE/#F5F5F5）      |
| `--el-color-primary-dark-2`      | `#000000`                                |
| `--el-border-color`              | `var(--color-line)`                      |
| `--el-border-color-light`        | `var(--color-line-soft)`                 |
| `--el-border-radius-base`        | `var(--radius-md)`                       |
| `--el-text-color-primary`        | `var(--color-ink)`                       |
| `--el-text-color-regular`        | `var(--color-ink-soft)`                  |
| `--el-text-color-secondary`      | `var(--color-ink-muted)`                 |
| `--el-bg-color`                  | `var(--color-surface)`                   |
| `--el-bg-color-page`             | `var(--color-bg)`                        |
| `--el-fill-color-light`          | `var(--color-hover)`                     |
| `--el-font-family`               | `var(--font-sans)`                       |

### 12.2 组件细节修正

- `el-button--primary`：黑底白字，hover 变 `--color-charcoal`，active 变 `--color-black`
- `el-input__wrapper` focus：`box-shadow: 0 0 0 1px var(--color-ink) inset`（不用蓝色）
- `el-table` 表头：`background: --color-surface-alt`，行 hover 变 `--color-hover`
- `el-pagination.is-background` 当前页高亮：`background: --color-ink`
- `el-tag--primary`：`--color-hover` 底 + `--color-line` 边框
- `el-message`：`--radius-md` + `--shadow-lg`
- `el-dialog`：`--radius-xl`

### 12.3 Confirm 弹窗规范（`ElMessageBox.confirm`）

用于“确认交卷 / 离开页面 / 删除 / 停用”等需要二次确认的业务动作。统一使用 Element Plus `ElMessageBox.confirm`，并通过 `customClass: 'app-confirm-box'` 挂载全局样式类。

**尺寸与容器**
- 宽度：`460px`
- 最大宽度：`calc(100vw - 64px)`
- 最小高度：`204px`
- 内边距：`20px 30px 12px`
- 边框：`1px solid var(--color-line)`
- 圆角：`var(--radius-md)`
- 阴影：`var(--shadow-lg)`
- Overlay 使用 `display: flex` + `align-items/justify-content: center`，保证弹窗位于页面中心
- 遮罩关闭：关键业务确认建议 `closeOnClickModal: false`

**标题**
- 标题文案固定为短词，如“提示 / 确认 / 删除”
- 字号：`var(--text-xl)`
- 字重：`var(--weight-bold)`
- 行高：`var(--leading-snug)`
- 对齐：居中
- Header 底部间距：`22px`

**内容**
- 正文字号：`var(--text-base)`
- 行高：`var(--leading-relaxed)`
- 字色：`var(--color-ink)`
- 内容区居中；警告图标与文案作为一个整体水平居中
- 警告图标使用 `var(--color-warning)`
- 文案保持一行主问题，必要时补一句影响说明；不要写成长段落。

**按钮区**
- 按钮区居中，按钮间距 `16px`
- 按钮区使用 `margin-top: auto`，贴近弹窗底部
- 按钮高度：`40px`
- 最小宽度：`112px`
- 圆角：`var(--radius-md)`
- 主按钮：黑底白字，`background/border: var(--color-ink)`，hover 使用 `--color-charcoal`
- 次按钮：白底黑字，`border: var(--color-ink)`，hover 使用 `--color-hover`

### 12.4 状态色映射

`ElMessage.success/warning/error/info` 走 `--color-success/warning/danger/info` + 对应 `-bg`。**不在 UI 大面积使用**这些色。

---

## 十、禁用事项

| 禁止                             | 替代方案                              |
| -------------------------------- | ------------------------------------- |
| 使用 `!important`                | 通过选择器优先级控制                  |
| 硬编码色值（`#4f46e5` 等）        | 使用 `var(--color-*)` 全局变量        |
| 组件内声明 `:root { --xxx: ... }` | 令牌只在 `base.css` 中定义          |
| 行内样式 `style="color: red"`   | 使用 class + 全局变量                 |
| 使用蓝 / 紫 / 靛 / mint 品牌色    | 全部走中性灰阶                        |
| 彩色渐变、彩色阴影                | 用纯灰度阴影 + 平面色块               |
| 固定宽高（`width: 400px`）        | 使用 `max-width` + 百分比 + `min()`   |
| 深层嵌套选择器（>3 层）           | 使用 scoped 或 BEM 命名               |
| `dark mode` prefers-color-scheme  | 项目只支持浅色，dark 相关规则不添加   |

---

## 十一、组件命名规范

采用 **PascalCase** 文件名 + **kebab-case** class 名：

```
components/
├── NavBar.vue           ← 导航栏
├── PricingSection.vue   ← 定价区
├── QuestionCard.vue     ← 题目卡
├── FormulaBlock.vue     ← 公式渲染
├── LatexText.vue        ← LaTeX 文本
└── AppPagination.vue    ← 分页组件
```

class 名使用 kebab-case + 元素修饰符（BEM 简化）：

```css
.plan-card {}
.plan-card--pro {}
.plan-cta {}
.plan-cta--pro {}
```

---

## 十二、快速引用

- Token 源：[quiz-web/src/assets/base.css](quiz-web/src/assets/base.css)
- Element Plus 覆盖：[quiz-web/src/assets/element-overrides.css](quiz-web/src/assets/element-overrides.css)
- 全局挂载：[quiz-web/src/assets/main.css](quiz-web/src/assets/main.css)
- Hero / 六段落地页参考实现：[quiz-web/src/views/home/HomeView.vue](quiz-web/src/views/home/HomeView.vue)
- 导航栏参考：[quiz-web/src/components/NavBar.vue](quiz-web/src/components/NavBar.vue)
- 定价卡参考：[quiz-web/src/components/PricingSection.vue](quiz-web/src/components/PricingSection.vue)

---

*最后更新：2026-07-01（极简黑白灰改造）*
