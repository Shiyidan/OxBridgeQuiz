# 数据库构建方案

> 数据库表结构参考文档，后续所有数据库变更均以此为准。

---

## 一、数据库选型

### 1.1 当前：SQLite（开发期）

当前使用 SQLite（`prisma/dev.db`），理由：

| 优点 | 说明 |
|------|------|
| 零配置 | 无需安装数据库服务，文件即数据库 |
| 轻量 | 单文件，备份只需拷贝 `dev.db` |
| Prisma 兼容 | Prisma 对 SQLite 支持完善，开发体验一致 |

| 局限 | 说明 |
|------|------|
| 并发写入弱 | 写入时锁全表，高并发场景性能差 |
| 无用户管理 | 不支持数据库用户/权限体系 |
| 不适合生产 | 多实例部署时无法共享数据文件 |

### 1.2 上线后：PostgreSQL（或 MySQL）

正式上线推荐切换到 **PostgreSQL**（阿里云 RDS），理由：

| 维度 | 评估 |
|------|------|
| **扩展性** | 支持读写分离、连接池、水平分表，业务增长时平滑扩容 |
| **维护性** | 托管服务（RDS）自动备份/监控/升级，DBA 成本低 |
| **并发** | 行级锁 + MVCC，多用户同时答题不阻塞 |
| **数据类型** | 原生 JSONB 支持，`options` / `tags` 字段可建索引加速查询 |
| **Prisma 兼容** | 切换只需改 `datasource` 配置 + 连接字符串，Schema 不用动 |

### 1.3 迁移路径

```
SQLite (开发) → PostgreSQL (测试/预发) → PostgreSQL RDS (生产)
```

Prisma 作为 ORM 屏蔽了底层差异，迁移只需：
1. 改 `schema.prisma` 中 `provider = "postgresql"`
2. 改 `.env` 中 `DATABASE_URL` 指向新库
3. 执行 `prisma migrate deploy`
4. 数据从 SQLite 导出 → 导入 PostgreSQL

---

## 二、试题与试卷

### 2.1 表关系说明

**Paper（试卷）和 Question（试题）是多对多关系**——一套试卷包含多道题，同一道题也可出现在多套试卷中。这个关系通过 `PaperQuestion` 关联表承载。

关联表的核心是 `order` 字段，它记录了题目在试卷中的位置（第几题），查询时按 `order` 升序排列即可保证题目顺序与原始试卷完全一致。

例如 ENGAA 2023 这套卷子有 3 道题，传入的题号顺序为：

```
PaperQuestion 表：
┌──────────┬──────────────────────┬──────────────────────┬───────┐
│ id       │ paperId              │ questionId           │ order │
├──────────┼──────────────────────┼──────────────────────┼───────┤
│ pq-001   │ engaa-2023           │ q-surface-area       │ 1     │
│ pq-002   │ engaa-2023           │ q-spaceship          │ 2     │
│ pq-003   │ engaa-2023           │ q-rearrange          │ 3     │
└──────────┴──────────────────────┴──────────────────────┴───────┘
```

查询时一行代码即可得到按题号排序的完整试题列表：

```ts
prisma.paper.findUnique({
  where: { id: 'engaa-2023' },
  include: {
    paperQuestions: {
      orderBy: { order: 'asc' },
      include: { question: { include: { images: true } } },
    },
  },
})
```

**各表之间的关系概要：**

- `Paper` 1:N `ParseTask` — 一张试卷可能被解析多次（上传、重试）
- `Paper` 1:N `PaperQuestion` N:1 `Question` — 试卷与试题多对多，`order` 字段保证排序
- `Question` 1:N `QuestionImage` — 一道题可有多张配图（SVG/PNG），`order` 字段控制展示顺序
- `Question` 1:N `AnswerRecord` — 一道题可被多个学生多次作答
- `Paper` 1:N `ExamRecord` — 一套试卷可被多个学生多次练习

```
Paper ──1:N── ParseTask
Paper ──1:N── PaperQuestion ──N:1── Question ──1:N── QuestionImage
                                                  │
Question ──1:N── AnswerRecord ──N:1── ExamRecord ──N:1── User
                                                  │
Paper ──1:N── ExamRecord
```

### 1.2 Paper（试卷）

`Paper` 承担两重角色：**整卷导入时**通过 `questions` JSON 列直接存储题目快照；**自由组卷时**通过 `PaperQuestion` 关联表引用已有试题。

```prisma
model Paper {
  id             String   @id @default(uuid())
  title          String                        // 试卷名称（如 "ENGAA 2023 Section 1"）
  code           String?                       // 考试代码（ENGAA / NSAA / TMUA / PAT / ESAT）
  year           Int                           // 年份（如 2023）
  duration       Int                           // 建议考试时长（分钟）
  totalQuestions Int      @default(0)          // 题目总数
  pdfUrl         String?                       // PDF 原文件路径（本地存路径，上线后存 OSS key）
  status         String   @default("draft")    // draft | review | published | archived
  questions      String   @default("[]")       // 整卷导入时的题目快照 JSON
  createdAt      DateTime @default(now())      // 创建时间
  updatedAt      DateTime @updatedAt           // 最后修改时间

  parseTasks     ParseTask[]                   // 解析任务列表
  paperQuestions PaperQuestion[]               // 自由组卷关联
  examRecords    ExamRecord[]                  // 答题记录
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | UUID | ✅ | 主键 |
| `title` | String | ✅ | 试卷名称 |
| `code` | String? | | 考试代码，用于分类筛选 |
| `year` | Int | ✅ | 年份 |
| `duration` | Int | ✅ | 建议时长，单位分钟 |
| `totalQuestions` | Int | ✅ | 题目总数 |
| `pdfUrl` | String? | | PDF 存储路径（OSS 未接入时为 null） |
| `status` | String | ✅ | draft / review / published / archived |
| `questions` | String | ✅ | 题目快照 JSON 字符串 |
| `createdAt` | DateTime | 自动 | 创建时间 |
| `updatedAt` | DateTime | 自动 | 最后修改时间 |

#### questions JSON 格式

`questions` 字段存储为 JSON 字符串，是题目对象的数组。所有字段均为统一格式，不分"核心/扩展"。

```json
[
  {
    "number": 1,
    "title": "题干全文（保留旧格式兼容），LaTeX 用 $...$ / $$...$$，段落用 \\n\\n",
    "content_blocks": [
      { "type": "paragraph", "text": "题干段落一..." },
      { "type": "image_ref", "image_id": "q1_graph", "alt": "图形描述" },
      { "type": "paragraph", "text": "题干段落二..." }
    ],
    "options": [
      { "label": "A", "text": "选项内容（含 LaTeX）" }
    ],
    "answer": ["A"],
    "images": [
      {
        "id": "q1_graph",
        "type": "svg",
        "alt": "图形描述",
        "code": "<svg>...</svg>",
        "diagram_type": "geometry",
        "semantic": {},
        "graph_schema": {}
      }
    ],
    "subject": "Mathematics",
    "question_type": "geometry",
    "difficulty": {
      "level": "medium",
      "score": 0.52,
      "reason": "难度评估说明"
    },
    "knowledge_points": [
      { "name": "知识点名", "taxonomy": "学科 > 分类", "confidence": 0.95 }
    ],
    "skills": ["技能标签1", "技能标签2"],
    "learning_analysis": {
      "exam_focus": [
        {
          "title": "知识点名称",
          "description": "本题中该知识点的考察说明",
          "source_knowledge_points": ["知识点名"]
        }
      ],
      "solution": {
        "status": "placeholder",
        "summary": "",
        "steps": [],
        "final_answer": "",
        "distractor_analysis": []
      },
      "review_guidance": {
        "status": "placeholder",
        "summary": "",
        "recommended_topics": [],
        "practice_suggestions": [],
        "common_mistakes": []
      }
    },
    "generation_profile": {
      "can_generate_similar": true,
      "similarity_template": "变体生成模板描述",
      "variable_parameters": [
        { "name": "参数名", "type": "number", "current_value": 1, "allowed_range": "positive integer" }
      ],
      "diagram_required": true,
      "diagram_reusable": true,
      "generation_notes": "生成备注"
    },
    "source": {
      "pages": [4],
      "bbox": null
    },
    "confidence": 0.96,
    "quality": {
      "text_confidence": 0.96,
      "options_confidence": 0.97,
      "diagram_confidence": null,
      "answer_confidence": 0.93,
      "needs_human_review": false,
      "validation_notes": ["校验备注"]
    },
    "rendering": {
      "text_format": "markdown_latex",
      "prefer_content_blocks": true
    }
  }
]
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `number` | number | ✅ | 题号 |
| `title` | string | ✅ | 题干全文（旧格式兼容），LaTeX 用 `$...$` / `$$...$$` |
| `content_blocks` | array | ✅ | 富文本段落数组，前端优先按此渲染 |
| `content_blocks[].type` | string | ✅ | `"paragraph"` 文本段落 或 `"image_ref"` 图片引用 |
| `content_blocks[].text` | string | | `paragraph` 块的文本内容 |
| `content_blocks[].image_id` | string | | `image_ref` 块：匹配 `images[].id` |
| `content_blocks[].alt` | string | | `image_ref` 块：图片备选描述 |
| `options` | array | ✅ | 选项列表，每项含 `label` + `text` |
| `answer` | string[] | ✅ | 正确答案标签数组 |
| `images` | array | ✅ | 配图列表，可为空数组 |
| `images[].id` | string | | 图片唯一标识 |
| `images[].type` | string | ✅ | `"svg"` 或 `"image"` |
| `images[].alt` | string | ✅ | 图片文字描述 |
| `images[].code` | string | | SVG 源码 |
| `images[].diagram_type` | string | | 图表类型：geometry / coordinate_graph 等 |
| `images[].semantic` | object | | 图片语义结构 |
| `images[].graph_schema` | object | | 图形坐标系/约束定义 |
| `subject` | string | | 学科 |
| `question_type` | string | | 题型：word_problem / geometry / graph_interpretation 等 |
| `difficulty` | object | | `{ level, score, reason }` 难度评估 |
| `knowledge_points` | array | | `[{ name, taxonomy, confidence }]` 知识点标注 |
| `skills` | string[] | | 技能标签列表 |
| `learning_analysis` | object | | 学情分析：`exam_focus` 考察要点、`solution` 解答、`review_guidance` 复习指导 |
| `generation_profile` | object | | AI 变体生成参数 |
| `source` | object | | 数据来源：`pages` 页码、`bbox` 边界框 |
| `confidence` | number | | 整体提取置信度 0-1 |
| `quality` | object | | 逐项质量评估：文本/选项/图表/答案置信度 + `needs_human_review` |
| `rendering` | object | | 渲染提示：`text_format` + `prefer_content_blocks` |

**读取示例**：

```ts
// GET /api/papers/:id 返回
const paper = await prisma.paper.findUnique({ where: { id } })
res.json(success({
  ...paper,
  questions: JSON.parse(paper.questions)
}))
```

**写入示例**：

```ts
import { prisma } from '../services/prisma.js'

// 整卷导入：直接写 questions JSON
await prisma.paper.create({
  data: {
    title, year, duration, code,
    questions: JSON.stringify(questions),
    totalQuestions: questions.length,
  },
})

// 自由组卷：通过 PaperQuestion 关联已有试题
await prisma.paperQuestion.createMany({
  data: questionIds.map((qid, i) => ({
    paperId,
    questionId: qid,
    order: i + 1,
  })),
})
```

### 1.3 Question（试题）

单道试题的独立存储，服务于自由组卷和试题检索。

```prisma
model Question {
  id          String   @id @default(uuid())
  title       String                       // 题干（含 LaTeX）
  options     String   @default("[]")      // 选项 JSON：[{"label":"A","text":"..."}]
  answer      String   @default("[]")      // 答案 JSON：["A"] 或 ["A","B"]
  year        Int                          // 试题年份
  subject     String?                      // 学科：Mathematics / Physics / Chemistry 等
  examType    String?                      // 考试类型：ENGAA / NSAA / TMUA / PAT / ESAT
  difficulty  Int?                         // 难度 1-5
  tags        String?                      // 知识点标签 JSON：["algebra","quadratic"]
  explanation String?                      // 解析/解答
  status      String   @default("draft")   // draft | published
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  images          QuestionImage[]          // 配图
  paperQuestions  PaperQuestion[]          // 所属试卷关联
  answerRecords   AnswerRecord[]           // 答题记录
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | UUID | ✅ | 主键 |
| `title` | String | ✅ | 题干（含 LaTeX） |
| `options` | String | ✅ | 选项 JSON 数组 |
| `answer` | String | ✅ | 正确答案 JSON 数组 |
| `year` | Int | ✅ | 试题所属年份 |
| `subject` | String? | | 学科分类 |
| `examType` | String? | | 考试类型 |
| `difficulty` | Int? | | 难度 1-5 |
| `tags` | String? | | 知识点标签 JSON |
| `explanation` | String? | | 答案解析 |
| `status` | String | ✅ | draft / published |
| `createdAt` | DateTime | 自动 | 创建时间 |
| `updatedAt` | DateTime | 自动 | 最后修改时间 |

### 1.4 QuestionImage（试题配图）

```prisma
model QuestionImage {
  id          String  @id @default(uuid())
  questionId  String                       // 外键 → Question.id
  type        String                       // "svg" | "png"
  code        String?                      // SVG 源码（type=svg 时）
  src         String?                      // PNG 路径（type=png 时）
  alt         String?                      // 图片描述
  order       Int     @default(0)          // 同一题多图时的排序

  question Question @relation(fields: [questionId], references: [id], onDelete: Cascade)

  @@index([questionId])
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | UUID | ✅ | 主键 |
| `questionId` | String | ✅ | 外键，所属试题 ID |
| `type` | String | ✅ | svg 或 png |
| `code` | String? | | SVG 代码（type=svg 时存） |
| `src` | String? | | 图片路径（type=png 时存） |
| `alt` | String? | | 图片描述文字 |
| `order` | Int | ✅ | 排序（一题多图时用） |

### 1.5 PaperQuestion（试卷-试题关联）

```prisma
model PaperQuestion {
  id         String @id @default(uuid())
  paperId    String                       // 外键 → Paper.id
  questionId String                       // 外键 → Question.id
  order      Int                          // 题号（1, 2, 3...）

  paper    Paper    @relation(fields: [paperId], references: [id], onDelete: Cascade)
  question Question @relation(fields: [questionId], references: [id], onDelete: Cascade)

  @@unique([paperId, questionId])         // 同一套卷不能重复添加同一题
  @@unique([paperId, order])              // 同一套卷不能有两题占同一序号
  @@index([paperId])
  @@index([questionId])
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | UUID | ✅ | 主键 |
| `paperId` | String | ✅ | 外键，试卷 ID |
| `questionId` | String | ✅ | 外键，试题 ID |
| `order` | Int | ✅ | 题号，从 1 开始 |

### 1.6 ParseTask（解析任务）

```prisma
model ParseTask {
  id        String   @id @default(uuid())
  paperId   String                       // 外键 → Paper.id
  status    String   @default("pending") // pending | processing | completed | failed
  progress  Int      @default(0)         // 进度 0-100
  error     String?                      // 失败时的错误信息
  result    String?                      // 成功时摘要 JSON：{"questionsFound":20}
  createdAt DateTime @default(now())     // 创建时间
  updatedAt DateTime @updatedAt          // 最后修改时间

  paper Paper @relation(fields: [paperId], references: [id], onDelete: Cascade)

  @@index([paperId])
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | UUID | ✅ | 主键 |
| `paperId` | String | ✅ | 外键，所属试卷 ID |
| `status` | String | ✅ | pending=等待 / processing=进行中 / completed=完成 / failed=失败 |
| `progress` | Int | ✅ | 解析进度 0-100 |
| `error` | String? | | 失败时错误信息 |
| `result` | String? | | 完成时结果摘要 JSON |
| `createdAt` | DateTime | 自动 | 创建时间 |
| `updatedAt` | DateTime | 自动 | 最后修改时间 |

### 1.7 SyllabusNode（考纲节点）

考纲以平表存储，通过 `parentCode` 自引用。前端请求时由 API 组装为树结构。按 `examType` 区分不同考试（ESAT / TMUA）。

```prisma
model SyllabusNode {
  id         String   @id @default(uuid())
  code       String                      // 节点编码，如 "112001"
  label      String                      // 节点名称
  examType   String                      // ESAT | TMUA
  parentCode String?                     // 父节点编码，null=根节点
  order      Int      @default(0)        // 同级排序
  createdAt  DateTime @default(now())

  @@unique([examType, code])
  @@index([examType])
  @@index([parentCode])
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | UUID | ✅ | 主键 |
| `code` | String | ✅ | 节点编码，与 examType 联合唯一 |
| `label` | String | ✅ | 节点显示名称 |
| `examType` | String | ✅ | 考试类型：ESAT / TMUA |
| `parentCode` | String? | | 父节点编码，null 为根 |
| `order` | Int | ✅ | 同级排序序号 |
| `createdAt` | DateTime | 自动 | 创建时间 |

**组装为树的逻辑**（API 层实现）：

```ts
// GET /api/papers/syllabus?examType=ESAT
const nodes = await prisma.syllabusNode.findMany({
  where: { examType }, orderBy: { order: 'asc' }
})
// 按 parentCode 挂接 children，叶子节点无需 children 字段
```

---

## 三、用户与答题记录

### 3.1 ER 关系

```
User ──1:N── ExamRecord ──1:N── AnswerRecord ──N:1── Question
                                                      │
                                                  Question ──N:1── Paper（通过 PaperQuestion）
```

### 3.2 User（用户）

```prisma
model User {
  id             String   @id @default(uuid())
  name           String                       // 用户名（昵称）
  email          String   @unique            // 邮箱（唯一，用于登录）
  password       String                       // 密码哈希
  role           String   @default("student") // student | admin
  paymentStatus  String   @default("free")    // free | paid | expired
  diagnosticUsed Boolean  @default(false)     // 是否已绑定/使用诊断报告
  avatar         String?                      // 头像 URL
  createdAt      DateTime @default(now())     // 注册时间
  updatedAt      DateTime @updatedAt          // 最后修改时间

  examRecords      ExamRecord[]
  diagnosticSession DiagnosticSession[]
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | UUID | 是 | 主键 |
| `name` | String | 是 | 用户名/昵称 |
| `email` | String | 是 | 邮箱，唯一，用于登录 |
| `password` | String | 是 | 密码哈希 |
| `role` | String | 是 | student=学生 / admin=管理员 |
| `paymentStatus` | String | 是 | free=免费 / paid=付费 / expired=已过期 |
| `diagnosticUsed` | Boolean | 是 | 是否已绑定/使用诊断报告 |
| `avatar` | String? | 否 | 头像图片 URL |
| `createdAt` | DateTime | 自动 | 注册时间 |
| `updatedAt` | DateTime | 自动 | 最后修改时间 |

### 3.3 ExamRecord（答题会话）

```prisma
model ExamRecord {
  id            String    @id @default(uuid())
  userId        String                       // 外键 → User.id
  paperId       String                       // 外键 → Paper.id
  totalQuestions Int      @default(0)        // 总题数
  correctCount  Int      @default(0)         // 答对题数
  startedAt     DateTime                     // 开始答题时间
  submittedAt   DateTime?                    // 提交时间（null=未提交）
  status        String    @default("in_progress")  // in_progress | submitted

  user    User          @relation(fields: [userId], references: [id])
  paper   Paper         @relation(fields: [paperId], references: [id])
  answers AnswerRecord[]

  @@index([userId])
  @@index([paperId])
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | UUID | ✅ | 主键 |
| `userId` | String | ✅ | 外键，答题用户 |
| `paperId` | String | ✅ | 外键，所做试卷 |
| `totalQuestions` | Int | ✅ | 试卷总题数 |
| `correctCount` | Int | ✅ | 答对数量（提交时计算） |
| `startedAt` | DateTime | ✅ | 开始答题时间 |
| `submittedAt` | DateTime? | | 提交时间，null 表示未提交 |
| `status` | String | ✅ | in_progress=答题中 / submitted=已提交 |

### 3.4 AnswerRecord（逐题答题明细）

```prisma
model AnswerRecord {
  id              String  @id @default(uuid())
  examRecordId    String                     // 外键 → ExamRecord.id
  questionId      String                     // 外键 → Question.id
  selectedAnswer  String?                    // 学生选的选项（如 "B"），null=未作答
  isCorrect       Boolean @default(false)    // 是否答对
  answeredAt      DateTime?                  // 作答时间

  examRecord ExamRecord @relation(fields: [examRecordId], references: [id], onDelete: Cascade)
  question   Question   @relation(fields: [questionId], references: [id])

  @@index([examRecordId])
  @@index([questionId])
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | UUID | ✅ | 主键 |
| `examRecordId` | String | ✅ | 外键，所属答题会话 |
| `questionId` | String | ✅ | 外键，所答题目 |
| `selectedAnswer` | String? | | 学生选择的选项，null=跳过未答 |
| `isCorrect` | Boolean | ✅ | 是否答对（与 `Question.answer` 比对） |
| `answeredAt` | DateTime? | | 作答时间 |

### 3.5 错题本查询

```ts
// 某学生的所有错题
prisma.answerRecord.findMany({
  where: {
    examRecord: { userId: 'xxx' },
    isCorrect: false,
  },
  include: {
    question: { include: { images: true } },
    examRecord: { include: { paper: true } },
  },
  orderBy: { answeredAt: 'desc' },
})
```

### 3.6 自由组卷查询

```ts
// 按学科 + 难度 + 题型筛选（自由组卷必须限定 type，防止真题/模考题/仿真题混搭）
prisma.question.findMany({
  where: {
    subject: 'Math',
    difficulty: 3,
    type: 'exam',       // 只选真题，不与模考题混淆
    status: 'published',
  },
  take: 5,
  include: { images: true },
})

// 筛选结果直接创建 PaperQuestion 关联生成试卷
```

---

## 四、全部表一览

| 表名 | 所属模块 | 用途 |
|------|---------|------|
| `Paper` | 试题试卷 | 试卷元信息 + questions JSON 快照 |
| `Question` | 试题试卷 | 单道试题（自由组卷的基础） |
| `QuestionImage` | 试题试卷 | 试题配图（SVG/PNG） |
| `PaperQuestion` | 试题试卷 | 试卷与试题多对多关联（含排序） |
| `ParseTask` | 试题试卷 | PDF 上传后的解析任务进度 |
| `SyllabusNode` | 试题试卷 | 考纲节点（平表存储，按 examType 查） |
| `User` | 用户 | 用户账号信息 |
| `ExamRecord` | 用户 | 一次答题会话 |
| `AnswerRecord` | 用户 | 逐题答题明细 |
| `DiagnosticSession` | 用户 | 匿名诊断报告 |
| `RevenueCost` | 营收与成本 | 管理员后台成本管理 |

---

## 五、营收与成本

### 5.1 RevenueCost（成本记录）

管理员后台「营收与数据」中的成本导入、查询与编辑记录。

```prisma
model RevenueCost {
  id                  String   @id @default(uuid())
  rechargeItem        String
  amount              Float
  operator            String
  occurredAt          DateTime
  reimbursementStatus String   @default("unreimbursed")
  remark              String?
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  @@index([occurredAt])
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | String | 是 | 主键，UUID |
| `rechargeItem` | String | 是 | 充值项：deepseek / claude / codex |
| `amount` | Float | 是 | 充值金额 |
| `operator` | String | 是 | 操作人：S / L / P / SS |
| `occurredAt` | DateTime | 是 | 成本发生时间 |
| `reimbursementStatus` | String | 是 | unreimbursed=未报销 / reimbursing=报销中 / reimbursed=已报销 / non_reimbursable=不可报销 |
| `remark` | String? | 否 | 备注 |
| `createdAt` | DateTime | 自动 | 创建时间 |
| `updatedAt` | DateTime | 自动 | 最后修改时间 |

*最后更新：2026-06-12*
