# 登录模块技术方案

> 描述当前认证与权限体系的实现方式，后续变更需同步更新此文档。

---

## 一、模块总览

```
前端                                 后端
stores/auth.ts  ──API──→  routes/auth.ts     → User 表
stores/auth.ts  ──API──→  routes/member.ts   → UserMembership + EntitlementConfig
router/index.ts           middleware/auth.ts  → JWT 校验
                           middleware/admin.ts → 角色校验
```

| 文件 | 职责 |
|------|------|
| `api/src/routes/auth.ts` | 注册 / 登录 / 登出 |
| `api/src/routes/member.ts` | 当前用户会员权益上下文 |
| `api/src/middleware/auth.ts` | `requireAuth` — Bearer Token 校验，失败返回 401 |
| `api/src/middleware/admin.ts` | `requireAdmin` — role=admin 校验，失败返回 403 |
| `api/src/services/jwt.ts` | JWT 签发（7 天）+ 验证 |
| `api/src/services/member.ts` | 会员上下文汇总：各考试类型的订阅状态与用量配额 |
| `api/src/utils/userPresenter.ts` | `formatUserForClient` — 按角色输出用户信息 |
| `quiz-web/src/stores/auth.ts` | Pinia store：登录态 / 会员上下文 / localStorage 持久化 |
| `quiz-web/src/api/auth.ts` | 前端 API 层：login / register / logout |
| `quiz-web/src/api/member.ts` | 前端 API 层：getMember |
| `quiz-web/src/router/index.ts` | 路由守卫：未登录拦截、admin 角色校验 |

---

## 二、User 表

```prisma
model User {
  id             String   @id @default(uuid())
  name           String
  email          String   @unique
  password       String              // bcrypt 哈希
  role           String   @default("student")
  paymentStatus  String   @default("free")
  diagnosticUsed Boolean  @default(false)
  avatar         String?
  examPreferences String   @default("[]")  // JSON: [{ examType, subjects }]
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  examRecords       ExamRecord[]
  diagnosticSession DiagnosticSession[]
  memberships       UserMembership[]
}
```

| 字段 | 说明 |
|------|------|
| `role` | student / admin |
| `paymentStatus` | 历史字段，会员状态以 `UserMembership` + `EntitlementConfig` 为准 |
| `diagnosticUsed` | 历史字段，诊断额度以会员系统的 `diagnostic.limit/used` 为准 |

关联表：

```prisma
model MembershipPlan {
  id           String   @id @default(uuid())
  examType     String
  plan         String              // free / pro / admin
  priceCents   Int      @default(0)
  durationDays Int      @default(30)
  status       String   @default("active")
}

model UserMembership {
  id        String   @id @default(uuid())
  userId    String
  examType  String
  plan      String
  status    String   @default("active")
  startsAt  DateTime
  endsAt    DateTime

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model EntitlementConfig {
  id                String   @id @default(uuid())
  examType          String   @unique
  diagnosticLimit   Int      @default(2)
  questionBankLimit Int      @default(100)
  status            String   @default("active")
}
```

---

## 三、JWT 鉴权

### 令牌规格

| 属性 | 值 |
|------|-----|
| 有效期 | 7 天 |
| 存储位置 | 前端 localStorage |
| 传输方式 | `Authorization: Bearer <token>` |
| Payload | `{ userId, email, role }` |
| 签名算法 | HS256 |

### 签发与验证

`api/src/services/jwt.ts`：

```ts
export function signToken(user: { id: string; email: string; role: string }): string
export function verifyToken(token: string): JwtPayload
```

- `signToken` — 签发 Token，写入 `exp`（7 天后过期）
- `verifyToken` — 调用 `jwt.verify()`，过期自动抛错，中间件捕获后返回 401

### 中间件

`requireAuth`：从 `Authorization: Bearer <token>` 提取 Token → `verifyToken` → 写入 `req.user`。失败返回 `401 fail('请先登录')`。

`requireAdmin`：在 `requireAuth` 之后，检查 `req.user.role === 'admin'`。失败返回 `403 fail('无权限')`。

---

## 四、Auth 路由

路由挂载：`app.use('/api/auth', authRouter)`，全路由加速率限制（1 分钟 5 次）。

### POST /api/auth/register

```
输入：{ email, password, confirmPassword, name, examPreferences? }
  examPreferences: [{ examType: "ESAT"|"TMUA"|"STEP", subjects: [...] }]（选填）
校验：
  - 邮箱 + 密码 + 姓名为必填
  - 密码长度 8-32 位，必须包含字母和数字
  - password === confirmPassword
逻辑：
  1. 查邮箱是否已注册 → 409 冲突
  2. bcrypt.hash(password, 12) → 创建 User
  3. signToken → 签发 JWT
返回：{ user, token }
```

### POST /api/auth/login

```
输入：{ email, password }
逻辑：
  1. 查邮箱 → bcrypt.compare()
  2. 密码不匹配 → 401 '邮箱或密码错误'（不区分用户是否存在）
  3. signToken → 签发 JWT
返回：{ user, token }
```

### POST /api/auth/logout

```
鉴权：requireAuth
逻辑：无服务端状态，仅返回成功。前端自行清除 localStorage。
返回：{ data: null }
```

---

## 五、会员上下文

路由挂载：`app.use('/api/getMember', memberRouter)`。

### GET /api/getMember

```
鉴权：requireAuth
返回：{
  user: { id, name, email, role, avatar },
  role,
  isAdmin,
  memberships: [{ examType, plan, status, startsAt, endsAt, remainingDays }],
  examPreferences: [{ examType, subjects }],
  quotas: {
    "TMUA": {
      status, isMember, plan, startsAt, endsAt, remainingDays,
      diagnostic: { limit, used, remaining, unlimited },
      questionBank: { limit, used, remaining, unlimited }
    },
    "ESAT": { ... }
  }
}
```

`quotas` 以 examType 为 key，前端用 `quotas["TMUA"].diagnostic.remaining` 直接取值，无需遍历数组。

---

## 六、诊断测试权限

诊断测试相关路由挂载：`app.use('/api/diagnostic', diagnosticRouter)`。

### GET /api/diagnostic/questions（需登录）

```
鉴权：requireAuth
逻辑：
  1. 校验 examType 参数
  2. checkMemberAccess(userId, 'diagnostic', examType, 1) → 额度不足 403
  3. 从 Question 表随机取题（published + 匹配 examType），截取 20 题
返回：{ questions, answers }
```

### POST /api/diagnostic/submit（需登录）

```
鉴权：requireAuth
输入：{ answers, questionAnswers, examType }
逻辑：
  1. 校验 examType + checkMemberAccess（扣额度前再次确认）
  2. 逐题比对正确答案 → 计算 correctCount
  3. 创建 DiagnosticSession（status='linked'，绑定 userId）
  4. 生成完整报告 → buildFullReportFromSession
返回：{ report: FullReport }
```

### GET /api/diagnostic/report/:id（需登录）

```
鉴权：requireAuth
逻辑：查 DiagnosticSession，校验 userId 归属，生成完整报告
返回：{ report: FullReport }
```

---

## 七、前端

### Auth Store（`stores/auth.ts`）

```
状态：
  user, token, memberContext, loading, error

计算属性：
  isLoggedIn, isAdmin, isPaid,
  entitlements → quotas 按 examType 展开为 { examType, isMember, diagnostic, questionBank }
  permissions → { isAdmin, canAccessAdmin }

初始化：
  initFromStorage() → 从 localStorage 恢复 token + user + memberContext

登录/注册：
  login/register() → 调 API → 存 token + user → 页面调用 getMember() → setMemberContext()

退出：
  logout() → 清空 token + user + memberContext + localStorage
```

### 路由守卫（`router/index.ts`）

```
需登录：/profile, /practice, /assessment, /mistake-notebook
  → 未登录跳 /login

需 admin：/admin/*
  → 非 admin 跳 /

已登录访问 /login 或 /register
  → 跳 /
```

### 登录/注册页面

- `LoginView.vue`：邮箱 + 密码 → `auth.login()` → `getMember()` → `setMemberContext()` → 跳首页
- `RegisterView.vue`：姓名 + 邮箱 + 密码 + 确认密码 → `auth.register()` → `getMember()` → `setMemberContext()` → 跳首页

---

## 八、环境变量

```env
# api/.env
JWT_SECRET=<强随机字符串>
```

生产环境必须替换默认值。生成命令：

```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

---

## 九、安全措施

| 措施 | 实现 |
|------|------|
| 密码存储 | bcrypt hash（salt rounds=12） |
| 密码传输 | 生产环境强制 HTTPS |
| 暴力破解 | 登录/注册 1 分钟 5 次限速 |
| 错误信息模糊 | 登录失败统一 `邮箱或密码错误`，不区分用户是否存在 |
| SQL 注入 | 全部查询走 Prisma ORM |
| CSRF | Bearer Token 方案天然免疫（不依赖 Cookie） |
| CSP | 生产环境配置 Content-Security-Policy 头 |

---

## 十、依赖

| 包 | 用途 |
|------|------|
| `bcryptjs` | 密码哈希 |
| `jsonwebtoken` | JWT 签发与验证 |
| `express-rate-limit` | 速率限制 |

*最后更新：2026-06-26*

---

## 十一、邮箱验证方案（暂缓启用）

### 11.1 当前状态

由于当前项目尚未申请正式域名，也未完成邮件服务商配置，邮箱验证功能暂缓启用。

当前注册和登录仍使用原逻辑：

```text
用户填写注册信息 -> 后端创建账号 -> 后端签发 JWT -> 前端保存登录态 -> 进入系统
```

后端已保留邮箱验证相关代码、数据表和接口，但默认不开启。只有显式配置：

```env
EMAIL_VERIFICATION_ENABLED=true
```

时，注册才会切换到邮箱验证流程。

### 11.2 目标流程

正式启用后，注册流程调整为：

```text
用户填写注册信息
-> 后端创建未验证账号
-> 生成邮箱验证 token
-> 发送验证邮件
-> 用户点击邮件中的验证链接
-> 后端校验 token
-> 标记邮箱已验证
-> 用户登录
```

注册接口在邮箱验证模式下不直接签发 JWT，用户必须完成邮箱验证后才能登录。

### 11.3 数据模型

`User` 表新增：

| 字段 | 说明 |
| --- | --- |
| `emailVerifiedAt` | 邮箱验证完成时间；为空代表未验证 |

新增 `EmailVerificationToken` 表：

| 字段 | 说明 |
| --- | --- |
| `userId` | 对应用户 |
| `email` | 验证目标邮箱 |
| `tokenHash` | 验证 token 的 SHA-256 hash |
| `expiresAt` | 过期时间 |
| `usedAt` | 使用时间，非空代表已消费 |
| `createdAt` | 创建时间 |

安全要求：

- 邮件链接中的明文 token 不落库。
- 数据库只保存 token hash。
- token 单次使用。
- token 默认 24 小时有效。
- 历史用户默认视为已验证，避免上线后影响老账号登录。

### 11.4 接口设计

#### POST /api/auth/register

邮箱验证关闭时：

```text
创建账号 -> 签发 JWT -> 返回 { user, token }
```

邮箱验证开启时：

```text
创建未验证账号 -> 发送验证邮件 -> 返回 { userId, email, verificationRequired }
```

#### POST /api/auth/login

邮箱验证开启后，登录时增加判断：

- 邮箱或密码错误：返回 `AUTH_WRONG`
- 邮箱未验证：返回 `EMAIL_NOT_VERIFIED`
- 邮箱已验证：签发 JWT

#### POST /api/auth/verify-email

输入：

```json
{ "token": "邮件链接中的 token" }
```

处理：

- 校验 token 是否存在
- 校验 token 是否过期
- 校验 token 是否已使用
- 标记 `User.emailVerifiedAt`
- 标记 `EmailVerificationToken.usedAt`

#### POST /api/auth/resend-verification

用于重新发送验证邮件。

适用场景：

- 用户注册后未收到邮件
- 验证链接过期
- 登录时提示邮箱未验证

### 11.5 邮件服务配置

正式启用前需要准备：

```env
EMAIL_VERIFICATION_ENABLED=true
FRONTEND_URL=https://你的前端域名
RESEND_API_KEY=<邮件服务商 API Key>
MAIL_FROM="FrontierVUE <noreply@mail.yourdomain.com>"
EMAIL_VERIFY_TTL_HOURS=24
```

其中：

- `FRONTEND_URL` 用于生成邮件验证链接。
- `RESEND_API_KEY` 用于调用 Resend 发送邮件。
- `MAIL_FROM` 必须使用已完成 DNS 验证的发件域名。
- 建议使用 `mail.yourdomain.com` 这类子域名发送系统邮件。

### 11.6 前端页面

已预留：

- `/verify-email`：邮箱验证结果页
- 登录页未验证邮箱提示
- 重发验证邮件入口

当前默认注册页不展示邮箱验证步骤，保持原注册后直接登录。

### 11.7 暂缓原因

当前暂缓启用的原因：

- 尚未申请正式域名。
- 无法完成 Resend 的域名 DNS 验证。
- 未配置真实发件地址。
- 本地调试链接不能替代真实用户邮箱验证体验。

### 11.8 后续启用步骤

1. 申请正式域名。
2. 在 Resend 添加发件域名，建议使用子域名。
3. 按 Resend 要求配置 DNS 记录。
4. 创建 `RESEND_API_KEY`。
5. 配置生产环境 `.env`。
6. 设置 `EMAIL_VERIFICATION_ENABLED=true`。
7. 重启后端服务。
8. 使用真实邮箱完成注册验证测试。

*最后更新：2026-06-28*
