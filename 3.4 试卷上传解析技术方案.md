# 试卷上传解析技术方案（前端渲染 + 流式上传 + 并发控制）

## 一、整体流程

```
用户选择 PDF
  → 浏览器 pdf.js 逐页渲染页面为图片
  → 创建解析任务（元数据）
  → 逐页上传渲染好的图片
  → 后端收到一页立即调 Qwen 大模型识别题目
  → 信号量限制最多 5 个 Qwen 同时解析
  → 全部完成后入库
  → 前端轮询进度 → 完成
```

---

## 二、前端 PDF 渲染

### 2.1 技术选型

使用 Mozilla 的 **pdf.js**（`pdfjs-dist` 包），在浏览器端将 PDF 每页渲染为图片。选型考量：

| 考量点 | 说明 |
|--------|------|
| 无后端依赖 | 渲染在用户浏览器完成，服务器不需要 Python 环境 |
| 体积可控 | 包体约 2-3MB，gzip 后约 400KB，按需加载 |
| Worker 加速 | pdf.js 内部使用 Web Worker 解析 PDF，不阻塞 UI 主线程 |

### 2.2 渲染流程

1. **加载 PDF**：将用户选择的文件读入内存，pdf.js 打开文档获取总页数
2. **逐页渲染**：循环每一页，先提取文字判断是否为封面（前 2 页文字 < 100 字符则跳过），然后渲染到 Canvas 画布上
3. **输出图片**：Canvas 转为 JPEG 格式（质量 0.85），再 Base64 编码为文本字符串
4. **进度回调**：每完成一页通知 UI 更新进度条

### 2.3 渲染参数

| 参数 | 值 | 说明 |
|------|:---:|------|
| 渲染倍率 | 1.5 | 相当于 108 DPI，比原分辨率更清晰，体积仍可控 |
| 图片格式 | JPEG | 比 PNG 小约 65%，Qwen 识别无差异 |
| 质量系数 | 0.85 | 平衡清晰度与文件大小 |
| 封面跳过 | 前 2 页文字 < 100 字符 | 过滤封面、目录等无题目页面 |

### 2.4 代码引用

- 实现文件：[quiz-web/src/utils/pdfRenderer.ts](quiz-web/src/utils/pdfRenderer.ts)
- 调用方：[quiz-web/src/views/admin/PaperUpload.vue](quiz-web/src/views/admin/PaperUpload.vue)

---

## 三、流式逐页上传

### 3.1 设计思路

旧方案是将所有页面打包成一个 JSON 请求发送。对于 40 页试卷，单次请求体约 10MB，网络不稳定时容易超时，且必须等全部传完才能开始解析。

新方案拆成两步：先创建任务，再逐页提交。每页请求体仅约 250KB，单页失败自动跳过，边传边解析。

### 3.2 前后端调用时序

```
前端                                后端
 |                                   |
 |-- POST /api/upload/paper-pages/create --> 创建 Paper + ParseTask
 |       { title, year, duration, totalPages }
 |<---- { paperId, taskId } -----------|
 |                                   |
 |-- POST /api/parse-tasks/:id/pages --> 收到第 1 页，调 Qwen（异步）
 |       { page: 1, base64: "...", totalPages }
 |<---- { accepted: true } ------------|     （立刻返回，不等 Qwen）
 |                                   |            ↓ 后台解析中
 |-- POST /api/parse-tasks/:id/pages --> 收到第 2 页，调 Qwen（异步）
 |       { page: 2, base64: "...", totalPages }
 |<---- { accepted: true } ------------|
 |                                   |
 | ... 重复直到全部上传 ...             | 多页 Qwen 并发执行中
 |                                   |
 |-- GET /api/parse-tasks/:id --------> 轮询进度
 |<---- { progress: 45 } -------------|
 |                                   |
 | ... 每 2 秒轮询 ...                |
 |                                   | 全部完成 → 题目入库
 |<---- { status: "completed" } ------|
```

### 3.3 关键设计点

- **立刻返回**：后端收到页面后只做基本校验，返回 200 OK 让前端继续下一页，Qwen 调用在后台执行
- **单页容错**：某页上传或解析失败，不影响其他页，不会阻塞整个流程
- **进度分段**：前端进度条分两段 —— 渲染阶段 0-50%，上传阶段 50-100%，随后切到后端解析进度轮询
- **重试机制**：前端缓存所有渲染好的页面，重试时直接用缓存数据重新发送，无需重新渲染

### 3.4 代码引用

- 前端路由：[quiz-web/src/views/admin/PaperUpload.vue](quiz-web/src/views/admin/PaperUpload.vue)（`startUpload` 方法）
- 后端 create 路由：[api/src/routes/upload.ts](api/src/routes/upload.ts)（`POST /paper-pages/create`）
- 后端 page 路由：[api/src/routes/parse.ts](api/src/routes/parse.ts)（`POST /:id/pages`）
- 解析核心：[api/src/services/parseService.ts](api/src/services/parseService.ts)（`addPageToTask` 方法）

---

## 四、后端 Qwen 并发控制

### 4.1 问题场景

逐页上传意味着 12 页试卷 ≈ 12 个 Qwen 调用几乎同时触发。40 页就是 40 个同时调用。大量并发会导致：

- 触发 DashScope API 的并发/频率限制（返回 429 错误）
- 被限流的页面解析失败，题目永久丢失
- 无法预知什么时候会触发限制，稳定性差

### 4.2 解决方案：信号量限流

在 Qwen 调用入口加入**信号量（Semaphore）**，限制同时进行的 Qwen 调用数量。

**工作方式**（类比停车场）：

- 停车场有 5 个车位（信号量初始值 = 5）
- 进来一辆车（一个 Qwen 调用），占用一个车位
- 车位满了，后续车辆在入口排队
- 有车离开（Qwen 调用完成），释放一个车位，队首车进入

### 4.3 并发数选择

| 并发数 | 效果 |
|:------|------|
| 1-2 | 串行等待，40 页需 5-7 分钟 |
| 5 | **当前选择**，40 页约 2-3 分钟，安全不触发限流 |
| >10 | 可能触发 429，题目丢失 |

### 4.4 协调器：多页结果汇合

多个 Qwen 调用可能同时完成。需要一个**协调器（TaskCoordinator）**来串行化结果写入：

1. 每个 Qwen 调用在锁外执行（可并发）
2. 完成后进入协调器锁，获取写入权
3. 将本页识别出的题目追加到总结果列表
4. 更新完成计数和进度百分比
5. 释放锁

由于写入操作的耗时仅 1-2 毫秒，锁几乎不存在等待。实际 5 个并发 Qwen 调用（各 10-15 秒）在写入时的等待可以忽略不计。

### 4.5 代码引用

- 信号量实现：[api/src/services/parseService.ts](api/src/services/parseService.ts)（`Semaphore` 类）
- 协调器：[api/src/services/parseService.ts](api/src/services/parseService.ts)（`TaskCoordinator` 接口 + `addPageToTask` 方法）

---

## 五、问题处理

### 5.1 SVG 缺少宽高

Qwen 偶发忘记输出 `width`/`height` 属性。后端入库时自动检查并补全：`<svg>` → `<svg width="100%" height="auto">`。

### 5.2 多段题干合并

Qwen 有时将多段落题目合并为单段。提示词中已包含段落自检清单（要求数原图段落数与输出 `\n\n` 次数一致），后续可进一步优化。

### 5.3 封面页误解析

前端 pdf.js 渲染时跳过前 2 页文字少于 100 字符的页面（封面/目录），减少无效 Qwen 调用。

---

## 六、文件清单

| 文件 | 职责 |
|------|------|
| [quiz-web/src/utils/pdfRenderer.ts](quiz-web/src/utils/pdfRenderer.ts) | 前端 pdf.js 渲染 PDF → JPEG Base64 |
| [quiz-web/src/views/admin/PaperUpload.vue](quiz-web/src/views/admin/PaperUpload.vue) | 上传页面：渲染 → 逐页 POST → 轮询 |
| [api/src/routes/upload.ts](api/src/routes/upload.ts) | `POST /api/upload/paper-pages/create` |
| [api/src/routes/parse.ts](api/src/routes/parse.ts) | `GET /:id` 状态查询 + `POST /:id/pages` 逐页提交 |
| [api/src/services/parseService.ts](api/src/services/parseService.ts) | 信号量 + 协调器 + 核心解析流程 |
| [api/src/services/qwenService.ts](api/src/services/qwenService.ts) | DashScope Qwen-VL-Max API 封装 |
