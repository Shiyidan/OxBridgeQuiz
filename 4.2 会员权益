# 4.2 会员权益

> 本文档用于定义学生端会员权益、免费额度、后台管理能力与权限控制边界。当前阶段先作为产品与技术实现约束，后续支付样式、弹窗样式和字段枚举可继续补充。

---

## 一、目标

建立一套围绕 `examType` 的会员权益体系，让不同考试类型可以独立配置免费额度、会员套餐和使用权限。

核心目标：

- 管理员拥有全产品操作权限与使用权限。
- 学生按考试类型享有免费额度或付费会员权益。
- 免费学生在每个考试类型下可免费使用有限次数/题量。
- 月度会员与年度会员按考试类型单独购买，权益相同，仅价格与有效期不同。
- 前端通过统一会员接口获取权限上下文，后端负责最终权限判定。

---

## 二、角色与权限层级

系统权限分为三层：

### 1. 身份认证层

判断用户是否登录，以及当前用户是谁。

核心内容：

- `userId`
- token 有效性
- 登录状态

### 2. 角色权限层

区分管理员与学生。

| 角色 | 权限说明 |
|------|----------|
| admin | 进入管理员后台，拥有整个产品的所有管理、操作和使用权限 |
| student | 进入学生端，受会员权益与免费额度限制 |

管理员不受学生端会员额度限制。即使管理员访问学生端功能，也应默认放行。

### 3. 会员权益层

学生端功能按 `userId + examType` 判断权益。

权益判断维度：

- 是否为指定 `examType` 的有效会员
- 是否仍有该 `examType` 的免费诊断额度
- 是否仍有该 `examType` 的试题库练习额度

---

## 三、考试类型维度

会员权益、免费额度、题库、试卷和诊断测试均需要归属到考试类型。

当前规划：

```text
examType = TMUA / ESAT / ENGAA / NSAA
```

具体字段枚举值后续可再确认，但产品逻辑上必须统一使用 `examType` 作为主维度。

后台上传试卷时，需要区分：

```text
examType = 所属考试类型
paperType = 试卷用途类型
```

示例：

| 内容 | examType | paperType |
|------|----------|-----------|
| TMUA 2024 真题 | TMUA | past |
| TMUA 代数练习 | TMUA | practice |
| ESAT 模考卷 1 | ESAT | mock |
| ENGAA 诊断卷 | ENGAA | diagnostic |

`examType` 与 `paperType` 不应混用。

---

## 四、免费学生权益

免费学生在每个考试类型下分别拥有独立免费额度。

| 权益项 | 免费额度 | 统计维度 |
|--------|----------|----------|
| 诊断测试 | 2 次 | `userId + examType` |
| 试题库练习 | 100 道 | `userId + examType` |

不同考试类型之间额度互不影响。

例如同一学生：

| examType | 诊断免费额度 | 试题库免费额度 |
|----------|--------------|----------------|
| TMUA | 2 次 | 100 道 |
| ESAT | 2 次 | 100 道 |
| ENGAA | 2 次 | 100 道 |

---

## 五、诊断测试额度规则

诊断测试属于整卷行为，按次数限制。

规则：

- 每个 `examType` 免费诊断测试 2 次。
- 点击“开始诊断测试”前进行额度检查。
- 如果该 `examType` 下已用满 2 次，则不能开始新的诊断测试。
- 诊断测试次数在交卷成功后消耗。
- 中途退出、网络失败、未交卷不消耗次数。
- 历史诊断报告可继续查看，不受会员过期影响。

后端必须在开始诊断测试接口中进行最终判断。

### 诊断次数统计口径

诊断测试可能来自两类流程：

- 独立诊断接口生成的 `DiagnosticSession`
- 诊断测试页复用在线答题页后生成的 `ExamRecord`

因此诊断已用次数必须合并统计：

```text
DiagnosticSession.status = linked
+ ExamRecord.status = submitted 且 Paper.paperType in (past, diagnostic, mock)
```

不能只统计其中一张表，否则会出现某一类诊断入口不消耗额度的问题。

---

## 六、试题库练习额度规则

试题库免费额度只统计试题库练习题，不统计真题、模考、诊断测试。

计入范围：

```text
examType = 当前考试类型
paperType = practice / question-bank
```

不计入范围：

```text
paperType = past
paperType = mock
paperType = diagnostic
```

不计入免费额度的行为：

- 真题套卷练习
- 模考试卷
- 诊断测试
- 错题本查看
- 试题解析查看
- 历史练习报告查看

### 额度消耗口径

MVP 阶段不新增题目浏览记录表。

试题库练习额度按交卷成功后的 `AnswerRecord` 数量累计消耗：

- 本次练习提交 5 道题，消耗 5 道额度。
- 跳过/未答的题如果生成 `AnswerRecord`，也计入消耗。
- 中途退出未交卷，不消耗额度。
- 查看错题或解析，不额外消耗额度。

---

## 七、额度不足时的处理

试题库练习在开始前检查本次题量是否超过剩余额度。

判断逻辑：

```text
如果 role = admin：
  放行

如果 student 当前 examType 有效会员：
  放行

如果 student 当前 examType 非会员：
  获取剩余试题库额度 remaining
  如果本次练习题数 <= remaining：
    放行
  如果本次练习题数 > remaining：
    拒绝开始，返回 remaining
```

前端收到额度不足时弹窗引导付费。

弹窗应包含：

- 当前考试类型
- 当前剩余额度
- 本次选择题量
- 开通月度会员
- 开通年度会员
- 调整题目数量
- 取消

如果后续增加自由组卷功能，则额度不足时推荐引导用户进入自由组卷：

```text
当前免费额度剩余 5 道，本套练习包含 10 道。
你可以开通会员，或前往自由组卷选择最多 5 道题开始练习。
```

自由组卷中，免费学生可选题目数量必须小于等于当前 `examType` 的剩余额度。超出时按钮禁用或弹窗提示开通会员。

---

## 八、会员权益规则

会员按考试类型单独购买。

示例：

| 用户 | examType | 会员状态 |
|------|----------|----------|
| 张三 | TMUA | 月度会员 |
| 张三 | ESAT | 免费 |
| 张三 | ENGAA | 年度会员 |

月度会员与年度会员权益相同，仅区别：

- 价格
- 有效期

有效会员在对应 `examType` 下：

- 可进行诊断测试。
- 可进行试题库练习。
- 可查看对应考试类型下的学习记录、错题本和报告。
- 不受免费诊断次数和免费试题库题量限制，或按后台配置的大额度处理。

会员过期后：

- 历史练习记录可查看。
- 历史诊断报告可查看。
- 历史错题本可查看。
- 历史解析可查看。
- 不能继续进行超出免费额度的新练习或诊断。
- 已用完免费额度的考试类型，过期后不能继续免费使用。

---

## 九、统一会员接口

登录成功后，前端应调用统一会员接口获取用户信息、角色、会员和权益上下文。

建议接口：

```http
GET /api/member/me
```

返回内容保持简单，优先服务前端展示与入口判断：

| 字段 | 内容 |
|------|------|
| user | 用户 ID、姓名、邮箱、角色、头像等基础信息 |
| role | 当前用户角色，student / admin |
| isAdmin | 是否为管理员 |
| memberships | 已开通或曾开通过的考试类型会员记录，仅展示 examType、套餐、状态、开始时间、到期时间、剩余天数 |
| examTypes | 各考试类型的当前权益状态，包含会员状态、套餐、到期时间、诊断额度、试题库额度 |

`user.paymentStatus` 是历史兼容字段，仅允许表达学生旧付费状态：`free / paid / expired`，不作为新的会员权益判断依据，也不承载管理员语义。管理员用户不依赖该字段展示权益；学生端具体权益以 `memberships` 和 `examTypes` 为准。

`memberships` 用于展示“我已经开通了哪些考试类型”：

| 字段 | 说明 |
|------|------|
| examType | 考试类型，如 TMUA / ESAT |
| plan | 套餐类型，free / monthly / yearly / admin |
| status | active / expired / free |
| startsAt | 会员开始时间 |
| endsAt | 会员到期时间 |
| remainingDays | 剩余有效天数 |

`examTypes` 用于前端判断当前考试类型的入口状态：

| 字段 | 说明 |
|------|------|
| examType | 考试类型 |
| status | 当前状态：free / active / expired |
| isMember | 当前是否拥有无限制使用权益 |
| plan | 当前套餐：free / monthly / yearly / admin |
| startsAt | 当前或最近一次会员开始时间 |
| endsAt | 当前或最近一次会员到期时间 |
| remainingDays | 有效会员剩余天数，非有效会员为 0 |
| diagnostic | 诊断测试额度：limit / used / remaining / unlimited |
| questionBank | 试题库额度：limit / used / remaining / unlimited |

其中会员状态需要能表达：

- 免费用户
- 有效会员
- 已过期
- 已取消

套餐类型需要能表达：

- 免费
- 月度会员
- 年度会员

前端 store 保存：

- 用户信息
- 用户角色
- 管理员权限
- 各 `examType` 下会员状态
- 各 `examType` 下诊断和试题库剩余额度

前端 store 用于展示与提前提示，不作为最终权限依据。

---

## 十、后端最终权限判定

所有消耗权益或访问管理能力的关键接口，都必须由后端进行最终判断。

前端可以：

- 控制入口显示
- 控制按钮状态
- 提前弹窗提示
- 展示剩余额度

后端必须：

- 判断用户角色
- 判断指定 `examType` 的会员状态
- 判断免费额度是否足够
- 阻止越权请求
- 返回明确的错误码和剩余额度

额度不足建议返回：

```ts
{
  code: 'QUOTA_EXCEEDED',
  message: '当前免费额度不足',
  data: {
    examType: string
    remaining: number
    requested: number
    upgradeOptions: ['monthly', 'yearly']
  }
}
```

---

## 十一、功能边界

本阶段包含：

- 角色与会员权益模型设计。
- 登录后获取会员权益上下文。
- 学生端按 `examType` 进行诊断和试题库额度限制。
- 管理员后台手动开通和管理会员。
- 免费额度后台可配置的基础设计。

本阶段暂不包含：

- 付费策略

---

## 十二、待确认项

- `examType` 的最终枚举值与展示名称。
- `paperType` 中试题库练习的最终取值，是 `practice` 还是单独的 `question-bank`。
- 免费额度是否按自然周期重置。当前默认不重置。
- 会员过期后是否允许重新获得免费额度。当前默认不重置。
- 会员是否支持退款/撤销后的权益回滚。
- 自由组卷功能的入口、题量限制和交互样式。
- 付费弹窗的具体视觉样式与支付路径。
