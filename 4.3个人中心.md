# 4.3 学生端个人中心 PRD

## 1. 功能概述

学生端个人中心用于集中展示学生的会员权益、不同考试类型下的学习统计、基础资料、报考目标和订阅记录。页面入口来自顶部导航栏头像下拉卡片。

本阶段个人中心重点覆盖：

- 学生从头像下拉菜单进入个人中心。
- 管理员从头像下拉菜单进入后台管理。
- 学生可查看当前已开通的会员权益和套餐模式。
- 学生可按 ESAT、TMUA、STEP 三种考试类型切换查看数据。
- 已开通考试类型展示学习统计。
- 未开通考试类型展示会员开通引导。
- 学生可编辑用户名和邮箱。
- 密码仅展示脱敏占位，不支持在个人中心修改。
- 已付费用户可查看订阅中心和订阅记录。

### 1.1 页面截图索引

以下截图来自当前本地开发环境，用于帮助阅读者理解页面状态和功能分区。

#### 1.1.1 已开通考试类型：学习统计状态

![已开通考试类型下的个人中心统计](./docs-assets/profile/profile-01-opened-exam-stats.png)

截图说明：

- 当前学生已开通 ESAT 月度版。
- 左侧学生卡片展示头像、用户名、会员权益标签和退出登录入口。
- 右侧 ESAT Tab 处于选中状态。
- 已开通考试类型展示预估分数、累计做题、累计考试。
- 基础信息中的用户名称、电子邮箱、用户密码在桌面端同一行展示。

#### 1.1.2 未开通考试类型：会员引导状态

![未开通考试类型下的会员引导](./docs-assets/profile/profile-02-locked-exam-upgrade.png)

截图说明：

- 当前学生仅开通 ESAT，切换到 TMUA 后展示会员引导。
- 未开通考试类型不展示预估分数、累计做题、累计考试。
- 引导区域展示当前考试类型会员权益说明和升级会员按钮。
- 左侧仍保留当前已开通权益标签，避免用户误以为所有会员失效。

#### 1.1.3 基础信息、报考目标和订阅中心

![基础信息、报考目标和订阅中心](./docs-assets/profile/profile-03-basic-subscription.png)

截图说明：

- 基础信息区域展示用户名称、电子邮箱和用户密码。
- 报考目标区域展示年级、目标院校、目标专业、考试类型和考试科目。
- 订阅中心展示当前套餐、累计订阅、总消费和订阅记录。
- 订阅记录支持按状态筛选，并保留续费 / 重新订阅入口。

## 2. 入口与角色跳转

### 2.1 顶部头像下拉卡片

用户点击导航栏右上角头像后，展示用户信息下拉卡片。

下拉卡片展示内容：

- 用户名。
- 当前角色。
- 头像或用户名首字母头像。
- 角色相关入口。
- 退出登录入口。

角色跳转规则：

- 当前用户为管理员时，显示并跳转到后台管理。
- 当前用户为学生时，显示并跳转到个人中心。

### 2.2 退出登录

用户点击退出登录后：

- 前端清理本地登录状态。
- 跳转回首页或登录前默认页。
- 后端 logout 当前为无状态 JWT 登出确认接口，不维护服务端黑名单。

异常点：

- 如果 logout 接口失败，前端仍应清理本地登录状态，避免用户被卡在登录态不一致状态。

## 3. 页面整体布局

个人中心页面包含以下区域：

- 页面标题区。
- 左侧学生信息卡片。
- 右侧考试类型切换和学习统计 / 会员引导区域。
- 基础信息区域。
- 报考目标区域。
- 订阅中心区域。

页面标题区展示：

- 标题：个人中心 (User Center)。
- 描述：管理学习进度、个人信息和目标设定。

已移除右上角全局升级会员横幅。会员引导仅在用户切换到未开通考试类型时，在右侧考试信息区域展示。

## 4. 左侧学生信息卡片

### 4.1 基础展示

左侧卡片展示：

- 头像。
- 用户名。
- 当前已开通的会员权益标签。
- 退出登录按钮。

头像规则：

- 如果用户有头像，展示头像图片。
- 如果用户无头像，展示用户名首字母。

会员标签规则：

- 如果用户已开通考试类型会员，使用标签展示“考试类型 + 套餐模式”。
- 示例：ESAT 月度版、TMUA 专业版。
- 如果用户未开通任何会员，展示免费版。
- 标签使用 Element Plus 的 `el-tag` 视觉样式。

### 4.2 套餐模式展示

套餐模式包括：

- 月度版。
- 专业版 / 年度版。
- 管理员权益。
- 免费版。

异常点：

- 如果后端返回未知套餐类型，前端默认展示免费版或兜底文案，避免空白。
- 如果用户存在多个考试类型权益，需要展示多个标签。

## 5. 考试类型切换

### 5.1 支持考试类型

个人中心固定展示三种考试类型 Tab：

- ESAT。
- TMUA。
- STEP。

无论用户是否开通某个考试类型，三个 Tab 都必须展示。

### 5.2 Tab 样式

Tab 样式需与试题库中的考试类型切换保持一致：

- 未选中：白底、浅色边框、深色文字。
- 选中：蓝色背景、白色文字。
- 支持响应式布局，不应在窄屏发生文字重叠。

### 5.3 默认选中规则

进入个人中心时：

- 如果用户存在已开通考试类型，默认选中第一个已开通考试类型。
- 如果用户没有已开通考试类型，默认选中系统默认考试类型。

异常点：

- 如果用户会员数据加载失败，Tab 仍应展示。
- 如果当前选中考试类型数据缺失，按未开通状态展示会员引导。

## 6. 已开通考试类型的学习统计

当用户切换到已开通的考试类型时，右侧展示三个学习统计项：

- 预估分数。
- 累计做题。
- 累计考试。

### 6.1 预估分数

预估分数定义：

- 当前考试类型下，学生所有已完成诊断测试成绩的平均分。
- 分数按 9 分制展示。
- 单场考试分数按正确题数 / 总题数换算为 9 分制。
- 多场考试取平均值。
- 展示保留一位小数。

统计范围：

- 历年真题。
- 诊断测试。
- 模拟考试。
- 已完成 / 已提交的考试记录。
- 已绑定到用户的诊断测试记录。

异常点：

- 如果当前考试类型已开通，但没有任何已完成诊断测试，预估分数展示占位符。
- 如果考试记录总题数为 0，不参与平均分计算。

### 6.2 累计做题

累计做题定义：

- 当前考试类型下，学生已经做过的题目数量。
- 同一道题重复做多次，只计算一次。
- 当前阶段没有选项的题目也计入统计。
- 统计口径需与错题本按题目聚合的逻辑保持一致。

统计范围：

- 当前考试类型下已提交答题记录中的题目。
- 按题目唯一标识去重。

异常点：

- 如果历史答题记录中题目元数据缺失，只要答题记录存在，仍计入累计做题。
- 如果题目 ID 异常或为空，应避免页面报错，该条记录可不计入或由后端兜底处理。

### 6.3 累计考试

累计考试定义：

- 当前考试类型下，学生已完成诊断测试的场次数。

统计范围：

- 历年真题。
- 诊断测试。
- 模拟考试。
- 已提交考试记录。
- 已绑定诊断测试记录。

异常点：

- 进行中、未提交、被删除或无效考试记录不应计入。

## 7. 未开通考试类型的会员引导

当用户切换到未开通的考试类型时，不展示预估分数、累计做题、累计考试三个统计项。

右侧区域替换为会员开通引导。

### 7.1 引导内容

展示内容：

- 标题：开通当前考试类型会员。
- 描述：解锁当前考试类型的历年真题、海量练习题、预估分分析与模拟考试权益。
- 按钮：升级会员。

示例：

- 开通 TMUA 会员。
- 解锁 TMUA 历年真题、海量练习题、预估分分析与模拟考试权益。

### 7.2 行为

点击升级会员按钮：

- 当前阶段展示提示或预留入口。
- 后续可接入正式支付 / 订阅开通流程。

异常点：

- 如果用户已经开通其他考试类型会员，切换到未开通考试类型时仍展示该考试类型的开通引导。
- 不应显示 `--` 或空统计卡片，避免用户误解为数据缺失。

## 8. 基础信息

### 8.1 展示字段

基础信息区域展示：

- 用户名称。
- 电子邮箱。
- 用户密码。

已移除联系电话字段。

### 8.2 布局

用户名称、电子邮箱、用户密码三项在桌面端同一行展示。

布局要求：

- 三项宽度随页面宽度自适应。
- 输入框不应溢出父容器。
- 窄屏下自动折行或单列展示。

### 8.3 编辑规则

支持编辑：

- 用户名称。
- 电子邮箱。

不支持编辑：

- 用户密码。

密码展示规则：

- 始终展示脱敏占位。
- 始终置灰禁用。
- 不提供修改入口。

保存规则：

- 点击编辑后，用户名称和电子邮箱变为可编辑。
- 点击取消后，还原为当前账号信息。
- 点击保存后提交修改。
- 修改成功后，后续重新登录应使用修改后的用户名 / 邮箱信息展示。

校验规则：

- 用户名称不能为空。
- 用户名称长度不能超过限制。
- 电子邮箱必须符合邮箱格式。
- 邮箱不能与其他用户重复。

异常点：

- 保存失败时展示错误提示，并保留编辑状态。
- 用户邮箱重复时展示明确提示。
- 网络失败时展示通用错误提示。
- 密码字段无论是否处于编辑状态都不可编辑。

## 9. 报考目标

报考目标区域当前展示：

- 所在年级。
- 目标院校。
- 目标专业。
- 考试类型。
- 考试科目。

当前阶段：

- 主要作为信息展示。
- 编辑入口保留。
- 具体编辑保存逻辑尚未完整接入。

未确认项：

- 报考目标数据是否需要入库。
- 是否允许学生自行编辑年级、院校、专业、考试科目。
- 考试科目是否应根据考试类型动态变化。
- 是否支持多目标院校 / 多目标专业。

## 10. 订阅中心

订阅中心仅在用户存在会员权益或订阅记录时展示。

### 10.1 概览信息

订阅中心顶部展示：

- 当前套餐。
- 累计订阅次数。
- 总消费金额。

当前套餐规则：

- 优先展示当前有效会员套餐。
- 如果无有效会员，展示免费版或暂无有效订阅。

总消费规则：

- 当前阶段根据订阅记录中的套餐金额汇总。
- 月度版暂定金额为 69。
- 年度 / 专业版暂定金额为 259。
- 管理员手动权益金额为 0。

未确认项：

- 实际套餐金额是否以后端配置为准。
- 总消费是否应统计退款、取消、手动赠送等特殊记录。

### 10.2 订阅记录

订阅记录展示：

- 订阅标题。
- 订阅周期。
- 支付金额。
- 支付方式。
- 状态。
- 操作按钮。

订阅标题格式：

- 考试类型 + 套餐模式 + 订阅。
- 示例：ESAT-月度版订阅。

支付方式：

- 微信。
- 支付宝。
- 手动。

手动定义：

- 通过后台用户管理手动调整会员权益的记录，支付方式视为手动。

状态：

- 进行中。
- 已过期。
- 已取消。

状态口径：

- 时间自然到期展示已过期。
- 管理员手动取消展示已取消。
- 当前有效权益展示进行中。

操作按钮：

- 进行中记录展示续费。
- 已过期或已取消记录展示重新订阅。

当前阶段：

- 续费和重新订阅按钮保留入口。
- 正式支付流程尚未接入。

异常点：

- 订阅记录为空时，应展示空态或不展示订阅记录列表。
- 手动权益取消后，不应误展示为已过期。
- 支付方式缺失时默认展示手动或未知支付方式。

## 11. 数据统计口径与一致性

### 11.1 考试类型一致性

系统当前支持三种考试类型：

- ESAT。
- TMUA。
- STEP。

个人中心所有统计必须按考试类型隔离。

要求：

- ESAT 会员只展示 ESAT 统计。
- TMUA 会员只展示 TMUA 统计。
- STEP 会员只展示 STEP 统计。
- 用户开通多个考试类型时，切换 Tab 展示对应考试类型统计。

异常点：

- 历史数据中可能存在旧考试类型或错误考试类型，需要后端兼容或数据清理。
- 同名试卷可能存在不同考试类型版本，统计应以记录关联的试卷和考试类型为准，不能仅按试卷标题匹配。

### 11.2 已完成考试口径

已完成考试指：

- 已提交。
- 有提交时间。
- 状态为完成 / submitted。

不计入：

- 未开始。
- 进行中。
- 未提交。
- 无效或被删除记录。

## 12. 权益与额度

个人中心展示依赖会员权益上下文。

权益包括：

- 当前用户是否为管理员。
- 当前用户是否开通某考试类型会员。
- 当前考试类型的诊断测试额度。
- 当前考试类型的题库练习额度。
- 当前考试类型的订阅状态。

展示规则：

- 管理员可视为拥有权益。
- 学生以实际会员记录为准。
- 旧的支付状态字段不应单独作为会员权益的唯一依据。

异常点：

- 如果会员接口失败，页面应保留基础信息展示，并提示部分数据加载失败。
- 如果统计接口失败，会员标签和基础信息仍应可见。
- 如果会员接口与统计接口部分失败，不应导致整个页面白屏。

## 13. 异常与边界场景

### 13.1 数据加载失败

场景：

- 会员接口失败。
- 统计接口失败。
- 当前用户信息过期。
- 登录态失效。

处理：

- 局部展示错误提示。
- 已有用户信息可继续展示。
- 401 时跳转登录。

### 13.2 未开通任何会员

展示：

- 左侧标签展示免费版。
- 右侧展示未开通考试类型的会员引导。
- 不展示订阅中心，或展示空状态。

### 13.3 开通部分考试类型

示例：

- 用户只开通 ESAT。

展示：

- 左侧展示 ESAT 套餐标签。
- ESAT Tab 展示统计。
- TMUA / STEP Tab 展示会员引导。

### 13.4 有历史记录但无当前会员

展示：

- 如果当前考试类型未开通会员，即使存在历史记录，也优先展示会员引导。

未确认项：

- 用户会员过期后，是否还能查看历史统计。
- 用户取消会员后，是否还能查看已产生的统计。

### 13.5 历史脏数据

可能情况：

- 旧考试类型残留，如 ENGAA。
- 同名试卷存在多个考试类型版本。
- 历史考试记录关联 draft 试卷。
- 记录的考试类型与试卷考试类型不一致。

处理建议：

- 页面避免按标题匹配历史记录。
- 后端统计优先采用可靠的考试类型来源。
- 必要时通过数据迁移清理历史记录。

## 14. 未确认项汇总

- 正式会员支付流程入口跳转到哪里。
- 微信、支付宝支付接入时间。
- 升级会员按钮是否打开弹窗、跳转支付页，还是进入套餐选择页。
- 套餐金额是否固定，还是由后台配置。
- 月度版、专业版、年度版的命名是否需要统一。
- 管理员手动开通权益是否需要生成完整订阅记录。
- 用户取消会员后是否还能查看历史统计。
- 用户会员过期后是否还能查看历史统计。
- 报考目标是否支持编辑和保存。
- 报考目标字段是否需要动态化配置。
- 订阅记录是否需要支持发票、退款、订单号。
- 累计做题是否只统计题库练习，还是包含诊断测试中的题目。
- 预估分是否只来自诊断测试，还是未来要加入普通练习表现。
- 管理员角色在学生个人中心中的展示口径是否需要单独设计。

## 15. 当前阶段不包含

本阶段不包含以下能力：

- 正式在线支付。
- 修改密码。
- 手机号绑定。
- 头像上传。
- 报考目标完整编辑保存。
- 订阅续费真实支付。
- 发票、退款、订单详情。
- 多语言切换。
