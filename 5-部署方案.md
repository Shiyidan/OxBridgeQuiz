# QuizTestDemo 阿里云部署方案

> 适用环境：开发测试环境 | 团队：4人 | 目标：长期开发迭代

---

## 一、总体架构

```
                             互联网
                               │
                     ┌─────────▼─────────┐
                     │   Nginx (443/80)  │  HTTPS 反向代理 + 静态资源
                     └──┬────────────┬───┘
                        │            │
              ┌─────────▼──┐    ┌───▼──────────────┐
              │ 静态文件    │    │ 127.0.0.1:3001    │
              │ (Vue SPA)  │    │ Node.js Express   │
              └────────────┘    └───┬───────────────┘
                                    │
                        ┌───────────┼───────────┐
                        │           │           │
                  ┌─────▼────┐ ┌───▼───┐ ┌─────▼─────┐
                  │PostgreSQL│ │ Redis │ │ Python    │
                  │   :5432  │ │ :6379 │ │ :8000     │
                  └──────────┘ └───────┘ │(PDF解析)  │
                                         └───────────┘
```

**核心原则**：所有服务仅监听 `127.0.0.1`，Nginx 作为唯一对外入口。

---

## 二、需要购买的产品清单

### 2.1 必买项

| 产品 | 规格/说明 | 参考月费 | 用途 |
|------|----------|:------:|------|
| **ECS 云服务器** | 2核4G + 5Mbps + 80G ESSD Entry | ~150-200元 | 核心计算资源 |
| **域名** | `.com` / `.cn`，在阿里云万网购买 | ~60元/年 | 对外访问地址 |
| **SSL 证书** | 阿里云免费 DV 证书（单域名，自动续期） | 0元 | HTTPS 加密 |

> **ECS 选购细节**：
> - 系统镜像选 **Ubuntu 22.04 LTS**（Node.js / Python 生态兼容性最好）
> - 地区选离团队最近的 Region（如华东2-上海）
> - 云盘选 ESSD Entry 80G，勾选"随实例释放"

### 2.2 强烈建议购买

| 产品 | 规格/说明 | 参考月费 | 理由 |
|------|----------|:------:|------|
| **域名** | `.com` 或 `.cn` | ~60元/年 | ICP 备案必需，HTTPS 证书必需 |

如果不买域名，只能用 `http://公网IP` 访问，无法申请 SSL 证书（Let's Encrypt 和阿里云免费证书都需要域名），密码等敏感数据在 HTTP 明文传输存在严重安全隐患。

### 2.3 本期不买（后续迭代按需追加）

| 产品 | 当前替代方案 | 何时加 |
|------|------------|--------|
| RDS PostgreSQL | ECS 自建 PostgreSQL | 用户量增大、需要读写分离时 |
| OSS 对象存储 | ECS 本地磁盘 `/uploads` | 文件量超过 20G 或需要 CDN 时 |
| Redis 云服务 | ECS 自建 Redis | 缓存需求增长到需要独立实例时 |
| SLB 负载均衡 | Nginx 单节点 | 需要高可用/多节点时 |
| NAT 网关 | ECS 自带公网 IP | 需要统一出口 IP 时 |

---

## 三、服务器环境搭建

### 3.1 安全组配置（阿里云控制台 → ECS → 安全组）

| 方向 | 端口 | 来源 | 说明 |
|------|:---:|------|------|
| 入方向 | `22` | 公司出口 IP（或 0.0.0.0/0） | SSH 远程管理 |
| 入方向 | `80` | 0.0.0.0/0 | HTTP（用于 SSL 证书验证 + 重定向到 HTTPS） |
| 入方向 | `443` | 0.0.0.0/0 | HTTPS |
| 入方向 | `3001` | **删除/不开放** | API 直连端口，仅通过 Nginx 反向代理访问 |
| 入方向 | `5432` | **删除/不开放** | PostgreSQL，仅本机访问 |
| 入方向 | `6379` | **删除/不开放** | Redis，仅本机访问 |

> **原则：最小暴露面。** 除了 22/80/443 之外一律不对公网开放。

### 3.2 SSH 登录并初始化

```bash
# 1. SSH 登录（建议用密钥对，不要用密码）
ssh root@<你的公网IP>

# 2. 创建管理用户（不要用 root 日常操作）
adduser deploy
usermod -aG sudo deploy
# 将本地 SSH 公钥复制到 /home/deploy/.ssh/authorized_keys

# 3. 系统基础更新
sudo apt update && sudo apt upgrade -y
sudo apt install -y curl wget git build-essential ufw nginx certbot python3-certbot-nginx

# 4. 配置防火墙（作为安全组之外的二层防线）
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable
```

### 3.3 安装 Node.js 20

```bash
# 使用 NodeSource 官方源
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# 验证
node -v   # v20.x.x
npm -v

# 安装 PM2（全局进程管理器）
sudo npm install -g pm2
```

### 3.4 安装 PostgreSQL 15

```bash
# 安装
sudo apt install -y postgresql postgresql-contrib

# 启动并设开机自启
sudo systemctl enable postgresql
sudo systemctl start postgresql

# 创建数据库和用户
sudo -u postgres psql <<EOF
CREATE USER quiz_admin WITH PASSWORD '<生成强密码>';
CREATE DATABASE quiz_dev OWNER quiz_admin;
GRANT ALL PRIVILEGES ON DATABASE quiz_dev TO quiz_admin;
\c quiz_dev
GRANT ALL ON SCHEMA public TO quiz_admin;
EOF

# 确保仅监听本地
sudo sed -i "s/#listen_addresses = 'localhost'/listen_addresses = 'localhost'/" /etc/postgresql/15/main/postgresql.conf
sudo systemctl restart postgresql
```

### 3.5 安装 Redis 7

```bash
sudo apt install -y redis-server

# 仅监听本地 + 设置密码
sudo sed -i 's/^bind 127.0.0.1.*/bind 127.0.0.1/' /etc/redis/redis.conf
echo "requirepass <生成强密码>" | sudo tee -a /etc/redis/redis.conf

sudo systemctl enable redis-server
sudo systemctl restart redis-server
```

### 3.6 安装 Python 3（PDF 解析服务）

```bash
sudo apt install -y python3 python3-pip python3-venv tesseract-ocr tesseract-ocr-chi-sim

# 创建虚拟环境
python3 -m venv /opt/quiz-parser/venv
source /opt/quiz-parser/venv/bin/activate
pip install fastapi uvicorn pymupdf pillow pytesseract numpy python-multipart
```

---

## 四、应用部署

### 4.1 目录规划

```
/opt/quiz/
├── api/                 # 后端 Node.js 代码
│   ├── dist/            # tsc 编译产物
│   ├── uploads/         # 上传文件存储
│   ├── .env             # 生产环境变量（不要提交到 git）
│   └── node_modules/
├── web/                 # 前端构建产物（dist 目录）
│   └── dist/
├── parser/              # PDF 解析服务
└── scripts/
    └── deploy.sh        # 部署脚本
```

### 4.2 首次部署步骤

```bash
# 1. 切换到 deploy 用户
su - deploy

# 2. 克隆代码
cd /opt
sudo mkdir quiz && sudo chown deploy:deploy quiz
git clone <你的Git仓库地址> /opt/quiz/repo

# 3. 后端：安装依赖 + 构建 + 迁移数据库
cd /opt/quiz/repo/apps/api
npm install
cp .env.example .env   # 然后编辑 .env，填入生产配置
npx prisma migrate deploy
npx tsc

# 4. 前端：构建
cd /opt/quiz/repo/quiz-web
npm install
npm run build-only
# 产物在 quiz-web/dist/

# 5. 复制构建产物到部署目录
cp -r /opt/quiz/repo/quiz-web/dist /opt/quiz/web/dist
cp -r /opt/quiz/repo/apps/api/dist /opt/quiz/api/dist
cp /opt/quiz/repo/apps/api/package.json /opt/quiz/api/
cp /opt/quiz/repo/apps/api/node_modules /opt/quiz/api/node_modules

# 6. PDF 解析服务
cp -r /opt/quiz/repo/apps/parser /opt/quiz/parser
```

### 4.3 生产环境变量 `.env`

```env
# ── 数据库 ──
DATABASE_URL="postgresql://quiz_admin:<密码>@localhost:5432/quiz_dev"

# ── Redis ──
REDIS_URL="redis://:<密码>@localhost:6379"

# ── AI 服务 ──
DASHSCOPE_API_KEY="sk-your-dashscope-api-key"
SILICONFLOW_API_KEY="sk-obnumfdkwmqlkavhzvfketfezupmsldhulvvdmfqleeuhkdg"

# ── JWT ──（必须替换为强随机字符串）
JWT_SECRET="$(node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")"

# ── 文件存储 ──
UPLOAD_DIR="/opt/quiz/uploads"

# ── 前端地址 ──
FRONTEND_URL="https://<你的域名>"

# ── API 端口 ──
API_PORT=3001

# ── 环境标识 ──
NODE_ENV=production
```

### 4.4 Nginx 配置

```nginx
# /etc/nginx/sites-available/quiz

# HTTP → HTTPS 重定向
server {
    listen 80;
    server_name <你的域名>;

    # Let's Encrypt 验证用
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name <你的域名>;

    # SSL 证书（先用阿里云免费证书或 Let's Encrypt）
    ssl_certificate     /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Strict-Transport-Security "max-age=63072000" always;

    # 上传文件大小限制（试卷 PDF 最大 50MB）
    client_max_body_size 50m;

    # ── 前端静态资源 ──
    root /opt/quiz/web/dist;
    index index.html;

    # 静态资源缓存（带 hash 的文件名，可以长缓存）
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # SPA 路由回退
    location / {
        try_files $uri $uri/ /index.html;
    }

    # ── API 反向代理 ──
    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 长连接超时（诊断测试页面可能停留较久）
        proxy_read_timeout 120s;
    }

    # ── 上传文件静态访问 ──
    location /uploads/ {
        alias /opt/quiz/uploads/;
        expires 7d;
    }
}
```

```bash
# 启用配置
sudo ln -s /etc/nginx/sites-available/quiz /etc/nginx/sites-enabled/
sudo rm /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl reload nginx
```

### 4.5 PM2 进程管理

```javascript
// /opt/quiz/ecosystem.config.cjs
module.exports = {
  apps: [
    {
      name: 'quiz-api',
      cwd: '/opt/quiz/api',
      script: 'dist/index.js',
      env: {
        NODE_ENV: 'production',
      },
      max_memory_restart: '800M',   // 内存超 800M 自动重启
      log_date_format: 'YYYY-MM-DD HH:mm:ss',
      error_file: '/var/log/quiz/api-error.log',
      out_file: '/var/log/quiz/api-out.log',
    },
    {
      name: 'quiz-parser',
      cwd: '/opt/quiz/parser',
      script: '/opt/quiz-parser/venv/bin/uvicorn',
      args: 'src.main:app --host 127.0.0.1 --port 8000',
      interpreter: 'none',          // Python 程序，不需要 Node 解释器
      max_memory_restart: '1G',     // PDF 解析耗内存
      log_date_format: 'YYYY-MM-DD HH:mm:ss',
      error_file: '/var/log/quiz/parser-error.log',
      out_file: '/var/log/quiz/parser-out.log',
    },
  ],
};
```

```bash
# 启动所有服务
pm2 start /opt/quiz/ecosystem.config.cjs

# 设置开机自启
pm2 startup systemd
pm2 save
```

---

## 五、SSL 证书配置（Let's Encrypt 免费方案）

```bash
# 1. 安装 certbot
sudo apt install -y certbot python3-certbot-nginx

# 2. 申请证书（自动验证 + 自动写入 Nginx 配置）
sudo certbot --nginx -d <你的域名>

# 3. 设置自动续期（certbot 默认已添加 systemd timer）
sudo systemctl status certbot.timer
# 证书有效期 90 天，certbot 会在到期前自动续期
```

> 如果使用阿里云免费 DV 证书：在阿里云控制台申请 → DNS 验证 → 下载 Nginx 格式证书 → 上传到 `/etc/nginx/ssl/`。

---

## 六、ICP 备案（中国大陆必做）

如果服务器买在**中国大陆 Region**，域名解析到该服务器**必须先完成 ICP 备案**，否则域名访问会被阻断。

### 备案流程

```
购买域名 → 实名认证（1-3天）
    ↓
提交备案申请（阿里云备案系统）
    ↓
阿里云初审（1-2天）→ 短信/人脸核验
    ↓
管局审核（10-15个工作日）
    ↓
备案通过 → 获取 ICP 备案号 → 域名可正常访问
```

### 备案期间怎么办？

在备案通过之前，团队成员可以使用**公网 IP** 直接访问：

```
http://<公网IP>    （开发测试用，备案后切换到域名）
```

> 备案期间网站需要关停，不能通过域名访问。建议备案通过后再配置正式域名。

---

## 七、部署脚本

```bash
#!/bin/bash
# /opt/quiz/scripts/deploy.sh
# 使用方式：bash deploy.sh

set -e

echo "=== 开始部署 ==="

cd /opt/quiz/repo

# 1. 拉取最新代码
echo ">>> 拉取代码..."
git pull origin main

# 2. 后端
echo ">>> 构建后端..."
cd apps/api
npm install --production
npx prisma migrate deploy
npx tsc
cp -r dist /opt/quiz/api/dist
cp -r node_modules /opt/quiz/api/node_modules
cp package.json /opt/quiz/api/

# 3. 前端
echo ">>> 构建前端..."
cd ../../quiz-web
npm install
npm run build-only
rm -rf /opt/quiz/web/dist
cp -r dist /opt/quiz/web/dist

# 4. PDF 解析服务（如依赖无变动可跳过）
echo ">>> 同步解析服务..."
cp -r ../apps/parser/src /opt/quiz/parser/src

# 5. 重启服务
echo ">>> 重启服务..."
pm2 reload all

echo "=== 部署完成 ==="
```

---

## 八、运维要点

### 8.1 日志查看

```bash
# PM2 实时日志
pm2 logs quiz-api
pm2 logs quiz-parser

# Nginx 日志
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

### 8.2 监控

```bash
# PM2 资源监控面板
pm2 monit

# 系统资源
htop   # sudo apt install htop
df -h  # 磁盘使用
free -h # 内存使用
```

### 8.3 备份策略

```bash
# 数据库每日备份（crontab）
# 0 3 * * * pg_dump -U quiz_admin quiz_dev | gzip > /opt/backup/db_$(date +\%Y\%m\%d).sql.gz

# 上传文件同步备份（每周）
# 0 4 * * 0 tar -czf /opt/backup/uploads_$(date +\%Y\%m\%d).tar.gz /opt/quiz/uploads/
```

### 8.4 安全加固清单

| 项目 | 操作 |
|------|------|
| SSH | 禁用密码登录，仅用密钥对；禁用 root 登录 |
| 数据库 | 生产环境生成强随机密码，不使用默认端口 |
| JWT Secret | 生产环境使用 `crypto.randomBytes(64)` 生成，不与开发环境共用 |
| 防火墙 | 仅开放 22/80/443 端口 |
| 依赖 | 定期 `npm audit` 检查漏洞 |
| 日志 | 定期清理或配置 logrotate |
| 系统更新 | `sudo apt update && sudo apt upgrade` 每月执行 |

---

## 九、费用总览

| 项目 | 费用（月） | 费用（年） |
|------|:------:|:------:|
| ECS 2C4G 5Mbps 80G | ~150-200元 | ~1800-2400元 |
| 域名 | — | ~60元 |
| SSL 证书（阿里云免费） | 0 | 0 |
| **合计** | **~150-200元** | **~1860-2460元** |

---

## 十、实施检查清单

- [ ] 购买 ECS（2核4G + 5Mbps + 80G，Ubuntu 22.04）
- [ ] 购买域名并完成实名认证
- [ ] 配置安全组规则（仅开放 22/80/443）
- [ ] 创建 deploy 用户、配置 SSH 密钥
- [ ] 安装 Node.js 20 + PM2
- [ ] 安装 PostgreSQL 15 + 创建数据库
- [ ] 安装 Redis 7 + 设置密码
- [ ] 安装 Python 3 + OCR 依赖
- [ ] 克隆代码 + 配置 .env 生产变量
- [ ] 构建前后端 + 执行数据库迁移
- [ ] 配置 Nginx + SSL 证书
- [ ] 启动 PM2 服务 + 设置开机自启
- [ ] 提交 ICP 备案申请
- [ ] 备案通过后切换域名访问
- [ ] 配置数据库自动备份

---

*文档版本 v1.0，2026-05-19*

---

## 十一、实际操作记录（2026-05-22）

### 11.1 ECS 选型决策

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 实例规格 | 经济型 e（ecs.e-c1m2.large）2核4G | PDF 解析间歇突发、日常低负载，t6 经济型最匹配 |
| 操作系统 | Ubuntu 22.04 LTS | Node.js/Python/Nginx 生态最成熟 |
| 系统盘 | ESSD Entry 40G | 代码 + SQLite + 临时文件约 20G，40G 足够；后续 PDF 上 OSS 后更省 |
| 带宽 | 100Mbps 按流量计费 | 开发测试阶段突发快、空闲不花钱，优于 5Mbps 固定带宽 |
| 预装应用 | 无（裸系统） | 避免宝塔/LNMP/Docker 覆盖配置，每一步自己可控 |

### 11.2 已购实例信息

| 项目 | 值 |
|------|-----|
| 公网 IP | `120.55.84.30` |
| 私网 IP | `172.26.189.52` |
| 操作系统 | Ubuntu 22.04 64位 |
| 实例规格 | ecs.e-c1m2.large（2核4G） |
| 系统盘 | ESSD Entry 40G |
| 带宽 | 100Mbps 按流量 |
| 付费方式 | 按量付费（建议后期转包月或绑弹性公网 IP 防 IP 变动） |

### 11.3 安全组配置

**最终入方向规则（仅两条）：**

| 方向 | 端口 | 来源 | 说明 |
|------|:---:|------|------|
| 入方向 | `22` | 0.0.0.0/0 | SSH 远程管理 |
| 入方向 | `80` | 0.0.0.0/0 | HTTP 对外访问 |

**已删除的规则：** 3001（API 直连）、5432（PostgreSQL）、6379（Redis）、3389（Windows RDP）——一律不对外暴露。

**配置路径：** 阿里云控制台 → 安全组 → 点击实例关联的安全组 → 安全组规则 → 入方向。

### 11.4 SSH 登录操作记录

```bash
# 首次登录（root）
ssh root@120.55.84.30

# 修改 root 密码
passwd

# 创建日常操作用户
adduser deploy
usermod -aG sudo deploy

# 验证 sudo 权限已生效
id deploy
# 输出应包含: ...(sudo)
```

### 11.5 AI 辅助免密登录（临时）

配置流程：本地生成临时 SSH key → 公钥写入服务器 → 后续命令由 AI 直接执行。

```bash
# 在服务器上（depoloy 用户）执行：
mkdir -p ~/.ssh && echo 'ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFY2Fgt5G6MsNWXxyZtXF+54PcDrDtiz0suRsXwh+neA deploy_temp' >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys
```

> 此 key 为本次会话临时使用，后续可删除 `~/.ssh/authorized_keys` 中对应行。

### 11.6 下一步待执行

- [ ] `sudo apt update && sudo apt upgrade -y` — 系统更新
- [ ] 安装 Node.js 20 + PM2
- [ ] 安装 Nginx
- [ ] 克隆代码 + 构建 + 部署
- [ ] 配置 Nginx → 启动服务
