# QuizTestDemo 阿里云部署方案

> 适用环境：开发测试环境 | 团队：4人 | 目标：长期开发迭代

---

## 一、总体架构

```
公网用户
  │
  │ HTTP 80（当前公网 IP 访问）
  ▼
Nginx
  ├── /                 → /opt/quiz/web/dist        Vue SPA 静态资源
  ├── /api/             → 127.0.0.1:3001            Express API
  └── /uploads/         → /opt/quiz/uploads         上传文件目录

PM2
  └── quiz-api          → /opt/quiz/api/dist/index.js

SQLite
  └── /opt/quiz/data/prod.db

Git 仓库
  └── /opt/quiz/repo    → api/ + quiz-web/
```

**核心原则**：公网只开放 `22`、`80`、`443`；业务访问统一经 Nginx 进入，API 不通过安全组直接对外暴露。

---

## 二、需要购买的产品清单

### 2.1 当前上线所需产品

| 状态 | 产品 | 规格/说明 | 参考费用 | 用途 |
|------|------|----------|:------:|------|
| [x] | **ECS 云服务器** | 经济型 e 2核4G + ESSD Entry 40G + 100Mbps 按流量 | 按量计费 | 核心计算资源，当前已部署 |
| [ ] | **域名** | `.com` / `.cn`，在阿里云万网购买 | ~60元/年 | 正式对外访问地址，ICP备案和 HTTPS 前置条件 |
| [ ] | **SSL 证书** | 阿里云免费 DV 证书或 Let's Encrypt | 0元 | HTTPS 加密，需先有域名 |

> **ECS 选购细节**：
> - 系统镜像选 **Ubuntu 22.04 LTS**（Node.js 生态兼容性最好）
> - 地区选离团队最近的 Region（如华东2-上海）
> - 当前已购实例为 2核4G、ESSD Entry 40G、100Mbps 按流量

如果不买域名，只能用 `http://公网IP` 访问，无法申请 SSL 证书（Let's Encrypt 和阿里云免费证书都需要域名），密码等敏感数据在 HTTP 明文传输存在严重安全隐患。

### 2.2 本期不买（后续迭代按需追加）

| 状态 | 产品 | 当前替代方案 | 何时加 |
|------|------|------------|--------|
| [ ] | RDS PostgreSQL | SQLite 单机文件库 | 用户量增大、需要更强并发/备份/迁移能力时 |
| [ ] | OSS 对象存储 | ECS 本地磁盘 `/uploads` | 文件量超过 20G 或需要 CDN 时 |
| [ ] | Redis 云服务 | 暂不使用 | 需要队列、缓存、限流或多实例共享状态时 |
| [ ] | SLB 负载均衡 | Nginx 单节点 | 需要高可用/多节点时 |
| [ ] | NAT 网关 | ECS 自带公网 IP | 需要统一出口 IP 时 |

---

## 三、服务器环境搭建

本节保留部署动作清单，具体命令在实际操作时逐步执行，避免方案文档堆叠过多脚本。

### 3.1 安全组配置（阿里云控制台 → ECS → 安全组）

| 方向 | 端口 | 来源 | 说明 |
|------|:---:|------|------|
| 入方向 | `22` | 公司出口 IP（或临时 0.0.0.0/0） | SSH 远程管理 |
| 入方向 | `80` | 0.0.0.0/0 | HTTP |
| 入方向 | `443` | 0.0.0.0/0 | HTTPS |
| 入方向 | `3001` | **删除/不开放** | API 直连端口，仅通过 Nginx 反向代理访问 |
| 入方向 | `5432` | **删除/不开放** | 当前不使用 PostgreSQL |
| 入方向 | `6379` | **删除/不开放** | 当前不使用 Redis |

> **原则：最小暴露面。** 除了 22/80/443 之外一律不对公网开放。

### 3.2 初始化动作

- SSH 登录 ECS，创建 `deploy` 用户，并配置 SSH 密钥。
- 执行系统更新，安装 `curl`、`wget`、`git`、`build-essential`、`ufw`、`nginx`。
- 安装 Node.js 20。
- 安装 PM2，用于管理后端 Node.js 进程。
- UFW 只开放 `22`、`80`、`443`。

### 3.3 当前不安装的服务

- **Redis**：当前项目没有 Redis 调用，上线初期不是必需项。
- **PostgreSQL**：当前 Prisma datasource 仍是 SQLite，不能只改 `.env` 直接切库。

---

## 四、应用部署

### 4.1 目录规划

```
/opt/quiz/
├── repo/                # Git 仓库
├── api/                 # 后端运行目录
│   ├── dist/
│   ├── node_modules/
│   ├── package.json
│   └── .env             # 生产环境变量（不要提交到 Git）
├── web/
│   └── dist/            # 前端构建产物
├── data/
│   └── prod.db          # SQLite 生产库
├── uploads/             # 上传文件存储
├── scripts/
└── backups/
```

### 4.2 首次部署步骤

- 使用 `deploy` 用户克隆代码到 `/opt/quiz/repo`。
- 后端进入 `/opt/quiz/repo/api`，安装依赖、执行 Prisma migration、生成 Prisma Client、构建 TypeScript。
- 将后端 `dist/`、`node_modules/`、`package.json` 同步到 `/opt/quiz/api/`。
- 前端进入 `/opt/quiz/repo/quiz-web`，设置 `VITE_API_BASE` 后构建，并同步 `dist/` 到 `/opt/quiz/web/dist/`。
- 首次部署后用 PM2 启动后端，用 Nginx 对外提供静态文件和 API 反向代理。

### 4.3 生产环境变量 `.env`

后端环境变量放在 `/opt/quiz/api/.env`，不要提交到 Git。

```env
DATABASE_URL="file:/opt/quiz/data/prod.db"
API_PORT=3001
NODE_ENV=production

JWT_SECRET="<强随机字符串>"

FRONTEND_URL="https://<你的域名>"
CORS_ORIGINS="https://<你的域名>"

DASHSCOPE_API_KEY="<你的 DashScope Key>"
```

如果暂时没有域名，只用公网 IP 测试，则 `FRONTEND_URL` 和 `CORS_ORIGINS` 临时写成：

```env
FRONTEND_URL="http://<公网IP>"
CORS_ORIGINS="http://<公网IP>"
```

前端构建时必须设置：

```env
VITE_API_BASE="https://<你的域名>"
```

公网 IP 测试阶段则临时使用：

```env
VITE_API_BASE="http://<公网IP>"
```

---

## 五、SSL 证书配置

### 5.1 有域名后配置 HTTPS

- 域名完成实名认证和 ICP 备案后，将域名解析到 ECS 公网 IP。
- 可以使用阿里云免费 DV 证书，也可以使用 Let's Encrypt。
- Nginx 开启 `443`，并将 `80` 重定向到 `443`。
- 证书和私钥只放在服务器，不提交到 Git。

### 5.2 没有域名时的临时方案

- 备案通过前，可以先用 `http://<公网IP>` 做内部测试。
- 公网 IP 阶段不建议录入真实用户密码或敏感数据。
- 等域名、备案、证书完成后，再切换到正式 HTTPS 地址。

---

## 六、ICP 备案（中国大陆必做）

如果服务器买在**中国大陆 Region**，域名解析到该服务器**必须先完成 ICP 备案**，否则域名访问会被阻断。

### 备案流程

```
购买域名 → 实名认证（1-3天）
    ↓
提交备案申请（阿里云备案系统）
    ↓
阿里云初审（1-2天）→ 短信/人脸核验
    ↓
管局审核（10-15个工作日）
    ↓
备案通过 → 获取 ICP 备案号 → 域名可正常访问
```

### 备案期间怎么办？

在备案通过之前，团队成员可以使用**公网 IP** 直接访问：

```
http://<公网IP>    （开发测试用，备案后切换到域名）
```

> 备案期间网站需要关停，不能通过域名访问。建议备案通过后再配置正式域名。

---

## 七、部署脚本

部署脚本只保留为自动化方向，不在方案文档中维护完整脚本。实际脚本建议放在 `/opt/quiz/scripts/deploy.sh`，并按当前真实目录维护。

脚本需要完成的步骤：

1. 拉取 `/opt/quiz/repo` 最新代码。
2. 后端在 `api/` 下安装依赖、执行 `prisma migrate deploy`、生成 Prisma Client、执行构建。
3. 前端在 `quiz-web/` 下设置 `VITE_API_BASE` 后执行构建。
4. 同步后端构建产物到 `/opt/quiz/api/`，同步前端产物到 `/opt/quiz/web/dist/`。
5. 使用 PM2 reload 后端进程。
6. 必要时执行 Nginx 配置测试和 reload。

注意事项：

- 当前项目真实后端目录是 `api/`，不是 `apps/api`。
- 当前没有独立 `parser/` 服务，不需要部署 `/opt/quiz/parser`。
- 生产 `.env`、证书、数据库文件不得被脚本覆盖。

---

## 八、运维要点

### 8.1 日志查看

- 后端日志：使用 `pm2 logs quiz-api` 查看。
- Nginx 日志：查看 `/var/log/nginx/access.log` 和 `/var/log/nginx/error.log`。
- 应用错误优先看 PM2 日志，再看 Nginx 反代日志。

### 8.2 监控

- 用 PM2 观察 Node.js 进程状态、重启次数和内存占用。
- 用系统工具定期检查 CPU、内存、磁盘空间。
- SQLite 单机部署要特别关注磁盘空间和数据库备份是否正常。

### 8.3 备份策略

- 每日备份 `/opt/quiz/data/prod.db`。
- 每周备份 `/opt/quiz/uploads/`。
- 备份文件保存在 `/opt/quiz/backups/`，后续可再同步到 OSS。
- 部署前建议手动备份一次生产库。

### 8.4 安全加固清单

| 项目 | 操作 |
|------|------|
| SSH | 禁用密码登录，仅用密钥对；后续禁用 root 登录 |
| API | 只监听 `127.0.0.1:3001` 或仅允许本机访问 |
| JWT Secret | 生产环境使用强随机字符串，不与开发环境共用 |
| 防火墙 | 仅开放 22/80/443 端口 |
| 依赖 | 定期检查 npm 依赖漏洞 |
| 日志 | 定期清理或配置 logrotate |
| 系统更新 | 每月执行一次安全更新 |

---

*文档版本 v1.0，2026-05-19*

---

## 九、实际操作记录（2026-05-22）

### 9.1 ECS 选型决策

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 实例规格 | 经济型 e（ecs.e-c1m2.large）2核4G | PDF 解析间歇突发、日常低负载，t6 经济型最匹配 |
| 操作系统 | Ubuntu 22.04 LTS | Node.js/Nginx 生态最成熟 |
| 系统盘 | ESSD Entry 40G | 代码 + SQLite + 临时文件约 20G，40G 足够；后续 PDF 上 OSS 后更省 |
| 带宽 | 100Mbps 按流量计费 | 开发测试阶段突发快、空闲不花钱，优于 5Mbps 固定带宽 |
| 预装应用 | 无（裸系统） | 避免宝塔/LNMP/Docker 覆盖配置，每一步自己可控 |

### 9.2 已购实例信息

| 项目 | 值 |
|------|-----|
| 公网 IP | `120.55.84.30` |
| 私网 IP | `172.26.189.52` |
| 操作系统 | Ubuntu 22.04 64位 |
| 实例规格 | ecs.e-c1m2.large（2核4G） |
| 系统盘 | ESSD Entry 40G |
| 带宽 | 100Mbps 按流量 |
| 付费方式 | 按量付费（建议后期转包月或绑弹性公网 IP 防 IP 变动） |

### 9.3 安全组配置

**最终入方向规则（仅两条）：**

| 方向 | 端口 | 来源 | 说明 |
|------|:---:|------|------|
| 入方向 | `22` | 0.0.0.0/0 | SSH 远程管理 |
| 入方向 | `80` | 0.0.0.0/0 | HTTP 对外访问 |

**已删除的规则：** 3001（API 直连）、5432（PostgreSQL）、6379（Redis）、3389（Windows RDP）——一律不对外暴露。

**配置路径：** 阿里云控制台 → 安全组 → 点击实例关联的安全组 → 安全组规则 → 入方向。

### 9.4 SSH 登录操作记录

```bash
# 首次登录（root）
ssh root@120.55.84.30

# 修改 root 密码
passwd

# 创建日常操作用户
adduser deploy
usermod -aG sudo deploy

# 验证 sudo 权限已生效
id deploy
# 输出应包含: ...(sudo)
```

### 9.5 AI 辅助免密登录（临时）

配置流程：本地生成临时 SSH key → 公钥写入服务器 → 后续命令由 AI 直接执行。

```bash
# 在服务器上（depoloy 用户）执行：
mkdir -p ~/.ssh && echo 'ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFY2Fgt5G6MsNWXxyZtXF+54PcDrDtiz0suRsXwh+neA deploy_temp' >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys
```

> 此 key 为本次会话临时使用，后续可删除 `~/.ssh/authorized_keys` 中对应行。

### 9.6 下一步待执行

- [ ] `sudo apt update && sudo apt upgrade -y` — 系统更新
- [ ] 安装 Node.js 20 + PM2
- [ ] 安装 Nginx
- [ ] 克隆代码 + 构建 + 部署
- [ ] 配置 Nginx → 启动服务

### 9.7 更新记录（2026-06-15）

本次按当前代码仓库重新核对部署方案，结论如下：

- 当前后端真实目录是 `api/`，前端真实目录是 `quiz-web/`；旧文档中 `apps/api`、`apps/parser` 不适用于当前仓库。
- 当前 Prisma 使用 SQLite，生产环境建议先用 `/opt/quiz/data/prod.db` 跑通最小可行上线。
- 当前项目没有 Redis 依赖和调用，上线初期不需要安装 Redis。
- 当前不建议立刻切 PostgreSQL；如果要切，需要单独做 Prisma datasource、migration、数据迁移和备份策略调整。
- 前端生产构建必须设置 `VITE_API_BASE`，否则线上浏览器可能请求 `localhost:3001`。
- 安全组不应开放 `3001`、`5432`、`6379`，API 统一通过 Nginx 反向代理访问。
- 备案和 HTTPS 完成前，可以先用公网 IP 做内部测试；正式访问建议切换到域名 + HTTPS。

准备正式部署前还需要确认：

- Git 仓库地址和部署分支。
- 是否已有域名、备案状态、证书方案。
- DashScope API Key。
- 生产环境 JWT_SECRET。
- 是否允许我通过 SSH 连接目标服务器继续执行部署。

### 9.8 实际部署记录（2026-06-15）

本次已按最小可行上线方案完成公网 IP 部署。

| 项目 | 值 |
|------|-----|
| 公网 IP | `8.149.140.115` |
| 登录用户 | `deploy` |
| 部署分支 | `20260519-feature-projectDeployment` |
| Git 仓库 | `https://github.com/Shiyidan/OxBridgeQuiz.git` |
| 后端运行目录 | `/opt/quiz/api` |
| 前端静态目录 | `/opt/quiz/web/dist` |
| SQLite 生产库 | `/opt/quiz/data/prod.db` |
| PM2 进程 | `quiz-api` |
| Nginx 配置 | `/etc/nginx/sites-available/quiz` |

已完成：

- 安装 Node.js `v20.20.2`、npm `10.8.2`、Nginx `1.18.0`、PM2 `7.0.1`。
- 克隆 GitHub 仓库并切换到部署分支。
- 写入 `/opt/quiz/api/.env`，生成生产 `JWT_SECRET`。
- 执行 Prisma migration，创建 SQLite 生产库。
- 完成后端 TypeScript 构建和前端 Vite 构建。
- 使用 PM2 启动 API，并配置 `pm2-deploy` systemd 开机自启。
- 配置 Nginx：`/` 指向前端 SPA，`/api/` 反向代理到 `127.0.0.1:3001`。
- 启用 UFW，仅放行 `22`、`80`、`443`。

验证结果：

- `http://8.149.140.115/` 返回 `200 OK`。
- `http://8.149.140.115/api/health` 返回 `{ "status": "ok" }`。
- PM2 中 `quiz-api` 状态为 `online`。

待补充：

- 当前 `DASHSCOPE_API_KEY` 为空，AI 试卷解析能力暂不可用；拿到 Key 后补入 `/opt/quiz/api/.env` 并执行 `pm2 reload quiz-api`。
- 当前仍使用公网 IP + HTTP，正式对外使用前建议完成域名、备案和 HTTPS。
- 部署用临时 SSH 公钥后续可从服务器 `~/.ssh/authorized_keys` 中删除。
- npm 安装阶段提示存在依赖漏洞，后续应单独安排依赖升级和回归测试。

### 9.9 用户数据迁移记录（2026-06-15）

本次没有做全量数据库迁移，只迁移本地开发库中的 `User` 表数据，目的是让线上具备可登录的管理员账号。

迁移方式：

- 从本地 `api/prisma/dev.db` 导出 `User` 表 4 条记录。
- 上传到服务器 `/opt/quiz/api/users-import.json`。
- 在服务器通过 Prisma 按 `email` 执行 upsert。
- 导入完成后删除临时 JSON 和临时导入脚本。

迁移结果：

| 角色 | 数量 |
|------|-----:|
| admin | 2 |
| student | 3 |

线上当前用户：

- `ss@main.com`：admin
- `22@mail.com`：admin
- `123@mail.com`：student
- `stu2@mail.com`：student
- `test@mail.com`：student

备份文件：

- 导入前备份：`/opt/quiz/backups/prod_before_user_import_.db`
- 导入后备份：`/opt/quiz/backups/prod_after_user_import_20260615.db`

### 9.10 大纲数据迁移记录（2026-06-15）

试题库页面依赖 `SyllabusNode` 大纲树数据。本次没有做全量数据库迁移，只迁移本地开发库中的大纲表。

处理过程：

- 线上生产库缺少 `SyllabusNode` 表，先执行 `npx prisma db push` 将 SQLite 结构同步到当前 Prisma schema。
- 从本地 `api/prisma/dev.db` 导出 `SyllabusNode` 表 98 条记录。
- 上传到服务器 `/opt/quiz/api/syllabus-import.json`。
- 在服务器通过 Prisma 按 `examType + code` 执行 upsert。
- 导入完成后删除临时 JSON 和临时导入脚本。

迁移结果：

| examType | 节点数 |
|----------|------:|
| ESAT | 98 |

根节点：

- `100000`：ESAT (工程与科学入学测试)

备份文件：

- 导入前备份：`/opt/quiz/backups/prod_before_syllabus_import_20260615.db`
- 导入后备份：`/opt/quiz/backups/prod_after_syllabus_import_20260615.db`

验证接口：

- `http://8.149.140.115/api/papers/syllabus`
- `http://8.149.140.115/api/papers/question-bank/summary?code=100000`
- `http://8.149.140.115/api/papers/question-bank/summary?code=110000`

### 9.11 增量上线记录（2026-06-16）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260519-feature-projectDeployment` |
| 上线 Commit | `bb7b269` |
| Commit 说明 | 修改试题syllabus字段和考纲对应关系 |
| 上线范围 | 后端 + 前端 |
| 数据库迁移 | `npx prisma migrate deploy`，无待执行 migration |
| 生产库备份 | `/opt/quiz/backups/prod_before_deploy_20260616_120504.db` |

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `http://8.149.140.115/` 返回 `200 OK`。
- `http://8.149.140.115/api/health` 正常。
- PM2 中 `quiz-api` 状态为 `online`。

### 9.12 增量上线记录（2026-06-22）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260519-feature-projectDeployment` |
| 上线 Commit | `a543ea2` |
| Commit 说明 | 错题本增加分页功能 |
| 上线范围 | 后端 + 前端 |
| 数据库备份 | `/opt/quiz/backups/prod_before_deploy_20260622_105550.db`、`/opt/quiz/backups/prod_before_deploy_20260622_105949.db` |

处理记录：

- 首次执行 `npx prisma migrate deploy` 时，发现线上库中已存在 `SyllabusNode` 表，但 migration `20260620000000_add_syllabus_node` 未被 Prisma 标记为完成。
- 经检查，线上 `SyllabusNode` 表和索引与 migration 内容一致，因此执行 `npx prisma migrate resolve --applied 20260620000000_add_syllabus_node` 修复迁移状态。
- 继续执行 `npx prisma migrate deploy`，成功应用 `20260620000001_add_question_table`、`20260620000002_add_answer_fk`。
- 后端完成 TypeScript 构建并重载 PM2；前端完成 Vite 构建并同步到 `/opt/quiz/web/dist`。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `http://8.149.140.115/` 返回 `200 OK`。
- `http://8.149.140.115/api/health` 正常。
- PM2 中 `quiz-api` 状态为 `online`。
- Prisma 显示生产库结构已是最新。

### 9.13 后端修复上线记录（2026-06-22）

本次使用 `quiztestdemo-deploy` Skill 执行 `backend` 范围上线，用于修复线上 `Question.syllabusPoints` 字段缺失导致的接口 500 问题。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260519-feature-projectDeployment` |
| 上线 Commit | `5df4937` |
| Commit 说明 | 新增考纲树 migration |
| 上线范围 | 后端 |
| 数据库备份 | `/opt/quiz/backups/prod_before_deploy_20260622_134247.db` |
| 应用迁移 | `20260622000000_add_question_syllabus_points` |

处理记录：

- 部署前执行 Prisma migration guard，确认 `schema.prisma` 与 migrations 已对齐。
- 执行 `npx prisma migrate deploy`，成功为 `Question` 表增加 `syllabusPoints` 字段。
- 后端完成 TypeScript 构建、运行目录同步并重载 PM2。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `http://8.149.140.115/api/health` 正常。
- `http://8.149.140.115/api/papers/question-bank/summary?code=110000` 返回 `200 OK`，不再出现 `Question.syllabusPoints` 缺失错误。
- PM2 中 `quiz-api` 状态为 `online`。
- Prisma 显示生产库结构已是最新。

### 9.14 全量上线记录（2026-06-30）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布考纲管理相关功能。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `2e0c8c1` |
| Commit 说明 | Add user exam preferences migration |
| 上线范围 | 后端 + 前端 |
| 数据库备份 | `/opt/quiz/backups/prod_before_deploy_20260630_161331.db` |
| 应用迁移 | `20260622000001_add_membership_entitlements`、`20260628000000_add_syllabus_library`、`20260630000000_add_user_exam_preferences` |

处理记录：

- 首次部署被 Prisma migration guard 拦截，原因是 `User.examPreferences` 字段缺少已提交 migration。
- 补充 migration `20260630000000_add_user_exam_preferences` 后推送到部署分支，并重新执行全量上线。
- 部署前执行 Prisma migration guard，确认 `schema.prisma` 与 migrations 已对齐。
- 执行 `npx prisma migrate deploy`，成功应用 3 条待执行 migration。
- 后端完成 TypeScript 构建、运行目录同步并重载 PM2；前端完成 Vite 构建并同步到 `/opt/quiz/web/dist`。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `http://8.149.140.115/` 返回 `200 OK`。
- `http://8.149.140.115/api/health` 正常。
- PM2 中 `quiz-api` 状态为 `online`。
- Prisma 显示生产库结构已是最新。
