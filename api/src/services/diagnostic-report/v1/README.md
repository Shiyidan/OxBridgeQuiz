# V1 历史兼容边界

V1 仅负责历史报告快照兼容，不再为新的 ESAT/TMUA 答卷生成报告。

- 历史数据通过 `reportVersion` 识别，任何非 `diagnostic-report-v2-*` 版本都归入产品 V1。
- V1 页面必须继续读取旧快照中已有字段，缺失的新字段不得触发重新生成。
- V1 不自动升级；只有用户点击“更新报告”才用原答卷生成 V2 并替换该答卷的报告快照。
- 通用旧考试类型仍由公共门面中的兼容生成逻辑处理。
- 评分换算必须引用 `../shared/scoring.ts`，不得在 V1 目录保存独立评分表。
