# 实验与实现报告

**任务**：esat-module-positioning  
**完成日期**：2026-08-03  
**实验轮数**：1  
**提示词变体数**：3

## 实验总览

| 指标 | 值 |
|---|---:|
| 基线得分 | 7.5 |
| 最终提示词得分 | 9.4 |
| 提升幅度 | +1.9 |
| 真实 DeepSeek 验证 | 3/3 模块接受 |
| 报告版本 | diagnostic-report-v4 |

## 关键实现

1. 科目整体评价使用独立 DeepSeek 请求，只输出 `moduleId + positioningInsight`。
2. 批量结果缺失模块时，只对缺失模块执行一次补偿请求。
3. 模型输入增加预计算正确率、平台等级、参考百分位与难度样本提示。
4. 辅助诊断字段逐字段接受，部分有效时使用 `mixed` 来源，不再整模块丢弃。
5. 整体评价失败时动态引用分数、正确率、主要难度缺口和小样本边界。
6. 程序校验拒绝固定套话、绝对化措辞，以及“核心瓶颈”和“优先方向”层级矛盾。
7. 提示词参与缓存键计算，提示词升级后不会复用旧评价。
8. 历史 v3 报告可通过页面升级入口使用原答卷生成 v4，无需重新考试。

## 真实验证结果

- 数学1、化学、生物三条 `positioning.analysisSource` 均为 `deepseek`。
- 三条评价均包含预估分、总正确率、难度证据、核心瓶颈和唯一优先方向。
- 最新本地报告已成功保存为 `diagnostic-report-v4`。
- 七日计划独立请求曾超时并使用规则兜底，因此整份报告模式为 `mixed_fallback`；不影响本任务的三条科目整体评价。

## 自动化验证

- 拒绝“该模块仍有提升空间”等固定空话。
- 拒绝瓶颈与优先方向不一致的评价。
- 拒绝“极弱”等绝对化措辞。
- 验证动态兜底包含分数、正确率、主要难度层和小样本提示。
- 验证部分 AI 字段与规则字段合并为 `mixed`。
- 后端 TypeScript 构建、专项回归测试、前端类型检查和 ESLint 均通过。
