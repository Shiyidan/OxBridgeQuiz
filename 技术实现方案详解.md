# 在线试卷解析与答题系统 - 技术实现方案详解

## 一、系统架构概览

```
PDF试卷 → 解析引擎 → 结构化JSON → Vue前端 → 用户界面
```

整个系统分为两大核心模块：
1. **PDF解析引擎**（Python）：负责从PDF提取内容并结构化
2. **前端渲染引擎**（Vue 3）：负责将结构化数据渲染为可交互界面

---

## 二、PDF解析模块详解

### 2.1 核心技术栈

| 技术/包 | 用途 | 选型理由 |
|---------|------|----------|
| **PyMuPDF (fitz)** | PDF文本和图片提取 | 速度快、内存占用低、API简洁、支持多种PDF格式 |
| **Pillow (PIL)** | 图像处理 | Python标准图像库，用于保存提取的图片 |
| **pix2tex** | 公式OCR识别（可选） | 基于深度学习的公式识别，可将公式图片转为LaTeX |
| **正则表达式 (re)** | 文本模式匹配 | 用于题号识别、选项解析等 |
| **JSON** | 数据序列化 | 标准数据交换格式，前后端通用 |

### 2.2 PDF解析流程

#### 阶段1：文档加载与预处理
- 使用PyMuPDF打开PDF文件，获取总页数和文档元数据
- 遍历每一页，提取原始文本和图片资源

#### 阶段2：文本提取与特征分析
PyMuPDF提供多种文本提取模式：
- **普通文本模式**：获取纯文本内容
- **字典模式**：获取带格式信息的文本块，包括：
  - 字体名称（如 CMMI、Times-Bold、Symbol）
  - 字体大小
  - 文本位置坐标（bbox）
  - 字体样式标志（粗体、斜体等）
  - 颜色信息

**公式检测逻辑**：
通过字体特征识别公式区域：
- 数学字体标识：CMMI、CMEX、CMSY、CMR（Computer Modern数学字体系列）
- 字体名称包含 "Math"、"Symbol"、"Times-Italic"
- 特殊数学符号：∫∑∏√∞∂∆παβγ等希腊字母和运算符
- 上下标特征：如 `x^2`、`y_i` 等模式

#### 阶段3：图片提取与分类
- 遍历PDF中的图像对象（XObject）
- 提取图片字节流，保存为PNG/JPG格式
- 记录图片位置信息（bbox坐标）
- 图片分类：
  - 公式图片：通过位置与文本公式区域关联
  - 图表图片：几何图、电路图等
  - 装饰图片：页眉页脚logo等

#### 阶段4：题目分割算法
基于题号模式识别题目边界：

**分割策略**：
1. **正则匹配**：识别题号模式（如 "1.", "1 ", "1)" 等）
2. **位置定位**：记录每个题号的起始位置
3. **内容提取**：提取两个题号之间的内容作为一个题目区块

**边界处理**：
- 处理跨页题目（通过页眉页脚识别）
- 过滤非题目内容（如说明文字、页码）
- 识别题目与选项的分界点

#### 阶段5：选项解析
识别选择题的选项部分：

**选项模式识别**：
- 标准选项：A. B. C. D. E. F.
- 选项格式：字母 + 分隔符（点、括号、空格）+ 内容
- 选项内容类型：
  - 纯文本选项
  - 包含公式的选项
  - 图片选项

**选项分离**：
通过正则表达式将题目正文与选项分离，分别存储。

#### 阶段6：公式处理与转换

**文本公式转换**：
对于检测到的公式文本，进行LaTeX转换：
- 希腊字母映射：π → \\pi, α → \\alpha 等
- 根号转换：√ → \\sqrt{}
- 分数转换：a/b → \\frac{a}{b}
- 上下标转换：x^2 → x^{2}, y_i → y_{i}
- 特殊符号：≤≥≠≈±×÷等映射为LaTeX命令

**图片公式OCR**（可选）：
使用pix2tex模型进行端到端识别：
- 输入：公式区域的图片
- 输出：LaTeX代码
- 支持：行内公式、独立公式、复杂数学表达式

#### 阶段7：结构化输出
生成标准JSON格式，包含：
- 试卷元信息（标题、年份、时长）
- 题目数组（每道题包含内容块、选项、标签等）
- 内容块支持多种类型：文本、公式、图片、换行

---

## 三、前端渲染模块详解

### 3.1 核心技术栈

| 技术/包 | 用途 | 选型理由 |
|---------|------|----------|
| **Vue 3** | 前端框架 | 组合式API、更好的TypeScript支持、性能优化 |
| **TypeScript** | 类型系统 | 编译时类型检查、更好的IDE支持、代码可维护性 |
| **Pinia** | 状态管理 | Vue官方推荐、TypeScript友好、API简洁 |
| **KaTeX** | 公式渲染 | 渲染速度快、支持服务器端渲染、体积小 |
| **Vite** | 构建工具 | 快速冷启动、热更新、现代化构建 |

### 3.2 组件架构设计

采用**分层组件架构**：

```
ExamView (页面层)
  └── QuestionRenderer (题目层)
        ├── ContentBlock (内容块层)
        │     ├── TextBlock (文本)
        │     ├── FormulaBlock (公式 - KaTeX渲染)
        │     ├── ImageBlock (图片)
        │     └── BreakBlock (换行)
        └── OptionList (选项层)
              └── ContentBlock (选项内容)
```

#### 组件职责说明：

**1. ExamView（页面容器）**
- 负责试卷数据的加载（fetch JSON）
- 管理考试状态（答题中/已提交）
- 显示考试头部信息（标题、进度、计时）
- 处理交卷逻辑和成绩计算

**2. QuestionRenderer（题目渲染器）**
- 接收单个题目数据
- 渲染题号和题目内容
- 管理题目答案的选择状态
- 显示答题反馈（正确/错误提示）

**3. ContentBlock（内容块渲染器）**
- 根据内容块类型分发到不同渲染器
- 支持动态组件切换
- 处理混合内容（文本+公式+图片）

**4. FormulaBlock（公式渲染器）**
- 使用KaTeX将LaTeX代码渲染为HTML
- 处理渲染错误（降级显示原始文本）
- 支持行内公式和块级公式

**5. OptionList（选项列表）**
- 渲染所有选项
- 管理选项选择状态
- 显示选项反馈样式（选中/正确/错误）

### 3.3 数据流设计

```
加载阶段：
  PDF解析器 → paper.json → 前端fetch → Pinia Store

渲染阶段：
  Pinia Store → 组件props → 子组件props → DOM渲染

交互阶段：
  用户点击 → 组件emit事件 → Store更新状态 → 响应式更新UI
```

### 3.4 响应式状态管理

使用Pinia管理以下状态：

**State（状态）**：
- `paper`: 当前试卷数据
- `answers`: 答题记录（Map: questionId → selectedOption）
- `submitted`: 是否已交卷
- `startedAt` / `submittedAt`: 时间记录

**Getters（计算属性）**：
- `totalQuestions`: 总题数
- `answeredCount`: 已答题数
- `progress`: 答题进度百分比
- `score`: 得分计算（正确数/总数/百分比）
- `isComplete`: 是否全部答完

**Actions（操作）**：
- `loadPaper`: 加载试卷数据
- `setAnswer`: 记录答案
- `submit`: 交卷
- `reset`: 重置考试

### 3.5 公式渲染机制

**KaTeX渲染流程**：

1. **输入**：LaTeX字符串（如 `R = \sqrt{\frac{25r}{2}}`）

2. **编译**：KaTeX解析LaTeX语法，生成HTML DOM结构
   - 识别数学命令（\sqrt、\frac等）
   - 应用数学字体和样式
   - 生成嵌套的HTML元素

3. **输出**：HTML字符串，包含：
   - 数学符号（使用特殊字体）
   - 上标/下标（使用定位样式）
   - 分数线/根号（使用CSS边框和SVG）
   - 间距调整（margin/padding）

4. **渲染**：Vue的v-html指令将HTML插入DOM

**样式处理**：
- 引入KaTeX CSS文件（提供数学字体和基础样式）
- 自定义样式调整（字号、颜色等）
- 错误处理（渲染失败时显示原始文本）

### 3.6 图片渲染策略

**图片类型处理**：

1. **公式图片**：
   - 优先使用LaTeX渲染（如果有转换后的代码）
   - 降级显示原始图片（如果OCR失败）

2. **图表图片**：
   - 几何图、电路图等直接显示
   - 支持点击放大查看
   - 保持原始比例缩放

3. **图片加载**：
   - 使用相对路径引用（基于public目录）
   - 懒加载优化（长试卷）
   - 错误占位符（加载失败时）

### 3.7 交互设计

**答题流程**：

1. **选择答案**：
   - 点击选项按钮
   - 按钮高亮显示选中状态
   - 答案记录到Store

2. **进度显示**：
   - 顶部进度条实时更新
   - 显示已答/总题数

3. **交卷**：
   - 检查是否全部答完
   - 确认对话框防止误操作
   - 计算得分并显示结果

4. **答案反馈**：
   - 正确选项：绿色高亮
   - 错误选项：红色高亮
   - 显示正确答案提示

---

## 四、关键技术难点与解决方案

### 4.1 题目分割不准确

**问题**：PDF布局复杂，题号识别可能出错

**解决方案**：
- 多模式正则匹配（支持多种题号格式）
- 基于坐标位置的辅助判断
- 保留原始文本便于人工校对

### 4.2 公式识别准确率

**问题**：复杂公式OCR可能出错

**解决方案**：
- 文本公式优先（基于字体特征）
- 图片公式OCR作为补充
- 前端支持LaTeX和原始图片双模式显示

### 4.3 跨页题目处理

**问题**：一道题可能跨越多页

**解决方案**：
- 页眉页脚识别（过滤重复内容）
- 题目内容连续性检测
- 图片与题目的关联匹配

### 4.4 大量题目渲染性能

**问题**：169道题同时渲染可能卡顿

**解决方案**：
- 虚拟滚动（只渲染可见区域）
- 图片懒加载
- 组件按需渲染

### 4.5 公式渲染错误处理

**问题**：错误的LaTeX代码导致渲染失败

**解决方案**：
- try-catch包裹渲染逻辑
- 渲染失败时显示原始文本
- 错误日志记录便于调试

---

## 五、数据格式设计

### 5.1 内容块（Content Block）

统一的内容表示方式，支持多种类型：

```typescript
interface ContentBlock {
  type: 'text' | 'formula' | 'image' | 'svg' | 'break';
  value?: string;        // 原始文本或图片路径
  latex?: string;        // 公式LaTeX代码
  src?: string;          // 图片URL
  metadata?: {
    width?: number;
    height?: number;
    alt?: string;
  };
}
```

### 5.2 题目结构

```typescript
interface Question {
  id: string;                    // 唯一标识
  number: number;                // 题号
  content: ContentBlock[];       // 题目内容（富文本数组）
  options: Option[];             // 选项数组
  correctAnswer?: string;        // 正确答案（A-F）
  explanation?: string;          // 解析
  tags?: string[];               // 标签（知识点分类）
}
```

### 5.3 试卷结构

```typescript
interface Paper {
  title: string;                 // 试卷标题
  year: number;                  // 年份
  duration: number;              // 考试时长（分钟）
  totalQuestions: number;        // 总题数
  questions: Question[];         // 题目数组
}
```

---

## 六、部署与运行

### 6.1 环境要求

**后端解析环境**：
- Python 3.8+
- PyMuPDF
- Pillow
- pix2tex（可选，需要PyTorch）

**前端运行环境**：
- Node.js 18+
- npm 或 yarn
- 现代浏览器（Chrome/Firefox/Edge）

### 6.2 构建流程

1. **PDF解析**：
   ```bash
   python pdf_to_json.py input.pdf -o output/
   ```

2. **前端开发**：
   ```bash
   npm install
   npm run dev
   ```

3. **前端生产构建**：
   ```bash
   npm run build
   ```

### 6.3 目录结构

```
project/
├── parser/                 # PDF解析器
│   ├── pdf_to_json.py     # 主解析脚本
│   └── requirements.txt   # Python依赖
│
├── quiz-web/               # 前端项目
│   ├── src/
│   │   ├── components/    # Vue组件
│   │   ├── views/         # 页面
│   │   ├── stores/        # Pinia状态管理
│   │   └── types/         # TypeScript类型定义
│   └── public/
│       └── data/          # 试卷JSON和图片
│
└── ENGAA_2023_S1_QuestionPaper.pdf  # 示例PDF
```

---

## 七、扩展性设计

### 7.1 支持更多题型

当前实现支持选择题，可扩展支持：
- 填空题：输入框组件
- 解答题：富文本编辑器
- 多选题：多选逻辑

### 7.2 公式渲染增强

- MathJax备用方案（复杂公式）
- 手写公式识别（Canvas + 机器学习）
- 公式编辑器（可视化编辑）

### 7.3 数据持久化

- 后端API + 数据库存储
- 用户答题历史记录
- 成绩统计分析

---

## 总结

本方案采用**解析-存储-渲染**的三层架构：

1. **解析层**：PyMuPDF提取内容，字体特征识别公式，正则分割题目
2. **存储层**：JSON结构化数据，前后端通用格式
3. **渲染层**：Vue 3组件化架构，KaTeX公式渲染，Pinia状态管理

核心技术特点：
- **自动化**：PDF到JSON全自动转换，无需人工干预
- **组件化**：可复用的渲染组件，易于维护和扩展
- **响应式**：Pinia状态驱动，实时更新UI
- **类型安全**：TypeScript全程类型检查
