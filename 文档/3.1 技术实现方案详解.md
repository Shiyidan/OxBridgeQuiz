# 在线试卷解析与答题系统 - 技术实现方案详解

## 一、系统架构概览

```
PDF/图片 → pdf.js 浏览器端渲染 → Qwen-VL-Max AI 识别 → 结构化 JSON → Vue 前端渲染
```

整个系统分为三大核心模块：
1. **PDF渲染引擎**（pdf.js 浏览器端）：负责将 PDF 逐页渲染为图片
2. **AI识别引擎**（Qwen-VL-Max）：负责从图片中识别题目/公式/图形并结构化
3. **前端渲染引擎**（Vue 3）：负责将结构化数据渲染为可交互界面

---

## 二、PDF解析模块（pdf.js + Qwen 方案）

> 已全面采用浏览器端 pdf.js 渲染 + Qwen-VL-Max AI 识别方案。详见 [3.4 试卷上传解析技术方案](3.4%20试卷上传解析技术方案.md)。

### 2.1 核心技术栈

| 技术/包 | 用途 | 选型理由 |
|---------|------|----------|
| **pdf.js (pdfjs-dist)** | PDF 浏览器端渲染 | Mozilla 官方，Web Worker 异步解析，Canvas 渲染为 JPEG |
| **Qwen-VL-Max** | AI 多模态识别 | 阿里云 DashScope，图片 → 结构化 JSON |
| **KaTeX** | 前端公式渲染 | 快速 LaTeX 渲染，模块级缓存 |
| **JSON** | 数据序列化 | 标准数据交换格式，前后端通用 |

### 2.2 整体流程

```
用户选择 PDF/图片
  → 浏览器 pdf.js 逐页渲染为 JPEG Base64（或图片直传原图）
  → 前端逐页 POST /api/parse-tasks/:id/pages
  → 后端收到每页立即调 Qwen-VL-Max（信号量限制 5 并发）
  → 协调器串行汇合多页结果
  → 全部完成后去重排序 → 写入 Paper.questions
```

### 2.3 关键设计点

- **PDF 渲染前端化**：pdf.js 在浏览器端完成 Canvas 渲染，服务器零 Python 依赖
- **流式逐页上传**：每页约 250KB，避免单次 10MB 请求超时
- **信号量并发控制**：限制 Qwen 最大 5 并发，避免 API 限流
- **协调器汇合**：多页 Qwen 结果串行汇入，避免并发写入冲突
- **LaTeX 占位符策略**：模型输出用 `[[BS]]` / `[[PARA]]` / `[[NL]]` 替代反斜杠和换行，消除 JSON 转义错误
- **复杂图跳过**：复杂电路图等用 `[[FIG]]` 占位，优先保证题目文字完整

---

## 三、前端渲染模块详解

### 3.1 核心技术栈

| 技术/包 | 用途 | 选型理由 |
|---------|------|----------|
| **Vue 3** | 前端框架 | 组合式API、更好的TypeScript支持、性能优化 |
| **TypeScript** | 类型系统 | 编译时类型检查、更好的IDE支持、代码可维护性 |
| **Pinia** | 状态管理 | Vue官方推荐、TypeScript友好、API简洁 |
| **KaTeX** | 公式渲染 | 渲染速度快、支持服务器端渲染、体积小 |
| **Vite** | 构建工具 | 快速冷启动、热更新、现代化构建 |

### 3.2 组件架构设计

采用**分层组件架构**：

```
ExamView (页面层)
  └── QuestionRenderer (题目层)
        ├── ContentBlock (内容块层)
        │     ├── TextBlock (文本)
        │     ├── FormulaBlock (公式 - KaTeX渲染)
        │     ├── ImageBlock (图片)
        │     └── BreakBlock (换行)
        └── OptionList (选项层)
              └── ContentBlock (选项内容)
```

#### 组件职责说明：

**1. ExamView（页面容器）**
- 负责试卷数据的加载（fetch JSON）
- 管理考试状态（答题中/已提交）
- 显示考试头部信息（标题、进度、计时）
- 处理交卷逻辑和成绩计算

**2. QuestionRenderer（题目渲染器）**
- 接收单个题目数据
- 渲染题号和题目内容
- 管理题目答案的选择状态
- 显示答题反馈（正确/错误提示）

**3. ContentBlock（内容块渲染器）**
- 根据内容块类型分发到不同渲染器
- 支持动态组件切换
- 处理混合内容（文本+公式+图片）

**4. FormulaBlock（公式渲染器）**
- 使用KaTeX将LaTeX代码渲染为HTML
- 处理渲染错误（降级显示原始文本）
- 支持行内公式和块级公式

**5. OptionList（选项列表）**
- 渲染所有选项
- 管理选项选择状态
- 显示选项反馈样式（选中/正确/错误）

### 3.3 数据流设计

```
加载阶段：
  浏览器 pdf.js + 后端 Qwen → paper.json → 前端fetch → Pinia Store

渲染阶段：
  Pinia Store → 组件props → 子组件props → DOM渲染

交互阶段：
  用户点击 → 组件emit事件 → Store更新状态 → 响应式更新UI
```

### 3.4 响应式状态管理

使用Pinia管理以下状态：

**State（状态）**：
- `paper`: 当前试卷数据
- `answers`: 答题记录（Map: questionId → selectedOption）
- `submitted`: 是否已交卷
- `startedAt` / `submittedAt`: 时间记录

**Getters（计算属性）**：
- `totalQuestions`: 总题数
- `answeredCount`: 已答题数
- `progress`: 答题进度百分比
- `score`: 得分计算（正确数/总数/百分比）
- `isComplete`: 是否全部答完

**Actions（操作）**：
- `loadPaper`: 加载试卷数据
- `setAnswer`: 记录答案
- `submit`: 交卷
- `reset`: 重置考试

### 3.5 公式渲染机制

**KaTeX渲染流程**：

1. **输入**：LaTeX字符串（如 `R = \sqrt{\frac{25r}{2}}`）

2. **编译**：KaTeX解析LaTeX语法，生成HTML DOM结构
   - 识别数学命令（\sqrt、\frac等）
   - 应用数学字体和样式
   - 生成嵌套的HTML元素

3. **输出**：HTML字符串，包含：
   - 数学符号（使用特殊字体）
   - 上标/下标（使用定位样式）
   - 分数线/根号（使用CSS边框和SVG）
   - 间距调整（margin/padding）

4. **渲染**：Vue的v-html指令将HTML插入DOM

**样式处理**：
- 引入KaTeX CSS文件（提供数学字体和基础样式）
- 自定义样式调整（字号、颜色等）
- 错误处理（渲染失败时显示原始文本）

### 3.6 图片渲染策略

**图片类型处理**：

1. **公式图片**：
   - 优先使用LaTeX渲染（如果有转换后的代码）
   - 降级显示原始图片（如果OCR失败）

2. **图表图片**：
   - 几何图、电路图等由 Qwen 生成 SVG 代码渲染
   - 复杂图使用占位提示（[[FIG]]），避免 SVG 生成失控
   - 支持点击放大查看

3. **图片加载**：
   - SVG 内联渲染，无额外网络请求
   - 懒加载优化（长试卷）
   - 错误占位符（加载失败时）

### 3.7 交互设计

**答题流程**：

1. **选择答案**：
   - 点击选项按钮
   - 按钮高亮显示选中状态
   - 答案记录到Store

2. **进度显示**：
   - 顶部进度条实时更新
   - 显示已答/总题数

3. **交卷**：
   - 检查是否全部答完
   - 确认对话框防止误操作
   - 计算得分并显示结果

4. **答案反馈**：
   - 正确选项：绿色高亮
   - 错误选项：红色高亮
   - 显示正确答案提示

---

## 四、关键技术难点与解决方案

### 4.1 LaTeX JSON 转义

**问题**：Qwen 输出的反斜杠 `\` 在 JSON 中需要转义，容易出错导致解析失败

**解决方案**：
- 占位符策略：模型输出 `[[BS]]` 替代 `\`，后端 decode 时还原
- 同时处理 `[[PARA]]`（段落）、`[[NL]]`（换行）、`[[FIG]]`（图形占位）
- 解码时宽松正则兼容模型可能产生的变体（如 `[[:PARA]]`、`[[(BS)]` 等）

### 4.2 SVG 生成失控

**问题**：Qwen 生成几何图/坐标系 SVG 时容易陷入死循环，重复输出相同 `<line>` 元素数千次导致 JSON 截断

**解决方案**：
- Prompt 约束：SVG ≤1500 字符、≤25 个元素、严禁重复
- API 参数 `repetition_penalty: 1.1` 打破输出循环
- 复杂图直接跳过 SVG 生成，改用 `[[FIG]]` 占位

### 4.3 KaTeX 渲染报错

**问题**：`\text{}` 内部出现 `^` 或 `_` 时 KaTeX 报错红字

**解决方案**：
- 后端 decoder 自动将 `\text{X^Y}` 改写为 `\mathrm{X^Y}`
- Prompt 铁律：带上下标的单位必须用 `\mathrm{}`

### 4.4 公式分隔符不统一

**问题**：模型有时输出 `\(...\)` 或 `\[...\]` 而非 `$...$` / `$$...$$`，前端 KaTeX 不兼容

**解决方案**：
- Prompt 铁律：只允许 `$...$`（行内）和 `$$...$$`（居中块级）
- 后端 decoder 兜底：`\(...\)` → `$...$`，`\[...\]` → `$$...$$`

### 4.5 大量题目渲染性能

**问题**：大量题目同时渲染可能卡顿

**解决方案**：
- 虚拟滚动（只渲染可见区域）
- 图片懒加载
- 组件按需渲染

---

## 五、数据格式设计

### 5.1 内容块（Content Block）

统一的内容表示方式，支持多种类型：

```typescript
interface ContentBlock {
  type: 'text' | 'formula' | 'image' | 'svg' | 'break';
  value?: string;        // 原始文本或图片路径
  latex?: string;        // 公式LaTeX代码
  src?: string;          // 图片URL
  metadata?: {
    width?: number;
    height?: number;
    alt?: string;
  };
}
```

### 5.2 题目结构

```typescript
interface Question {
  id: string;                    // 唯一标识
  number: number;                // 题号
  content: ContentBlock[];       // 题目内容（富文本数组）
  options: Option[];             // 选项数组
  correctAnswer?: string;        // 正确答案（A-F）
  explanation?: string;          // 解析
  tags?: string[];               // 标签（知识点分类）
}
```

### 5.3 试卷结构

```typescript
interface Paper {
  title: string;                 // 试卷标题
  year: number;                  // 年份
  duration: number;              // 考试时长（分钟）
  totalQuestions: number;        // 总题数
  questions: Question[];         // 题目数组
}
```

---

## 六、部署与运行

### 6.1 环境要求

- Node.js >= 18
- npm

### 6.2 构建流程

1. **后端**：
   ```bash
   cd api && npm install && npx prisma migrate dev && npm run dev
   ```

2. **前端**：
   ```bash
   cd quiz-web && npm install && npm run dev
   ```

3. **生产构建**：
   ```bash
   cd quiz-web && npm run build
   cd api && npm run build
   ```

### 6.3 目录结构

```
project/
├── api/                     # 后端 API（Express + Prisma + SQLite）
│   ├── src/
│   │   ├── routes/          # 路由层
│   │   ├── services/        # 业务层（Qwen / 解析编排 / 诊断评分）
│   │   └── middleware/      # 中间件（JWT / 管理员）
│   ├── prisma/              # 数据模型
│   └── package.json
│
├── quiz-web/                # 前端（Vue 3 + TypeScript）
│   ├── src/
│   │   ├── components/      # 共享组件（QuestionCard / FormulaBlock / LatexText）
│   │   ├── views/           # 页面
│   │   ├── stores/          # Pinia 状态管理
│   │   └── utils/           # 工具（pdfRenderer / Axios / 校验）
│   └── public/
│
└── 文档索引.md
```

---

## 七、扩展性设计

### 7.1 支持更多题型

当前实现支持选择题，可扩展支持：
- 填空题：输入框组件
- 解答题：富文本编辑器
- 多选题：多选逻辑

### 7.2 公式渲染增强

- MathJax备用方案（复杂公式）
- 手写公式识别（Canvas + 机器学习）
- 公式编辑器（可视化编辑）

### 7.3 数据持久化

- 后端API + 数据库存储
- 用户答题历史记录
- 成绩统计分析

---

## 总结

本方案采用**渲染-识别-存储**的三层架构：

1. **渲染层**：pdf.js 浏览器端渲染 PDF 为图片，零 Python 依赖
2. **识别层**：Qwen-VL-Max 多模态 AI 识别题目/公式/图形并结构化
3. **展示层**：Vue 3 组件化架构，KaTeX 公式渲染，Pinia 状态管理

核心技术特点：
- **全 Node.js**：前后端统一 TypeScript，无 Python 依赖
- **AI 驱动**：Qwen-VL-Max 端到端识别，无需规则匹配
- **组件化**：可复用的渲染组件，易于维护和扩展
- **响应式**：Pinia状态驱动，实时更新UI
- **类型安全**：TypeScript全程类型检查
