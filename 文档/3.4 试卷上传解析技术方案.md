# 试卷上传解析技术方案

## 一、整体概述

系统支持三种试卷录入方式，当前以 **Markdown 导入** 和 **JSON 导入** 为主流方式（已解析完成的成品数据），**PDF 流式上传 + Qwen AI 解析** 为原始 PDF 的首轮解析入口。

| 方式 | 适用场景 | 输入 | 产出 |
|------|---------|------|------|
| PDF 流式上传 | 原始 PDF 试卷，需 AI 识别题目 | .pdf / .png / .jpg | Qwen 逐页识别 → 入库 |
| Markdown 导入 | 已解析完成的题目数据（含 JSON 块） | .md（内嵌 ```json） | 校验 + 清洗 → 入库 |
| JSON 直接导入 | 已格式化的成品题目数据 | .json（enhanced project JSON） | 结构校验 → 入库 |

三种方式最终均写入 `Paper` 表：`questions` 字段为 JSON 字符串，`status` 初始为 `draft`，管理员审核后可发布。

---

## 二、方式一：PDF 流式上传 + Qwen AI 解析

### 2.1 流程概述

```
用户选择 PDF → 浏览器 pdf.js 逐页渲染 JPEG
  → POST /upload/paper-pages/create（创建 Paper + ParseTask）
  → POST /parse-tasks/:id/pages（逐页提交 base64）
  → 后端信号量控制（max 5 并发）调 Qwen-VL-Max
  → 全部完成后去重、排序、写入 Paper.questions
  → 前端轮询进度 → 完成
```

### 2.2 关键技术点

**前端 PDF 渲染（pdf.js）**
- 渲染倍率 1.5（108 DPI），JPEG 质量 0.85，平衡清晰度与体积
- 前 2 页文字 < 100 字符则跳过（封面/目录过滤）
- 单题图片模式：直接读原始 bytes 为 base64，不做 Canvas 重编码（避免压糊电路图细线）

**流式逐页上传**
- 先创建任务，再逐页提交，每页 ~250KB，边传边解析
- 后端收到页面后立刻返回 `{ accepted: true }`，Qwen 调用在后台执行
- 前端缓存所有渲染页面，重试时直接复用缓存数据

**信号量限流（Semaphore）**
- 限制最多 5 个 Qwen 调用同时进行，避免触发 DashScope 429 限流
- 40 页试卷约 2-3 分钟完成

**协调器（TaskCoordinator）**
- 串行化多页结果汇入：Qwen 调用在锁外并发，写入在锁内串行（~1-2ms 无等待）
- 完成后去重（同编号保留内容更长的那条 → 跨页题后半页更完整）、按题号排序

**占位符编码**
- 模型输出的 `\` → `[[BS]]`，真实换行 → `[[NL]]` / `[[PARA]]`，防止 JSON.parse 失败
- 后端 `decodePlaceholders()` 还原为真实字符，并容错变体写法

### 2.3 代码引用

| 文件 | 职责 |
|------|------|
| [quiz-web/src/utils/pdfRenderer.ts](quiz-web/src/utils/pdfRenderer.ts) | pdf.js 渲染 PDF → JPEG Base64 |
| [quiz-web/src/views/admin/PaperUpload.vue](quiz-web/src/views/admin/PaperUpload.vue) | 上传页：渲染 + 逐页 POST + 轮询 |
| [api/src/routes/upload.ts](api/src/routes/upload.ts) | `POST /upload/paper-pages/create` |
| [api/src/routes/parse.ts](api/src/routes/parse.ts) | `GET /:id` 状态 + `POST /:id/pages` 逐页提交 |
| [api/src/services/parseService.ts](api/src/services/parseService.ts) | Semaphore + TaskCoordinator + 去重排序 |
| [api/src/services/qwenService.ts](api/src/services/qwenService.ts) | Qwen-VL-Max API 封装 + 占位符解码 + JSON 修复 |

---

## 三、方式二：Markdown 导入（主流）

### 3.1 流程概述

```
用户选择 .md 文件
  → 前端 FileReader 读取文本
  → 正则提取 ```json 代码块，JSON.parse 预览题目列表
  → 用户填写元数据（标题、年份、时长、学科代码）
  → POST /papers/import-markdown（发送原始 Markdown 文本）
  → 后端：提取 → 解析 → 结构校验 → 安全清洗 → 入库
  → 前端跳转预览 / 管理页面
```

### 3.2 关键技术点

**前端预览提取**
- 正则 `/```json\s*\n([\s\S]*?)\n\s*```/g` 匹配所有 JSON 代码块
- 每个块尝试 `JSON.parse`，兼容 `{ questions: [...] }` 和纯 `[...]` 两种格式
- 统计 JSON 块数量、题目总数，展示题号列表供用户确认

**后端校验流水线（markdownValidator.ts）**

```
extractJsonBlocks() → JSON.parse → validateQuestionStructure() → sanitizeQuestionContent()
```

1. **提取** — 同前端正则提取所有 JSON 代码块
2. **结构校验** — 每题检查 `number`（必填且为有效数字）、`title`（必填且非空）、`options`（必填且非空数组）
3. **安全清洗（sanitize）** — 递归遍历所有字符串字段：
   - 保存合法 SVG 块（`<svg>...</svg>`），清洗后还原
   - 移除危险标签（`<script>`, `<iframe>`, `<object>`, `<embed>`）
   - 移除内联事件处理器（`onerror=`, `onclick=` 等）
   - 移除 `javascript:` 协议
   - 移除其余所有 HTML 标签（`<div>`, `<a>` 等）
   - HTML 实体解码（防绕过）
   - 记录超过 5 字符的被移除内容作为 warning
4. **入库** — 清洗后的题目数组写入 `Paper.questions`

**错误与警告分离**
- `errors`：阻塞性（JSON 解析失败、必填字段缺失）→ 导入中止
- `warnings`：非阻塞性（如 `code` 字段不是字符串或为空 → 自动跳过该知识点）不阻断导入，仅提示用户

### 3.3 代码引用

| 文件 | 职责 |
|------|------|
| [quiz-web/src/views/admin/PaperUpload.vue](quiz-web/src/views/admin/PaperUpload.vue) | Markdown 模式：文件选择、JSON 块预览、导入 |
| [quiz-web/src/api/upload.ts](quiz-web/src/api/upload.ts) | `importMarkdown()` API 封装 |
| [api/src/routes/papers.ts](api/src/routes/papers.ts) | `POST /papers/import-markdown` 路由 |
| [api/src/services/markdownValidator.ts](api/src/services/markdownValidator.ts) | 提取 → 解析 → 校验 → 清洗 全流程 |

---

## 四、方式三：JSON 直接导入（主流）

### 4.1 流程概述

```
用户选择 .json 文件
  → 前端 FileReader + JSON.parse 解析
  → 兼容 { paper, questions } 完整对象 和 纯题目数组
  → 自动填充元数据（标题、年份等，如 JSON 中已包含）
  → POST /papers/import-json
  → 后端校验题目结构 → 入库
```

### 4.2 关键设计点

**前端兼容两种 JSON 格式**
- 纯题目数组 `[{ number, title, options, ... }]` → 直接提取
- 完整对象 `{ title, year, questions: [...] }` → 自动填充元数据字段

**后端必填校验**
- 每题校验：`number` 不为 null、`title` 非空、`options` 为非空数组
- 校验失败返回具体错误信息（指明第几题缺少什么字段）

**适用文件格式**
- 目标文件为 enhanced project JSON（如 Qwen 解析产出的 `*.enriched.project.json`）
- 包含完整的 `knowledge_points`（知识点映射，含 code 用于考纲筛选）、`subject_code` / `topic_code`、`difficulty`、`learning_analysis` 等增强字段
- 题目结构：`number, title, content_blocks, options, answer, images, subject_code, topic_code, difficulty, knowledge_points, ...`

**注意事项**
- JSON 导入**不做**安全清洗（Markdown 导入需清洗是因为 Markdown 文本中可能夹带恶意内容）
- JSON 导入的字段完整性由上传方保证，后端仅做必填字段的结构校验

### 4.3 代码引用

| 文件 | 职责 |
|------|------|
| [quiz-web/src/views/admin/PaperUpload.vue](quiz-web/src/views/admin/PaperUpload.vue) | JSON 模式：文件选择、预览、导入 |
| [quiz-web/src/api/upload.ts](quiz-web/src/api/upload.ts) | `importJson()` API 封装 |
| [api/src/routes/papers.ts](api/src/routes/papers.ts) | `POST /papers/import-json` 路由 |

---

## 五、三种方式的共同问题与处理

### 5.1 JSON.parse 容错（AI 解析数据）

Qwen 输出的增强 JSON 已通过 `buildJsonCandidates()` 做了多层修复（非法转义、符号缺引号、截断补全），但 Markdown 中嵌入的 JSON 块仍可能因人工编辑引入格式错误。后端对每个块独立 try-catch，单个块失败不影响其他块。

### 5.2 字段兼容设计

- `knowledge_points`：知识点映射（code 对应 `SyllabusNode.code`），前端考纲树筛选匹配此字段
- `subject_code` / `topic_code`：学科与主题层级 code，也参与考纲树筛选
- 后端试题库筛选检查 `knowledgePoints` + `subjectCode` + `topicCode` 三个字段（详见 [papers.ts](api/src/routes/papers.ts) `matchSyllabusFilter`）

### 5.3 图片处理

- PDF 模式：Qwen 将图表转为内联 SVG（`<svg>` 标签）
- Markdown/JSON 导入：图片字段直接透传存入 `Paper.questions`，由前端 QuestionCard 组件渲染
- SVG 缺少 width/height 属性时后端自动补全

---

## 六、文件清单

| 文件 | 职责 |
|------|------|
| [quiz-web/src/views/admin/PaperUpload.vue](quiz-web/src/views/admin/PaperUpload.vue) | 上传页（三模式切换：MD / 文件 / JSON） |
| [quiz-web/src/utils/pdfRenderer.ts](quiz-web/src/utils/pdfRenderer.ts) | pdf.js 渲染 PDF → JPEG Base64 |
| [quiz-web/src/api/upload.ts](quiz-web/src/api/upload.ts) | 上传 + 导入 API 封装 |
| [api/src/routes/upload.ts](api/src/routes/upload.ts) | `POST /upload/paper-pages/create`（PDF 方式） |
| [api/src/routes/parse.ts](api/src/routes/parse.ts) | `GET /:id` + `POST /:id/pages`（PDF 方式） |
| [api/src/routes/papers.ts](api/src/routes/papers.ts) | `POST /import-json` + `POST /import-markdown` |
| [api/src/services/parseService.ts](api/src/services/parseService.ts) | PDF 解析：Semaphore + TaskCoordinator + 去重排序 |
| [api/src/services/qwenService.ts](api/src/services/qwenService.ts) | Qwen-VL-Max API + 占位符解码 + JSON 修复 |
| [api/src/services/markdownValidator.ts](api/src/services/markdownValidator.ts) | MD 导入：提取 → 校验 → 安全清洗 |
