# 4.5 考纲管理 PRD

## 一、需求背景

当前试题库左侧组织树依赖考纲节点展示，学生通过选择考纲节点进入对应知识范围的题量统计和练习。此前考纲缺少后台可视化管理入口，管理员无法在系统内完成考纲上传、查看、启用、停用，也无法确认当前试题库实际使用的是哪一版考纲。

本次新增“考纲管理”能力，将考纲作为后台可管理内容。管理员可以上传不同考试类型的考纲，并控制其是否应用到试题库。考纲启用后，学生端试题库左侧组织树使用该考纲；考纲停用后，该考试类型不再展示组织树，也不能继续返回可练习题量。

## 二、产品目标

1. 管理员可以维护 ESAT / TMUA / STEP 等考试类型的考纲版本。
2. 管理员可以查看已上传考纲内容，便于启用前复核。
3. 管理员可以启用某份考纲，使其应用到试题库左侧组织树。
4. 管理员可以停用某份考纲，停用后该考试类型的试题库组织树为空。
5. 没有启用考纲树时，试题库不能返回题量和题目，避免“没有考纲但仍可做题”的逻辑问题。
6. 当前已在使用的 ESAT 考纲需要沉淀到考纲管理列表中，作为当前启用版本展示。

## 三、用户角色

| 角色 | 权限 |
|------|------|
| 管理员 | 上传考纲、查看考纲、启用考纲、停用考纲 |
| 学生/游客 | 无后台管理权限，仅能在试题库中看到当前启用的考纲树 |

## 四、功能点

### 4.1 考纲列表

入口：`/admin/core-library/syllabus`

页面以表格形式展示已上传考纲，表头包含：

| 字段 | 说明 |
|------|------|
| 序号 | 当前列表顺序，从 1 开始 |
| 考纲名称 | 上传时填写或由文件名推断 |
| 考试类型 | ESAT / TMUA / STEP |
| 上传时间 | 考纲上传时间 |
| 是否启用 | 已启用 / 未启用 |
| 操作 | 查看、启用、停用 |

列表规则：

- 默认按上传时间倒序展示。
- 同一考试类型可存在多份考纲。
- 同一考试类型最多只能有一份考纲处于“已启用”状态。
- 未启用考纲可执行“查看”和“启用”。
- 已启用考纲可执行“查看”和“停用”。

### 4.2 上传考纲

管理员点击右上角“上传考纲”按钮后选择本地 JSON 文件。

上传流程：

1. 选择 JSON 文件。
2. 系统解析文件内容。
3. 解析成功后弹出上传确认弹窗。
4. 管理员确认或修改考纲名称。
5. 管理员确认考试类型。
6. 点击保存后，考纲进入列表。

上传规则：

- 仅支持 JSON 文件。
- JSON 必须是树形考纲结构。
- 节点必须包含 `code` 和 `label`。
- 节点可包含 `children`。
- 同一份考纲内节点 `code` 不允许重复。
- 上传成功后默认不启用，不影响当前试题库组织树。

### 4.3 查看考纲

管理员点击“查看”后，系统打开详情弹窗，展示该考纲的 JSON 内容。

查看能力用于：

- 检查考纲层级是否正确。
- 检查节点名称是否正确。
- 检查节点编码是否符合题目映射规则。
- 在启用前做人工复核。

### 4.4 启用考纲

管理员点击“启用”后，该考纲应用到对应考试类型的试题库组织树。

启用规则：

1. 启用某份考纲后，该考试类型下其他考纲自动变为未启用。
2. 启用成功后，学生端试题库左侧组织树立即以该考纲为准。
3. 启用新考纲会覆盖该考试类型当前正在使用的组织树。
4. 题目能否被筛选，取决于题目中记录的考纲节点编码是否能匹配当前启用考纲。

### 4.5 停用考纲

管理员点击“停用”后，系统使用 Element Plus 二次确认弹窗提醒：

> 停用后该考试类型的试题库组织树将暂时为空，确认停用？

弹窗按钮：

- 确认停用
- 取消

停用规则：

1. 点击取消，不执行任何变更。
2. 点击确认停用后，该考纲状态变为未启用。
3. 如果停用的是当前启用考纲，则该考试类型的试题库组织树变为空。
4. 组织树为空时，学生端不能继续通过该考试类型进入有效练习。
5. 组织树为空时，试题库题量统计必须返回 0，取题列表必须返回空。

## 五、试题库联动规则

### 5.1 有启用考纲时

当某考试类型存在启用考纲时：

- 试题库左侧展示该考纲树。
- 用户选择某个考纲节点后，系统按该节点及其子节点统计题量。
- 用户选择难度后，系统按考试类型、考纲节点、难度筛选题目。
- 如果某节点下没有题目，对应难度卡片显示 0，不可开始练习。

### 5.2 无启用考纲时

当某考试类型没有启用考纲时：

- 试题库左侧组织树为空。
- 题量统计返回 0。
- 难度分布全部为 0。
- 取题结果为空数组。
- 即使请求里带了历史旧节点 code，也不能返回题量或题目。

该规则用于避免以下不一致情况：

```text
考纲已停用
→ 页面没有组织树
→ 但接口仍返回 total 和 difficultyCount
→ 用户仍可开始练习
```

## 六、功能异常点

### 6.1 上传异常

| 场景 | 系统表现 |
|------|----------|
| 文件不是 JSON | 阻止进入上传确认弹窗，并提示“当前选择的文件不是 JSON 格式，请上传 .json 考纲文件。” |
| JSON 解析失败 | 阻止进入上传确认弹窗，并提示“文件内容不是合法 JSON，请检查括号、逗号和引号是否正确。” |
| 根结构不是考纲树 | 阻止进入上传确认弹窗，并提示上传包含考纲节点的 JSON 文件 |
| 节点缺少 code | 阻止进入上传确认弹窗，并提示对应节点缺少 code |
| 节点缺少 label | 阻止进入上传确认弹窗，并提示对应节点缺少 label |
| 节点 code 重复 | 阻止进入上传确认弹窗，并提示重复的节点 code |
| children 不是数组 | 阻止进入上传确认弹窗，并提示对应节点的 children 必须是数组 |

### 6.2 启用异常

| 场景 | 系统表现 |
|------|----------|
| 考纲不存在 | 启用失败，提示错误 |
| 考纲内容无法生成组织树 | 启用失败，保持原状态 |
| 同考试类型已有启用考纲 | 自动停用旧考纲，启用新考纲 |
| 启用过程中失败 | 不应出现半启用状态 |

### 6.3 停用异常

| 场景 | 系统表现 |
|------|----------|
| 用户点击取消 | 不停用，不改变列表状态 |
| 考纲不存在 | 停用失败，提示错误 |
| 停用未启用考纲 | 仅保持未启用状态，不影响当前组织树 |
| 停用当前启用考纲 | 当前考试类型组织树清空 |

### 6.4 试题库异常

| 场景 | 系统表现 |
|------|----------|
| 考纲树为空 | 题量返回 0，取题返回空 |
| 请求携带旧 code | 只要当前考纲树为空，仍返回空 |
| 当前考纲节点与题目映射不一致 | 对应节点题量为 0 |
| 难度无题 | 对应难度卡片不可开始练习 |

## 七、功能边界

### 7.1 本期包含

- 考纲上传。
- 考纲列表展示。
- 考纲 JSON 查看。
- 考纲启用。
- 考纲停用。
- 同考试类型单启用状态控制。
- 当前 ESAT 已用考纲回写到管理列表。
- 停用后试题库题量和取题返回空。
- 停用操作使用 Element Plus 二次确认。

### 7.2 本期不包含

- 在线编辑考纲节点。
- 拖拽调整考纲节点顺序。
- 恢复历史版本的差异对比。
- 展示节点数量。
- 导出考纲 JSON。
- 对题目 `knowledgePoints` / `subjectCode` / `topicCode` 做自动重映射。
- 上传考纲后自动校验题目覆盖率。

### 7.3 需要后续确认

- 是否需要展示考纲节点数量。
- 是否需要增加“启用前影响预览”，例如预计有多少题能匹配新考纲。
- 是否需要支持导出考纲 JSON。

## 八、页面交互要求

### 8.1 页面布局

- 页面沿用后台管理风格。
- 表格为核心内容。
- 右上角为主操作“上传考纲”。
- 不增加营销式说明区。
- 空状态需要明确提示可通过上传考纲开始使用。

### 8.2 弹窗要求

上传确认弹窗：

- 可编辑考纲名称。
- 可选择考试类型。
- 可取消上传。
- 保存成功后关闭弹窗并刷新列表。

查看弹窗：

- 展示格式化 JSON。
- 内容可滚动。
- 不允许在查看弹窗中直接编辑。

停用确认弹窗：

- 使用 Element Plus。
- 类型为 warning。
- 文案明确说明停用后组织树为空。
- 取消不产生任何副作用。

## 九、验收标准

### 9.1 列表

- 管理员可以进入 `/admin/core-library/syllabus`。
- 页面表格展示考纲列表。
- 表头包含：序号、考纲名称、考试类型、上传时间、是否启用、操作。
- ESAT 当前已用考纲能在列表中展示。

### 9.2 上传

- 可以上传合法 JSON 考纲。
- 上传后列表新增记录。
- 上传后默认未启用。
- 上传非 JSON 文件时，不进入上传确认弹窗，并提示“请选择有效的 JSON 考纲文件”。
- 非法 JSON 上传失败。
- 重复节点编码上传失败。

### 9.3 查看

- 点击查看能打开详情弹窗。
- 弹窗展示格式化 JSON。
- 弹窗关闭后列表状态不变。

### 9.4 启用

- 点击启用后，该考纲状态变为已启用。
- 同考试类型下其他考纲变为未启用。
- 启用后试题库左侧组织树切换为该考纲。

### 9.5 停用

- 点击停用时出现 Element Plus 警告确认弹窗。
- 点击取消不执行停用。
- 点击确认停用后，该考纲状态变为未启用。
- 如果停用的是当前启用考纲，该考试类型试题库组织树为空。

### 9.6 试题库联动

- 有考纲树时，带节点查询题量返回正确统计。
- 没有考纲树时，不带节点查询题量返回 0。
- 没有考纲树时，带历史旧节点查询题量也返回 0。
- 没有考纲树时，取题接口返回空题目列表。
- 没有考纲树时，用户不能进入有效练习。

## 十、当前实现状态

已完成：

- 大纲库管理页面。
- 考纲上传。
- 考纲查看。
- 考纲启用。
- 考纲停用。
- ESAT 当前已用考纲回写到大纲库。
- 停用确认改为 Element Plus 弹窗。
- 停用后试题库题量和取题返回空。
- 大纲库列表响应中 `data` 直接为数组。

待补充：

- 展示节点数量。
- 导出考纲 JSON。
- 启用前影响预览。
