# 测试环境部署记录

> 测试环境的实际操作日志，按时间倒序记录每次部署的配置、操作和验证结果。

---

## 2026-07-24 全量上线（部署 Skill 与安全配置完善）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布部署配置脱敏、测试与生产环境隔离、部署前运行配置校验及报告安全存储等改进。

| 项目 | 值 |
|------|-----|
| 部署环境 | `test` |
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `e5681bd` |
| Commit 说明 | 完善部署skill |
| 上线范围 | 后端 + 数据库迁移检查 + 前端 |
| RDS 逻辑备份 | `/opt/quiz/backups/mysql/${QUIZ_TEST_DATABASE}_before_deploy_20260724_154450.sql.gz` |
| uploads 备份 | `/opt/quiz/backups/uploads/uploads_before_deploy_20260724_154450.tar.gz` |
| 运行配置备份 | `/opt/quiz/backups/config/config_before_deploy_20260724_154450.tar.gz` |
| 备份清单 | `/opt/quiz/backups/manifests/backup_before_deploy_20260724_154450.txt` |
| 应用迁移 | 无；18 条既有 migration 均已应用 |

处理记录：

- 测试机仓库无未提交改动，目标远端分支存在并更新到 `e5681bd`；未执行强制重置。
- 本地首次采集 SSH 输出时，PowerShell 将 Git 写入标准错误流的正常提示误判为异常；确认远端未残留部署进程、仓库干净且 PM2 原服务在线后，从构建和迁移前的安全状态重新执行部署。
- Prisma schema 与 migration guard 通过；测试环境未配置 `SHADOW_DATABASE_URL`，按既定门禁使用 `prisma validate + migrate deploy`，确认无待应用迁移。
- 数据库操作前完成 RDS MySQL、uploads 和运行配置备份，RDS 压缩逻辑备份约 1.6 MiB，并生成 SHA-256 校验信息和备份清单。
- 运行配置键检查通过，共核对 52 个键；`API_ENV_FILE=/opt/quiz/api/.env npm run validate:runtime` 验证测试运行模式、RDS 连接和 SMTP 登录均正常。
- 完成 Prisma Client 生成、后端 TypeScript 构建、运行目录同步与 PM2 重载；前端使用 `build-only:test` 完成 Vite 测试构建并同步静态资源。
- 部署过程中未使用 `prisma db push`，未覆盖 `/opt/quiz/api/.env`，未依赖旧 `/opt/quiz/data/prod.db`。

验证结果：

- `http://127.0.0.1:3001/api/health` 与 `http://127.0.0.1/api/health` 均正常。
- `${QUIZ_TEST_PUBLIC_URL}/`、`${QUIZ_TEST_PUBLIC_URL}/api/health` 和数据库依赖只读接口 `GET /api/papers/question-bank/summary?examType=ESAT` 均返回 `200 OK`；只读接口返回 ESAT 共 40 题。
- 健康检查 Request ID `5d2a85f1-d4ce-4379-95e7-662b8b185829` 已在 `/var/log/quiz/api-out.log` 中精确匹配。
- PM2 中 `quiz-api` 状态为 `online`，端口 `80` 与 `3001` 正常监听。
- Prisma 显示 18 条 migration 均已应用，RDS MySQL schema 为最新状态。
- 服务器根分区使用率为 15%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计存在 1 个 low 风险；前端依赖审计存在 9 个已知风险（1 low、1 moderate、6 high、1 critical）。本次未执行可能引入破坏性变更的自动升级，需另行评估并回归。

---

## 2026-07-22 全量上线（TMUA 分卷诊断与试卷管理）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布 TMUA Paper 1/2 标准试卷上传、真题库展示、分卷在线作答流程，以及真题库和试题库的真实删除操作；同时停用会误删数学比较符号的上传文本安全清洗，结构与业务校验保持启用。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `74d0e2f` |
| Commit 说明 | 增加tmua试卷上传 |
| 上线范围 | 后端 + 数据库迁移检查 + 前端 |
| RDS 逻辑备份 | `/opt/quiz/backups/mysql/${QUIZ_TEST_DATABASE}_before_deploy_20260722_213026.sql.gz` |
| uploads 备份 | `/opt/quiz/backups/uploads/uploads_before_deploy_20260722_213026.tar.gz` |
| 运行配置备份 | `/opt/quiz/backups/config/config_before_deploy_20260722_213026.tar.gz` |
| 备份清单 | `/opt/quiz/backups/manifests/backup_before_deploy_20260722_213026.txt` |
| 应用迁移 | 无；18 条既有 migration 均已应用 |

处理记录：

- 测试机仓库无未提交改动，从 `551b234` 快进到 `74d0e2f`；未执行强制重置。
- Prisma schema 校验与 migration guard 通过；测试环境未配置 `SHADOW_DATABASE_URL`，按既定门禁使用 `prisma validate + migrate deploy`，确认无待应用迁移。
- 数据库操作前完成 RDS MySQL、uploads 和运行配置备份，RDS 压缩逻辑备份为 782 KiB，并生成 SHA-256 校验信息和备份清单。
- 运行配置键检查通过，共核对 52 个键；`API_ENV_FILE=/opt/quiz/api/.env npm run validate:runtime` 验证测试运行模式、RDS 连接和 SMTP 登录均正常。
- 完成 Prisma Client 生成、后端 TypeScript 构建、运行目录同步与 PM2 重载；完成前端依赖安装、Vite 生产构建和静态资源同步。
- 部署过程中未使用 `prisma db push`，未覆盖 `/opt/quiz/api/.env`，未依赖旧 `/opt/quiz/data/prod.db`。

验证结果：

- `http://127.0.0.1:3001/api/health` 与 `http://127.0.0.1/api/health` 均正常。
- `${QUIZ_TEST_PUBLIC_URL}/`、`${QUIZ_TEST_PUBLIC_URL}/api/health` 和数据库依赖只读接口 `GET /api/papers/question-bank/summary?examType=TMUA` 均返回 `200 OK`。
- 健康检查 Request ID `68f50499-4248-4581-92e8-ecf591fd5268` 已在 `/var/log/quiz/api-out.log` 中精确匹配。
- PM2 中 `quiz-api` 状态为 `online`，端口 `80` 与 `3001` 正常监听。
- Prisma 显示 18 条 migration 均已应用，RDS MySQL schema 为最新状态。
- 服务器根分区使用率为 15%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计存在 1 个 low 风险；前端依赖审计存在 8 个已知风险（1 low、1 moderate、5 high、1 critical）。本次未执行可能引入破坏性变更的自动升级，需另行评估并回归。

---

## 2026-07-21 全量上线（ESAT 三模块诊断测试）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布 ESAT 历年真题等效组合卷导入、三模块顺序作答、科目间三分钟可跳过休息、诊断重测正式流程，以及真题库和诊断列表的模块展示。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `551b234` |
| Commit 说明 | 诊断测试全链路改版 |
| 上线范围 | 后端 + 数据库迁移 + 前端 |
| RDS 逻辑备份 | `/opt/quiz/backups/mysql/${QUIZ_TEST_DATABASE}_before_deploy_20260721_192548.sql.gz` |
| uploads 备份 | `/opt/quiz/backups/uploads/uploads_before_deploy_20260721_192548.tar.gz` |
| 运行配置备份 | `/opt/quiz/backups/config/config_before_deploy_20260721_192548.tar.gz` |
| 备份清单 | `/opt/quiz/backups/manifests/backup_before_deploy_20260721_192548.txt` |
| 应用迁移 | `20260719011000_modular_diagnostic_exams`、`20260719013000_active_diagnostic_attempt` |

处理记录：

- 部署前本地后端编译、模块诊断回归测试、Prisma schema 校验、迁移状态检查和前端生产构建均通过。
- 测试机仓库无未提交改动，从 `1def7aa` 快进到 `551b234`；未执行强制重置。
- 在迁移前完成 RDS MySQL、uploads 和运行配置备份，生成 SHA-256 校验信息和备份清单。
- 运行配置键检查通过，共核对 52 个键；`API_ENV_FILE=/opt/quiz/api/.env npm run validate:runtime` 验证测试运行模式、数据库连接和 SMTP 登录均正常。
- Prisma migration guard 通过；测试环境未配置 `SHADOW_DATABASE_URL`，按既定规则使用 `prisma validate + migrate deploy` 作为迁移门禁，两条新迁移均成功应用。
- 完成 Prisma Client 生成、后端 TypeScript 构建与 PM2 重载，以及前端 Vite 构建和静态资源同步。
- 部署过程中未使用 `prisma db push`，未覆盖 `/opt/quiz/api/.env`，未依赖旧 `/opt/quiz/data/prod.db`。

验证结果：

- `http://127.0.0.1:3001/api/health` 与 `http://127.0.0.1/api/health` 均正常。
- `${QUIZ_TEST_PUBLIC_URL}/`、`${QUIZ_TEST_PUBLIC_URL}/api/health` 和数据库依赖只读接口 `GET /api/payment/config` 均返回 `200 OK`。
- 健康检查 Request ID `ac307e0b-6c88-42b5-af75-a8ba171c4d12` 已在 `/var/log/quiz/api-out.log` 中精确匹配。
- PM2 中 `quiz-api` 状态为 `online`，端口 `80` 与 `3001` 正常监听。
- Prisma 显示 18 条 migration 均已应用，RDS MySQL schema 为最新状态。
- 服务器根分区使用率为 14%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计存在 1 个 low 风险；前端依赖审计存在 7 个已知风险（1 low、1 moderate、4 high、1 critical）。本次未执行可能引入破坏性变更的自动升级，需另行评估并回归。

---

## 2026-07-18 全量上线（学生行为分析与营收成本）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布操作日志后续完善、学生用户行为分析，以及营收成本的分类与成本项管理；同时将本地营收成本数据合并到测试环境。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `1def7aa` |
| Commit 说明 | 更新营收成本 |
| 上线范围 | 后端 + 数据库迁移 + 前端 + 营收成本数据同步 |
| RDS 逻辑备份 | `/opt/quiz/backups/mysql/${QUIZ_TEST_DATABASE}_before_deploy_20260718_181605.sql.gz` |
| uploads 备份 | `/opt/quiz/backups/uploads/uploads_before_deploy_20260718_181605.tar.gz` |
| 运行配置备份 | `/opt/quiz/backups/config/config_before_deploy_20260718_181605.tar.gz` |
| 备份清单 | `/opt/quiz/backups/manifests/backup_before_deploy_20260718_181605.txt` |
| 应用迁移 | `20260718170000_add_revenue_cost_category` |

处理记录：

- 部署前完成 Prisma schema/migration guard、RDS MySQL、uploads 与运行配置备份，并生成 SHA-256 校验清单。
- `API_ENV_FILE=/opt/quiz/api/.env npm run validate:runtime` 验证通过，测试运行模式、数据库连接和 SMTP 登录正常。
- 成功应用营收成本分类迁移，完成 Prisma Client 生成、后端 TypeScript 构建、PM2 重载、前端生产构建和静态资源同步。
- 本地 `RevenueCost` 共 17 条记录按 ID 合并到测试库，17 条全部匹配；采用新增或更新方式，不删除测试环境独有记录。同步后测试库共 17 条。
- 同步后的研发工具成本包括 deepseek 4 条、claude 3 条、codex 5 条；技术基础设施成本包括数据库租赁 2 条、域名与证书 1 条、服务器租赁 2 条。

验证结果：

- `http://127.0.0.1:3001/api/health` 与 `http://127.0.0.1/api/health` 均正常。
- `${QUIZ_TEST_PUBLIC_URL}/` 与 `${QUIZ_TEST_PUBLIC_URL}/api/health` 均返回 `200 OK`。
- 数据库依赖接口 `GET /api/payment/config` 返回 `200`，且响应 `success: true`。
- Request ID `ea2150c7-0e2b-49b1-bb2b-21af682cc3b7` 已在 `/var/log/quiz/api-out.log` 中精确定位。
- PM2 中 `quiz-api` 状态为 `online`，监听 `80` 与 `3001` 端口。
- Prisma 显示 16 条 migration 均已应用，RDS MySQL 结构为最新状态。
- 服务器根分区使用率为 14%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计为 0 个漏洞；前端依赖审计仍有 6 个已知漏洞（1 low、2 moderate、2 high、1 critical），未在本次部署中执行破坏性自动升级，需另行评估升级与回归。

---

## 2026-07-17 全量上线（操作审计与 Request ID 链路）

本次使用改进后的 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布操作审计日志、角色筛选与详情、IP 规范化，以及响应头、运行日志和操作日志共用 Request ID 的排查链路。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `59bab06` |
| Commit 说明 | 增加后端运维日志 |
| 上线范围 | 后端 + 数据库迁移 + 前端 |
| RDS 逻辑备份 | `/opt/quiz/backups/mysql/${QUIZ_TEST_DATABASE}_before_deploy_20260717_122920.sql.gz` |
| 备份清单 | `/opt/quiz/backups/manifests/backup_before_deploy_20260717_122920.txt` |
| 应用迁移 | `20260717120000_add_operation_logs`、`20260717153000_add_operation_log_request_id` |

处理记录：

- 服务器仓库存在上次部署留下的未提交文件；逐文件与目标提交及其历史 Blob 比对后，确认不存在服务器独有代码，并保存为可恢复的 `quiz-deploy-pre-20260717-122352-known-git-history` stash，未使用 `reset --hard`。
- 部署前执行 Prisma schema/migration guard，随后完成 RDS MySQL、uploads 和运行配置备份并生成 SHA-256 清单。
- 首次配置演进检查发现运行环境缺少 10 个非密钥默认配置，部署在迁移前安全中止；Skill 增加安全默认值白名单后再次备份，只追加缺失键且不覆盖现有值。
- `API_ENV_FILE=/opt/quiz/api/.env npm run validate:runtime` 验证通过，确认测试运行模式、RDS 连通性和 SMTP 登录正常。
- 成功应用操作审计表及 Request ID 两条迁移，完成 Prisma Client 生成、后端构建、PM2 重载、前端生产构建和静态资源同步。
- Skill 新增 PM2 日志路径动态发现与 Request ID 自检，不再假定日志位于 `~/.pm2/logs`；当前实际日志路径为 `/var/log/quiz/api-out.log` 和 `/var/log/quiz/api-error.log`。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/` 返回 `200 OK`。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 返回 `200` 和 `X-Request-ID`。
- Request ID 已在 `/var/log/quiz/api-out.log` 的 `http.request.completed` JSON 日志中精确定位。
- 数据库依赖接口 `GET /api/payment/config` 返回 `200`。
- PM2 中 `quiz-api` 状态为 `online`，监听 `80` 和 `3001` 端口。
- Prisma 显示 15 条 migration 均已应用，RDS MySQL 结构为最新状态。
- 前端依赖审计报告 6 个已知漏洞（1 low、2 moderate、2 high、1 critical），未在本次部署中执行破坏性自动升级，需另行评估升级与回归。

---

## 2026-07-14 全量上线

本次按 `all` 范围部署当前阶段功能，包含认证与会话、诊断报告、会员权益、支付联调、个人中心、试题库与错题本样式调整。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `8610479` |
| Commit 说明 | 支付方案联调 |
| 上线范围 | 后端 + 前端 |
| RDS 逻辑备份 | `/opt/quiz/backups/mysql/${QUIZ_TEST_DATABASE}_before_deploy_20260714_193919.sql.gz` |
| 备份清单 | `/opt/quiz/backups/manifests/backup_before_deploy_20260714_193919.txt` |

处理记录：

- 部署前执行 Prisma schema 与 migration guard，检查通过后再进行备份和迁移。
- 完成 RDS MySQL、uploads 和运行配置备份，并生成 SHA-256 校验清单。
- 成功应用诊断报告流水线、答题状态、考试截止时间、认证会话、支付订单与通知、免费权益额度等 7 条 migration。
- 后端完成 TypeScript 构建、运行目录同步和 PM2 重载；前端完成 Vite 构建并同步至 `/opt/quiz/web/dist`。
- 首次执行因 Windows 上传脚本缺少 Linux 可执行位，在备份和迁移前被安全检查中止；补齐临时脚本权限后重新执行成功。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/` 返回 `200 OK`。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 正常。
- PM2 中 `quiz-api` 状态为 `online`。
- Prisma 显示测试库 10 条 migration 均已应用，数据库结构为最新状态。

---

## 2026-07-04 全量上线

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布试卷类型标准化、用户管理样式与分页、试题库/真题库管理、登录用户名统一等功能。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `6d7257a` |
| Commit 说明 | 登录时填写邮箱替换成用户名 |
| 上线范围 | 后端 + 前端 |
| 数据库备份 | `/opt/quiz/backups/prod_before_deploy_20260704_174524.db` |
| 应用迁移 | `20260704000000_paper_type_source_values`、`20260704001000_paper_type_paper_values`、`20260704171500_rename_user_name_to_username` |

处理记录：

- 部署前执行 Prisma migration guard，确认 `schema.prisma` 与 migrations 已对齐。
- 执行 `npx prisma migrate deploy`，成功应用 3 条待执行 migration。
- `User.name` 已迁移为 `User.username`，生产登录凭据统一为用户名 + 密码。
- 后端完成 TypeScript 构建、运行目录同步并重载 PM2；前端完成 Vite 构建并同步到 `/opt/quiz/web/dist`。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/` 返回 `200 OK`。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 正常。
- PM2 中 `quiz-api` 状态为 `online`。
- Prisma 显示生产库结构已是最新。

---

## 2026-06-30 全量上线

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布考纲管理相关功能。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `2e0c8c1` |
| Commit 说明 | Add user exam preferences migration |
| 上线范围 | 后端 + 前端 |
| 数据库备份 | `/opt/quiz/backups/prod_before_deploy_20260630_161331.db` |
| 应用迁移 | `20260622000001_add_membership_entitlements`、`20260628000000_add_syllabus_library`、`20260630000000_add_user_exam_preferences` |

处理记录：

- 首次部署被 Prisma migration guard 拦截，原因是 `User.examPreferences` 字段缺少已提交 migration。
- 补充 migration `20260630000000_add_user_exam_preferences` 后推送到部署分支，并重新执行全量上线。
- 部署前执行 Prisma migration guard，确认 `schema.prisma` 与 migrations 已对齐。
- 执行 `npx prisma migrate deploy`，成功应用 3 条待执行 migration。
- 后端完成 TypeScript 构建、运行目录同步并重载 PM2；前端完成 Vite 构建并同步到 `/opt/quiz/web/dist`。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/` 返回 `200 OK`。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 正常。
- PM2 中 `quiz-api` 状态为 `online`。
- Prisma 显示生产库结构已是最新。

---

## 2026-06-22 后端修复上线

本次使用 `quiztestdemo-deploy` Skill 执行 `backend` 范围上线，用于修复线上 `Question.syllabusPoints` 字段缺失导致的接口 500 问题。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260519-feature-projectDeployment` |
| 上线 Commit | `5df4937` |
| Commit 说明 | 新增考纲树 migration |
| 上线范围 | 后端 |
| 数据库备份 | `/opt/quiz/backups/prod_before_deploy_20260622_134247.db` |
| 应用迁移 | `20260622000000_add_question_syllabus_points` |

处理记录：

- 部署前执行 Prisma migration guard，确认 `schema.prisma` 与 migrations 已对齐。
- 执行 `npx prisma migrate deploy`，成功为 `Question` 表增加 `syllabusPoints` 字段。
- 后端完成 TypeScript 构建、运行目录同步并重载 PM2。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/api/papers/question-bank/summary?code=110000` 返回 `200 OK`，不再出现 `Question.syllabusPoints` 缺失错误。
- PM2 中 `quiz-api` 状态为 `online`。
- Prisma 显示生产库结构已是最新。

---

## 2026-06-22 全量上线

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260519-feature-projectDeployment` |
| 上线 Commit | `a543ea2` |
| Commit 说明 | 错题本增加分页功能 |
| 上线范围 | 后端 + 前端 |
| 数据库备份 | `/opt/quiz/backups/prod_before_deploy_20260622_105550.db`、`/opt/quiz/backups/prod_before_deploy_20260622_105949.db` |

处理记录：

- 首次执行 `npx prisma migrate deploy` 时，发现线上库中已存在 `SyllabusNode` 表，但 migration `20260620000000_add_syllabus_node` 未被 Prisma 标记为完成。
- 经检查，线上 `SyllabusNode` 表和索引与 migration 内容一致，因此执行 `npx prisma migrate resolve --applied 20260620000000_add_syllabus_node` 修复迁移状态。
- 继续执行 `npx prisma migrate deploy`，成功应用 `20260620000001_add_question_table`、`20260620000002_add_answer_fk`。
- 后端完成 TypeScript 构建并重载 PM2；前端完成 Vite 构建并同步到 `/opt/quiz/web/dist`。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/` 返回 `200 OK`。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 正常。
- PM2 中 `quiz-api` 状态为 `online`。
- Prisma 显示生产库结构已是最新。

---

## 2026-06-16 增量上线

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260519-feature-projectDeployment` |
| 上线 Commit | `bb7b269` |
| Commit 说明 | 修改试题syllabus字段和考纲对应关系 |
| 上线范围 | 后端 + 前端 |
| 数据库迁移 | `npx prisma migrate deploy`，无待执行 migration |
| 生产库备份 | `/opt/quiz/backups/prod_before_deploy_20260616_120504.db` |

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/` 返回 `200 OK`。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 正常。
- PM2 中 `quiz-api` 状态为 `online`。

---

## 2026-06-15 大纲数据迁移

试题库页面依赖 `SyllabusNode` 大纲树数据。本次没有做全量数据库迁移，只迁移本地开发库中的大纲表。

处理过程：

- 线上生产库缺少 `SyllabusNode` 表，先执行 `npx prisma db push` 将 SQLite 结构同步到当前 Prisma schema。
- 从本地 `api/prisma/dev.db` 导出 `SyllabusNode` 表 98 条记录。
- 上传到服务器 `/opt/quiz/api/syllabus-import.json`。
- 在服务器通过 Prisma 按 `examType + code` 执行 upsert。
- 导入完成后删除临时 JSON 和临时导入脚本。

迁移结果：

| examType | 节点数 |
|----------|------:|
| ESAT | 98 |

根节点：`100000` — ESAT (工程与科学入学测试)

备份文件：

- 导入前备份：`/opt/quiz/backups/prod_before_syllabus_import_20260615.db`
- 导入后备份：`/opt/quiz/backups/prod_after_syllabus_import_20260615.db`

---

## 2026-06-15 用户数据迁移

本次没有做全量数据库迁移，只迁移本地开发库中的 `User` 表数据，目的是让线上具备可登录的管理员账号。

迁移方式：

- 从本地 `api/prisma/dev.db` 导出 `User` 表 4 条记录。
- 上传到服务器 `/opt/quiz/api/users-import.json`。
- 在服务器通过 Prisma 按 `email` 执行 upsert。
- 导入完成后删除临时 JSON 和临时导入脚本。

迁移结果：

| 角色 | 数量 |
|------|-----:|
| admin | 2 |
| student | 3 |

备份文件：

- 导入前备份：`/opt/quiz/backups/prod_before_user_import_.db`
- 导入后备份：`/opt/quiz/backups/prod_after_user_import_20260615.db`

---

## 2026-06-15 首次部署

本次已按最小可行上线方案完成公网 IP 部署。

| 项目 | 值 |
|------|-----|
| 公网 IP | `${QUIZ_TEST_SSH_HOST}` |
| 登录用户 | `deploy` |
| 部署分支 | `20260519-feature-projectDeployment` |
| Git 仓库 | `https://github.com/Shiyidan/OxBridgeQuiz.git` |
| 后端运行目录 | `/opt/quiz/api` |
| 前端静态目录 | `/opt/quiz/web/dist` |
| SQLite 生产库 | `/opt/quiz/data/prod.db` |
| PM2 进程 | `quiz-api` |
| Nginx 配置 | `/etc/nginx/sites-available/quiz` |

已完成：

- 安装 Node.js `v20.20.2`、npm `10.8.2`、Nginx `1.18.0`、PM2 `7.0.1`。
- 克隆 GitHub 仓库并切换到部署分支。
- 写入 `/opt/quiz/api/.env`，生成生产 `JWT_SECRET`。
- 执行 Prisma migration，创建 SQLite 生产库。
- 完成后端 TypeScript 构建和前端 Vite 构建。
- 使用 PM2 启动 API，并配置 `pm2-deploy` systemd 开机自启。
- 配置 Nginx：`/` 指向前端 SPA，`/api/` 反向代理到 `127.0.0.1:3001`。
- 启用 UFW，仅放行 `22`、`80`、`443`。

验证结果：

- `${QUIZ_TEST_PUBLIC_URL}/` 返回 `200 OK`。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 返回 `{ "status": "ok" }`。
- PM2 中 `quiz-api` 状态为 `online`。

待补充：

- 当前 `DASHSCOPE_API_KEY` 为空，AI 试卷解析能力暂不可用。
- 当前仍使用公网 IP + HTTP，正式对外使用前需完成域名、备案和 HTTPS。

### 2026-06-15 部署前核对

本次按当前代码仓库重新核对部署方案，结论如下：

- 当前后端真实目录是 `api/`，前端真实目录是 `quiz-web/`。
- 当前 Prisma 使用 SQLite，生产环境先用 `/opt/quiz/data/prod.db`。
- 当前没有 Redis 依赖和调用。
- 前端生产构建必须设置 `VITE_API_BASE`。
- 安全组不应开放 `3001`、`5432`、`6379`，API 统一通过 Nginx 反向代理访问。
- 备案和 HTTPS 完成前，可以先用公网 IP 做内部测试。

---

## 2026-05-22 环境初始化

### ECS 选型决策

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 实例规格 | 经济型 e（ecs.e-c1m2.large）2核4G | PDF 解析间歇突发、日常低负载 |
| 操作系统 | Ubuntu 22.04 LTS | Node.js/Nginx 生态最成熟 |
| 系统盘 | ESSD Entry 40G | 代码 + SQLite + 临时文件约 20G |
| 带宽 | 100Mbps 按流量计费 | 开发测试阶段突发快、空闲不花钱 |
| 预装应用 | 无（裸系统） | 避免面板/Docker 覆盖配置 |

### 已购实例信息

| 项目 | 值 |
|------|-----|
| 公网 IP | `${LEGACY_SSH_HOST}` |
| 私网 IP | `172.26.189.52` |
| 操作系统 | Ubuntu 22.04 64位 |
| 实例规格 | ecs.e-c1m2.large（2核4G） |
| 系统盘 | ESSD Entry 40G |
| 带宽 | 100Mbps 按流量 |
| 付费方式 | 按量付费 |

### 安全组配置

最终入方向规则（仅两条）：

| 方向 | 端口 | 来源 | 说明 |
|------|:---:|------|------|
| 入方向 | `22` | 0.0.0.0/0 | SSH 远程管理 |
| 入方向 | `80` | 0.0.0.0/0 | HTTP 对外访问 |

已删除的规则：3001（API 直连）、5432（PostgreSQL）、6379（Redis）、3389（Windows RDP）。

### SSH 登录操作

```bash
ssh root@${LEGACY_SSH_HOST}
passwd
adduser deploy
usermod -aG sudo deploy
```

### 下一步待执行

- [x] `sudo apt update && sudo apt upgrade -y` — 系统更新
- [x] 安装 Node.js 20 + PM2
- [x] 安装 Nginx
- [x] 克隆代码 + 构建 + 部署
- [x] 配置 Nginx → 启动服务
