# 测试环境部署记录

> 测试环境的实际操作日志，按时间倒序记录每次部署的配置、操作和验证结果。

---

## 2026-08-25 测试环境全量部署（模考单项管理与双视图）

本次使用 `quiztestdemo-deploy` Skill 将 `20260807-quizDev` 分支的前端和后端完整发布到测试环境，新增模考 Module/Paper 独立可用性、部分套卷发布与补齐能力，以及后台套卷封面和单项表格双视图。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `7787a039516b5293c9da5ea8080c457101dab1d4` |
| 上线范围 | 前端与后端 |
| 部署前备份 | `backup_before_deploy_20260825_234926.txt`，包含数据库、上传目录、运行配置及校验和 |
| Prisma 迁移 | 成功应用 `20260825143000_add_mock_module_readiness`，共 45 条迁移；未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 测试环境与预期数据库双重门禁通过，服务器仓库通过已校验 Git bundle 快进到目标 Commit；前后端制品均从目标提交的本地隔离工作树构建，并通过清单和 SHA-256 校验。
- 部署前已完成数据库、上传目录和运行配置备份；运行时数据库连接、交易邮件和批量邮件认证均通过，新 Prisma 迁移成功应用。
- API 经 PM2 重载后保持 `online`；第一次本机健康探测处于启动窗口，自动重试后恢复正常。ECS 本机 API、Nginx、公网页面、公网 `/api/health`、数据库依赖的支付配置接口及 Request ID 日志链路均验证通过。
- 部署脚本已清理本地与远端临时制品。运行时依赖审计仍报告 1 个高危和 1 个低危问题，本次未自动升级依赖，需后续单独评估兼容性后处理。

---

## 2026-08-21 测试环境全量部署（首页微信二维码）

本次使用 `quiztestdemo-deploy` Skill 将 `20260807-quizDev` 分支的前端和后端完整发布到测试环境，在首页页脚“小红书”二维码左侧新增微信二维码及平台标识。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `e89550b2dbc3654d6225a4d61879c7b458c515b2` |
| 上线范围 | 前端与后端 |
| 部署前备份 | `backup_before_deploy_20260821_210846.txt`，包含数据库、上传目录、运行配置及校验和 |
| Prisma 迁移 | 44 条迁移均已应用，无待执行迁移；未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 测试环境与预期数据库双重门禁通过，服务器仓库以 fast-forward 更新到目标 Commit；源码包、前后端制品及二维码静态资源均通过提交、清单和 SHA-256 校验。
- 前后端在本地隔离工作树构建成功；本次后端运行依赖未变化，沿用已安装的生产依赖集。运行时数据库连接、交易邮件和批量邮件认证均通过。
- API 经 PM2 重载后保持 `online`；第一次本机健康探测处于启动窗口，自动重试后恢复正常。ECS 本机 API、Nginx、公网页面、公网 `/api/health`、数据库依赖的支付配置接口及 Request ID 日志链路均验证通过。
- 部署脚本已清理本地与远端临时制品。本次未引入新的运行依赖风险；此前依赖审计记录的问题仍需按原计划单独评估处理。

---

## 2026-08-21 测试环境全量部署（后台优化与答题页水印）

本次使用 `quiztestdemo-deploy` Skill 将 `20260807-quizDev` 分支的前端和后端完整发布到测试环境，包含用户详情、操作日志、行为分析与支付订单展示优化，以及在线答题页多行倾斜水印。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `5c371b09b822d250c24f7693e27cbd724a03b033` |
| 上线范围 | 前端与后端 |
| 部署前备份 | `backup_before_deploy_20260821_205115.txt`，包含数据库、上传目录、运行配置及校验和 |
| Prisma 迁移 | 44 条迁移均已应用，无待执行迁移；未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 测试环境与预期数据库双重门禁通过，服务器仓库以 fast-forward 更新到目标 Commit；源码包、前后端制品及校验清单均验证通过。
- 前后端在本地隔离工作树构建成功；运行时数据库连接、交易邮件和批量邮件认证通过，API 经 PM2 重载后保持 `online`。
- API 重载后的第一次本机健康探测处于启动窗口，自动重试后恢复正常；ECS 本机 API、Nginx、公网页面、公网 `/api/health`、数据库依赖的支付配置接口及 Request ID 日志链路均验证通过。
- 部署脚本已清理本地与远端临时制品。运行时依赖审计仍报告 1 个高危和 1 个低危问题，本次未自动升级依赖，需后续单独评估兼容性后处理。

---

## 2026-08-19 测试环境全量部署（模考与支付回调安全修复）

本次使用 `quiztestdemo-deploy` Skill 将 `20260807-quizDev` 分支的前端和后端完整发布到测试环境，包含模考中心近期修复及银联商务通知在验签前不得写入审计表的安全修复。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `bfbef17b92201b25fd266d42f387252d2d0e3677` |
| 上线范围 | 前端与后端 |
| 部署前备份 | `backup_before_deploy_20260819_024230.txt`，包含 RDS、上传目录、运行配置及校验和 |
| Prisma 迁移 | 44 条迁移均已应用，无待执行迁移；未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 测试环境和预期数据库双重门禁通过，服务器仓库以 fast-forward 更新到目标 Commit，源码包与前后端制品通过提交、清单和 SHA-256 校验。
- 前后端均在本地隔离工作树构建成功；运行时数据库连接、交易邮件和批量邮件认证通过，API 经 PM2 重载后保持 `online`。
- API 重载后的第一次本机健康探测处于启动窗口并自动重试成功；ECS 本机 API、Nginx、公网首页、公网 `/api/health`、数据库依赖的支付配置接口和 Request ID 日志链路均验证通过。
- 测试环境银联商务通道当前保持关闭，因此本次只验证支付配置接口和回调代码部署，不执行二维码下单或真实交易。
- 部署脚本已清理本地与远端时间戳临时证据目录。

---

## 2026-08-19 测试环境全量部署（模考中心）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境完成前端与后端发布，主要上线模考试卷库、模考答题与记录、诊断报告衔接及错题本来源区分等功能。前后端均从确认提交在本地隔离工作树中构建，测试 ECS 仅接收并校验源码包与构建制品。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `41e3e1c227ef424eb6d788413e8af6d43523c81d` |
| 上线范围 | 前端与后端 |
| 部署前备份 | 成功部署对应 `backup_before_deploy_20260819_003110.txt`；首次中止尝试对应 `backup_before_deploy_20260819_002759.txt` |
| Prisma 迁移 | 成功应用 7 条待执行迁移，其中包含模考试卷库建表与发布迁移；未使用 `prisma db push` |
| 部署结果 | 成功（运行时配置守卫修复后重试完成） |

处理与验证记录：

- 首次尝试在运行时配置守卫处停止，原因是新增的学习资料文件大小配置尚未纳入安全默认值允许列表；停止发生在迁移、PM2 重载和前端切换之前，临时证据目录已自动清理。
- 将该非敏感默认配置加入版本化允许列表后提交并推送，第二次部署自动补齐默认值；运行时环境、数据库连接、交易与批量邮件认证均验证通过。
- 服务端仓库快进到目标 Commit，源码包与前后端制品均通过 Commit、清单和 SHA-256 校验；后端运行时依赖未变化，复用现有生产依赖集。
- 7 条 Prisma 迁移全部成功应用，API 经 PM2 重载后保持 `online`；首次进程健康探测在重载窗口内连接失败，自动重试后恢复正常。
- ECS 本机 API、本机 Nginx、公网首页、公网 `/api/health` 及数据库依赖只读接口均验证成功，Request ID 已在实际 PM2 日志中匹配。
- 运行时依赖审计仍报告 1 个高危和 1 个低危问题，本次未自动升级依赖，需后续单独评估兼容性后处理。

---

## 2026-08-18 测试环境正式试题库数据同步

本次将正式环境的独立试题库快照同步到测试环境。仅执行数据替换，不部署前后端代码、不执行数据库迁移，也不重载服务；真题卷及其题目、用户、会员等其他业务数据未修改。

| 项目 | 结果 |
|------|------|
| 数据范围 | `Question.paperId IS NULL` 的独立试题库、导入批次及考纲映射 |
| 同步策略 | 清除测试旧独立试题库及其关联练习记录后，事务导入正式题库快照 |
| 清理结果 | 旧题 1,292 道；关联答卷 30 份、作答 162 条、错题汇总 128 条 |
| 导入结果 | 题目 1,755 道、导入批次 33 个、考纲映射 1,894 条；1,755 道均为已发布 |
| 快照校验 | 上传前后 SHA-256 一致，事务内与事务后数量复核通过 |
| 操作前备份 | `backup_before_question_bank_sync_20260818-68516962.txt` 对应的数据库、上传文件、运行配置及校验清单 |
| 操作结果 | 成功 |

处理与验证记录：

- 目标运行环境为 `test`，运行时数据库与测试配置一致，双重环境门禁通过。
- 数据导入完成后独立复检：ESAT 1,305 道、TMUA 450 道；旧题相关作答与错题汇总均为 0。
- 组卷清单包含 820 个不同题号，其中 747 个可匹配；其余 73 个 `CHEM-MOCK-ADAPT-...-R7-FINAL-20260818` 题号在正式题库源数据中不存在，因此本次同步后仍不可匹配。
- 测试 ECS 本机 API 与公网 `/api/health` 均验证成功，公网返回 `200 OK`。

---

## 2026-08-16 测试环境营收数据同步

本次将开发环境“营收与数据”模块对应的 `RevenueCost` 数据同步到测试环境，仅执行数据导入，不部署前后端代码、不执行数据库迁移，也不重载服务。同步采用按记录 ID 合并写入的方式，保留测试环境独有数据。

| 项目 | 结果 |
|------|------|
| 操作分支 | `20260807-quizDev` |
| 数据范围 | 仅 `RevenueCost` |
| 同步策略 | 按 ID 新增或更新，不删除测试环境原有记录 |
| 同步结果 | 来源 21 条；新增 21 条；更新 0 条；目标由 0 条变为 21 条 |
| 数据核验 | 21 条记录逐字段核验通过，差异 0 条 |
| 操作结果 | 成功 |

处理与验证记录：

- 写入前已生成测试环境数据库、上传文件和运行配置的完整备份及校验清单，可用于本次数据操作回退。
- 目标运行环境、目标数据库和部署分支门禁均通过；运行配置校验确认数据库连接与 SMTP 登录正常。
- 测试 ECS 本机 API、本机 Nginx 转发和公网 `/api/health` 均返回标准成功响应，PM2 中 `quiz-api` 持续保持 `online`。

---

## 2026-08-14 测试环境邮件通道配置修正

本次仅修正测试 API 的邮件运行配置和重载服务，不部署代码、不执行数据库迁移，也不修改业务数据。

处理与验证记录：

- 合并前已备份 `/opt/quiz/api/.env`，备份文件为 `/opt/quiz/backups/config/runtime-env-before-mail-config-20260814_193520.tar.gz`；合并脚本只允许修改交易邮件与活动邮件相关键，未输出任何凭据值。
- 注册、重置密码和修改邮箱等交易邮件统一使用 `no-reply@mail.acemock.cn`；活动群发通知统一使用独立的 `news@mail.acemock.cn`。
- 两个 SMTP 通道均完成真实认证握手且未发送邮件；PM2 重载后本机 API 恢复健康，测试环境公网 `/api/health` 返回 `200 OK`。

## 2026-08-14 测试环境前端部署（邀请码周卡体验）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行前端发布。前端从确认提交在本地隔离工作树中构建，测试 ECS 仅接收并校验源码包与前端制品；未部署后端、未修改运行配置或数据库。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `f30c0fae2974c1dcd7e38b9401fe6ebdd8fca4e8` |
| 上线范围 | 仅前端 |
| Prisma 迁移 | 未执行迁移；只读复检确认现有 37 条迁移均已应用 |
| 部署报告邮件 | 本次选择不发送 |
| 部署结果 | 成功 |

处理与验证记录：

- 目标环境、运行时环境和预期数据库门禁通过；服务器仓库从 `c4d5ca3` 快进到目标提交，源码包与前端制品均通过 Commit、清单和 SHA-256 校验。
- Vite 测试模式构建成功，前端静态资源同步完成；后端 PM2 进程未重载并持续保持 `online`。
- 公网首页、公网 `/api/health` 和数据库依赖只读接口 `/api/payment/config` 均返回 `200 OK` 与标准成功响应；Request ID 已在实际 PM2 日志中匹配。
- 自动流程完成发布、验证和报告生成后，Windows 首次删除本地临时工作树时返回权限错误；未重复部署，随后使用专用收尾脚本成功删除本地与远端时间戳临时目录。
- 脱敏 HTML 报告保存为 `quiztestdemo-test-deploy-20260814-184958.html`，位于 Git 忽略的本地报告目录中。

---

## 2026-08-12 测试环境全量部署（邀请码与个人中心体验）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境完成前端与后端发布。前后端均从确认提交在本地隔离工作树中构建，测试 ECS 仅接收并校验源码包与构建制品。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `9766387` |
| 上线范围 | 前端与后端 |
| 部署前备份 | 已生成 `before_deploy_20260812_012920` 对应的数据库、上传文件、运行时配置、清单及校验和备份 |
| Prisma 迁移 | 从 34 条更新为 37 条，邀请码奖励相关迁移已应用；未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 目标环境、运行时环境和预期数据库双重门禁通过；服务端仓库从 `fdea5b9` 快进到 `9766387`，源码包及前后端制品均通过 Commit、清单和 SHA-256 校验。
- 后端发布前完成 RDS 逻辑备份；运行时配置、数据库连接与 SMTP 登录校验通过，Prisma Client 已重新生成，数据库迁移状态为最新。
- API 与前端制品同步成功，PM2 中 `quiz-api` 保持 `online`；API 重载后的首次健康探测短暂未就绪，自动重试后恢复正常。
- 公网首页返回 200，公网 `/api/health` 与数据库依赖只读接口 `/api/payment/config` 均返回成功；健康请求的 Request ID 已在实际 PM2 运行日志中匹配。
- 脱敏 HTML 报告保存为 `quiztestdemo-test-deploy-20260812-012812.html`，位于 Git 忽略的本地报告目录中；本地与远程原始临时证据已完成清理。

---

## 2026-08-10 测试环境全量部署（试题库考纲与练习记录优化）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境完成前端与后端发布。应用代码从确认提交分别以 `backend`、`frontend` 范围串行构建和部署，测试 ECS 仅接收经过 Commit 与 SHA-256 校验的源码包和构建制品。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `fdea5b9` |
| 上线范围 | 前端与后端 |
| 严格临时练习快照审计 | 部署前 dry-run 命中 0 条旧格式答卷，影响用户 0，无需执行删除 |
| 部署前备份 | 已生成 `before_deploy_20260810_022500` 对应的数据库、上传文件、运行时配置、清单及校验和备份 |
| Prisma 迁移 | 共 34 条迁移，无待应用迁移；未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 初次 `all` 并行构建在本地 API 依赖安装阶段因 Windows 子进程异常退出，服务器尚未执行代码切换；随后改用同一受控脚本按后端、前端顺序发布，两个构建均成功。
- 目标环境、运行时环境和预期数据库门禁通过；服务端仓库从 `1871c05` 快进到 `fdea5b9`，API 与前端制品均通过 Commit、清单和 SHA-256 校验。
- 后端发布前完成 RDS 逻辑备份；运行时配置与 SMTP 校验通过，Prisma Client 已重新生成，迁移状态为最新，PM2 中 `quiz-api` 重载后保持 `online`。
- 前端测试制品构建和同步成功；独立复检确认远端 API 与前端文件时间戳已更新，公网首页返回 200，公网 `/api/health` 和数据库依赖只读接口 `/api/payment/config` 均返回成功。
- 自动流程完成发布和报告生成后因 Windows 临时工作树目录短暂占用返回非零；已使用专用收尾脚本清理两次发布对应的本地与远程时间戳目录，不影响部署结果。
- 后端与前端脱敏 HTML 报告分别保存为 `quiztestdemo-test-deploy-20260810-022428.html`、`quiztestdemo-test-deploy-20260810-022559.html`，位于 Git 忽略的本地报告目录中。

---

## 2026-08-10 测试环境数据维护（清理旧临时练习）

本次针对严格临时练习快照格式上线后无法渲染的历史测试数据执行一次性清理，未发布代码、未修改数据库结构、未重载服务。

| 项目 | 结果 |
|------|------|
| 操作分支与 Commit | `20260807-quizDev` / `1871c05` |
| 维护范围 | 测试环境旧临时练习及其关联答题、错题数据 |
| 操作前备份 | 已生成 `before_legacy_practice_cleanup_20260810_005727` 对应的数据库、上传文件、运行时配置、清单及校验和备份 |
| 数据清理 | 删除 6 条旧临时练习、5 条关联错题尝试和 5 条无剩余尝试的错题汇总；其关联答题记录由数据库级联删除 |
| 操作结果 | 成功 |

处理与验证记录：

- 写入前 dry-run 只命中 6 条 TMUA 旧格式临时练习，涉及 2 个测试用户；每条均缺少学科、知识点路径、难度和计划题量。
- 清理脚本逐条在事务中删除旧答卷和错题尝试，并按剩余尝试修复错题汇总；本次没有需要重建的汇总，删除了 5 条已无剩余尝试的汇总。
- 独立数据库复检确认目标答卷、关联答题记录、错题尝试及错题汇总最新答卷引用均为 0；再次 dry-run 显示剩余旧格式记录为 0。
- 测试 ECS 本机 API、本机 Nginx、公网首页和公网 `/api/health` 均验证成功，PM2 中 `quiz-api` 保持 `online`。

---

## 2026-08-10 测试环境全量部署（题目难度收敛为三档）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境完成前端与后端部署。确认提交先后以 `backend`、`frontend` 范围通过本地隔离构建和制品校验，测试 ECS 未执行 TypeScript 或 Vite 编译。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `1871c05` |
| 上线范围 | 前端与后端 |
| 部署前备份 | 已生成 `before_deploy_20260810_003739` 对应的数据库、上传文件、运行时配置、清单及校验和备份 |
| Prisma 迁移 | 从 32 条更新为 34 条；已应用题目难度三档枚举与临时练习快照清理迁移，未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 目标环境、运行时环境和预期数据库双重门禁通过；服务端仓库从 `a6df687` 快进到 `1871c05`，源码包与前后端制品均通过 Commit、清单和 SHA-256 校验。
- 初次 `all` 并行构建因本机前端构建进程内存不足在服务器变更前停止；随后拆分为后端、前端顺序部署。前端首次独立构建仍因残留 Node watcher 占用虚拟内存停止，清理不监听端口的旧进程后重试成功，当前开发 API、Vite 与 MySQL 服务均保留运行。
- 后端部署前完成 RDS 逻辑备份；运行时配置和 SMTP 校验通过，两条新 Prisma 迁移成功应用，数据库迁移状态为最新，Prisma Client 已重新生成。
- 后端同步与 PM2 重载成功，前端测试制品同步成功；本机 API、本机 Nginx、公网首页、公网 `/api/health` 和数据库依赖只读接口均返回成功，健康请求的 Request ID 已在实际 PM2 运行日志中匹配。
- 自动流程完成发布和报告生成后两次因 Windows 临时工作树目录短暂占用返回非零；均使用专用收尾脚本删除对应的本地与远程时间戳目录。成功及失败重试报告已保存到 Git 忽略目录，未保留原始临时证据。

---

## 2026-08-09 测试环境全量部署（诊断报告版本提示修复）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `all` 范围部署。API 与前端均从确认提交在本地隔离工作树中构建，测试 ECS 仅接收并校验源码包与构建产物，随后完成后端运行时更新、PM2 重载和前端制品切换。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `a6df687` |
| 上线范围 | 前端与后端 |
| 部署前备份 | 已生成 `before_deploy_20260809_194811` 对应的数据库、上传文件、运行时配置、清单及校验和备份 |
| Prisma 迁移 | 共 32 条迁移，数据库结构已是最新状态；未产生新迁移，未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 目标环境、运行时环境和预期数据库双重门禁通过；本地工作区干净，确认提交已推送，API 与前端制品均通过 Commit、清单和 SHA-256 校验。
- 本次提交已将前端 ESAT、TMUA 最新报告版本分别同步为 `diagnostic-report-v5`、`diagnostic-report-v6`，与服务端当前生成版本一致。
- 首次执行在远端只读预检阶段因本地调用超时中断，未进入代码更新、备份、迁移或服务重载；调整调用时限后重新执行完整受保护流程并成功。
- 运行时配置校验、部署前备份、Prisma 校验与 `migrate deploy`、Prisma Client 生成、后端同步、PM2 重载及前端制品切换均成功。
- PM2 中 `quiz-api` 为 `online`；服务重载后的首次本机健康探测处于启动窗口，自动重试后本机 API、本机 Nginx、公网首页和公网 `/api/health` 均成功。
- 数据库依赖只读接口返回成功；健康请求的 Request ID 已在实际 PM2 运行日志中匹配。
- 脱敏 HTML 报告已保存到 Git 忽略目录，远端与本地时间戳临时目录均已清理。

---

## 2026-08-09 测试环境全量部署（放开免费卷数量限制）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `all` 范围部署。API 与前端均从确认提交在本地隔离工作树中构建，测试 ECS 仅接收并校验源码包与构建产物，随后完成后端运行时更新、PM2 重载和前端制品切换。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `9c1ed47` |
| 上线范围 | 前端与后端 |
| 部署前备份 | 已生成 `before_deploy_20260809_122217` 对应的数据库、上传文件、运行时配置、清单及校验和备份 |
| Prisma 迁移 | 共 32 条迁移，数据库结构已是最新状态；未产生新迁移，未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 目标环境、运行时环境和预期数据库双重门禁通过；本地工作区干净，确认提交已推送，API 与前端制品均通过 Commit、清单和 SHA-256 校验。
- 服务端代码从上一提交快进到 `9c1ed47`；运行时配置校验、部署前备份、Prisma 校验与 `migrate deploy`、Prisma Client 生成、后端同步、PM2 重载及前端制品切换均成功。
- PM2 中 `quiz-api` 为 `online`；服务重载后的首次本机健康探测处于启动窗口，自动重试后本机 API、本机 Nginx、公网首页和公网 `/api/health` 均成功。
- 数据库依赖只读接口返回成功；健康请求的 Request ID 已在实际 PM2 运行日志中匹配。
- 脱敏 HTML 报告已保存到 Git 忽略目录，远端与本地时间戳临时目录均已清理。

---

## 2026-08-08 测试环境前端部署（诊断年份页说明文字颜色）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `frontend` 范围部署。前端从确认提交在本地隔离工作树中构建，测试 ECS 仅接收并校验构建产物，随后完成前端制品切换和公网验证。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `3c698d4` |
| 上线范围 | 前端 |
| 部署前备份 | 不适用；本次未变更后端、数据库或运行时配置 |
| Prisma 迁移 | 未执行迁移；只读检查显示共 32 条且数据库已是最新状态，未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 目标环境、运行时环境和预期数据库双重门禁通过；前端制品在本地隔离工作树中完成构建，并通过 Commit、清单及 SHA-256 校验，测试 ECS 未执行 Vite 或 TypeScript 编译。
- 前端制品切换成功，公网首页、`GET /api/health` 和数据库依赖只读接口均返回成功；Request ID 已在运行日志中匹配，后端服务未重载。
- 自动流程在发布和报告生成完成后，因 Windows 临时工作树目录权限问题返回非零；确认 Git 已解除临时工作树注册后，使用专用收尾脚本删除本次精确的本地与远端时间戳目录，最终清理成功。
- 脱敏 HTML 报告已保存到 Git 忽略目录；构建存在 Vite 插件同级依赖兼容警告和大体积分包提示，但未阻断构建或发布。

---

## 2026-08-08 测试环境前端部署（题目解析选项分析去重）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `frontend` 范围部署。前端从确认提交在本地隔离工作树中构建，测试 ECS 仅接收并校验构建产物，随后完成前端制品切换和公网验证。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `a08ffda` |
| 上线范围 | 前端 |
| 部署前备份 | 不适用；本次未变更后端、数据库或运行时配置 |
| Prisma 迁移 | 未执行迁移；只读检查显示共 32 条且数据库已是最新状态，未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 目标环境、运行时环境和预期数据库双重门禁通过；前端制品在本地隔离工作树中完成构建，并通过 Commit、清单及 SHA-256 校验，测试 ECS 未执行 Vite 或 TypeScript 编译。
- 前端制品切换成功，公网首页、`GET /api/health` 和数据库依赖只读接口均返回成功；Request ID 已在运行日志中匹配，后端服务未重载。
- 自动流程在发布和报告生成完成后，因 Windows 临时工作树目录权限问题返回非零；确认 Git 已解除临时工作树注册后，使用专用收尾脚本删除本次精确的本地与远端时间戳目录，最终清理成功。
- 脱敏 HTML 报告已保存到 Git 忽略目录；构建存在 Vite 插件同级依赖兼容警告和大体积分包提示，但未阻断构建或发布。

---

## 2026-08-08 测试环境全量部署（题目解析展示与试卷上传超时处理）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `all` 范围部署。前后端从确认提交在本地隔离工作树中并行构建，测试 ECS 仅接收并校验构建产物，随后完成部署前备份、迁移检查、运行时同步和服务重载。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `c58dcec` |
| 上线范围 | 后端 + Prisma 迁移检查 + 前端 |
| 部署前备份 | `backup_before_deploy_20260808_154349.txt` |
| Prisma 迁移 | 共 32 条，数据库已是最新状态；无待应用迁移，未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 目标环境、运行时环境和预期数据库双重门禁通过；前端与 API 制品在本地隔离工作树中完成构建，并通过 Commit、清单及 SHA-256 校验，测试 ECS 未执行 Vite 或 TypeScript 编译。
- 部署前 RDS 逻辑备份、uploads 与运行配置备份、运行时配置校验、Prisma migration 检查、后端同步、PM2 重载和前端制品切换均成功。
- PM2 重载后的首次本机健康检查处于短暂启动窗口，脚本自动重试后成功；公网首页、`GET /api/health` 和数据库依赖只读接口均返回成功，Request ID 已在运行日志中匹配。
- 脱敏 HTML 报告已保存到 Git 忽略目录；本地及远端时间戳临时目录均已自动清理。

---

## 2026-08-08 测试环境全量部署（学习目标与诊断测试交互）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `all` 范围部署。前后端从确认提交在本地隔离工作树中并行构建，测试 ECS 仅接收并校验构建产物，随后完成部署前备份、迁移检查、运行时同步和服务重载。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `f94c1e1` |
| 上线范围 | 后端 + Prisma 迁移检查 + 前端 |
| 部署前备份 | `backup_before_deploy_20260808_022704.txt` |
| Prisma 迁移 | 共 32 条，数据库已是最新状态；无待应用迁移，未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 首次本地隔离构建中，前端构建成功，但 API 的 Prisma Client 生成返回非零退出码；流程在制品上传和服务器更新前安全停止，失败报告已生成，本地及远端临时目录已自动清理。
- 对同一 Commit 重试后 Prisma Client 生成与 API TypeScript 构建成功；前端和 API 制品通过 Commit、清单及 SHA-256 校验，测试 ECS 未执行 Vite 或 TypeScript 编译。
- 目标环境、运行时环境和预期数据库双重门禁通过；部署前逻辑备份、运行时配置校验、Prisma migration 检查、后端同步、PM2 重载和前端制品切换均成功。
- PM2 中 `quiz-api` 为 `online`；公网首页、`GET /api/health` 和数据库依赖只读接口均返回成功，Request ID 已在运行日志中匹配，最终临时目录清理成功。
- 脱敏 HTML 报告已保存到 Git 忽略目录；前端构建仍有 Vite 插件同级依赖兼容警告和大体积分包提示，但未阻断构建或发布。

---

## 2026-08-07 测试环境前端部署（首页命题模型文案与样式）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `frontend` 范围部署。前端从确认提交在本地隔离工作树中构建，测试 ECS 仅接收并校验构建产物，随后完成前端制品切换和公网验证。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `6222cc1` |
| 上线范围 | 前端 |
| 部署前备份 | 不适用；本次未变更后端、数据库或运行时配置 |
| Prisma 迁移 | 未执行迁移；只读检查显示共 32 条且数据库已是最新状态，未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 目标环境、运行时环境和预期数据库双重门禁通过；前端制品在本地隔离工作树中完成构建并通过 Commit、清单和 SHA-256 校验，测试 ECS 未执行 Vite 或 TypeScript 编译。
- 前端制品切换成功，公网首页与 `GET /api/health` 均返回成功；数据库依赖只读接口正常，Request ID 已在运行日志中匹配，后端服务未重载。
- 报告生成后，自动删除本地临时工作树时遇到权限错误，未影响已经完成的前端发布和验证；随后使用专用收尾脚本删除本次精确的本地及远端时间戳目录，最终清理成功。
- 脱敏 HTML 报告已保存到 Git 忽略目录；构建过程中出现 Vite 插件同级依赖兼容警告和大体积分包提示，但未阻断构建或发布。

---

## 2026-08-07 测试环境全量部署（首页样式与访客访问流程）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `all` 范围部署。前后端从确认提交在本地隔离工作树中并行构建，测试 ECS 仅接收并校验构建产物，随后完成部署前备份、迁移检查、运行时同步和服务重载。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260807-quizDev` |
| 上线 Commit | `27221fc` |
| 上线范围 | 后端 + Prisma 迁移检查 + 前端 |
| 部署前备份 | `backup_before_deploy_20260807_214800.txt` |
| Prisma 迁移 | 共 32 条，数据库已是最新状态；无待应用迁移，未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 首次远端预检因 Windows 工作区中的 Bash 脚本带有 CRLF 换行而安全停止，发生在 Git 更新、构建、备份、迁移和服务重载之前；临时目录已自动清理。将本次部署所需脚本规范化为 LF 并确认内容哈希与提交一致后重新执行成功。
- 目标环境、运行时环境和预期数据库双重门禁通过；前端与 API 制品均在本地隔离工作树中完成构建并通过 Commit、清单和 SHA-256 校验，测试 ECS 未执行 Vite 或 TypeScript 编译。
- 部署前逻辑备份、运行时配置校验、Prisma migration 检查、后端同步、PM2 重载和前端制品切换均成功；PM2 中 `quiz-api` 为 `online`。
- 公网首页、`GET /api/health` 和数据库依赖只读接口均返回成功，Request ID 已在运行日志中匹配；脱敏 HTML 报告已保存到 Git 忽略目录，本地及远端时间戳临时目录均已自动清理。

---

## 2026-08-06 测试环境全量部署（网站访问与用户分析）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `all` 范围部署。前后端从确认提交在本地隔离工作树中构建，测试 ECS 仅接收并校验构建产物，随后完成部署前备份、新增访问统计迁移、运行时同步和服务重载。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260805-feature-ProjectOptimization` |
| 上线 Commit | `8e7a522` |
| 上线范围 | 后端 + Prisma 迁移 + 前端 |
| 部署前备份 | `backup_before_deploy_20260806_004103.txt` |
| Prisma 迁移 | 应用 `20260805120000_website_traffic_analytics`，部署后共 32 条且数据库已同步；未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 首次尝试因测试服务器尚无目标功能分支，在 bundle 激活阶段安全停止；修复首次分支创建守卫并推送后重试。
- 第二次尝试完成 RDS、uploads 和运行配置备份后，配置守卫发现缺少访问统计 HMAC 密钥并停止于迁移和服务重载之前；随后使用带环境校验的专用脚本备份 `.env`、生成稳定密钥且未输出密钥值，数据库连接与 SMTP 登录验证通过。
- 最终尝试完成制品 Commit 与 SHA-256 校验、RDS 部署前逻辑备份、Prisma 迁移、后端同步、PM2 重载和前端制品切换；测试 ECS 未执行 Vite 或 TypeScript 编译。
- PM2 重载后的首次本机健康检查处于短暂启动窗口，脚本自动重试后成功；公网首页、`GET /api/health` 和数据库依赖只读接口均返回成功，Request ID 已在运行日志中匹配。
- PM2 中 `quiz-api` 为 `online`，迁移状态为最新；脱敏 HTML 报告已保存到 Git 忽略目录，本地及远端时间戳临时目录均已自动清理。

---

## 2026-08-05 测试环境全量部署（诊断测试逻辑与页面交互优化）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `all` 范围部署。前后端从确认提交在本地隔离工作树中并行构建，测试 ECS 仅接收并校验构建产物，随后完成部署前备份、迁移检查、运行时同步和服务重载。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `4793d88` |
| 上线范围 | 后端 + Prisma 迁移检查 + 前端 |
| 部署前备份 | `backup_before_deploy_20260805_181746.txt` |
| Prisma 迁移 | 共 31 条，数据库已是最新状态；无待应用迁移，未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 目标环境、运行时环境和预期数据库双重门禁通过；构建产物的 Commit 与 SHA-256 校验通过。
- 本地隔离工作树完成前后端依赖安装与并行构建；测试 ECS 未执行 Vite 或 TypeScript 编译。
- 部署前完成 RDS MySQL、uploads 和运行配置逻辑备份；运行配置完整性、数据库连接与 SMTP 登录验证均通过。
- PM2 重载后的首次本机健康检查处于短暂启动窗口，脚本自动重试后成功；未重新执行部署，也未发生回滚或部分发布。
- 公网首页、`GET /api/health` 和数据库依赖只读接口均返回成功；PM2 中 `quiz-api` 为 `online`，Request ID 已在运行日志中匹配。
- 脱敏 HTML 报告已保存到 Git 忽略目录，本地及远端时间戳临时目录均已自动清理。

---

## 2026-08-04 测试环境全量部署（TMUA 诊断报告与会员入口调整）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `all` 范围部署。前后端从确认提交在本地隔离工作树中并行构建，测试 ECS 仅接收并校验构建产物，随后完成部署前备份、迁移检查、运行时同步和服务重载。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `937dca8` |
| 上线范围 | 后端 + Prisma 迁移检查 + 前端 |
| 部署前备份 | `backup_before_deploy_20260804_031047.txt` |
| Prisma 迁移 | 共 31 条，数据库已是最新状态；无待应用迁移，未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 目标环境、运行时环境和预期数据库双重门禁通过；构建产物的 Commit 与 SHA-256 校验通过。
- 本地隔离工作树完成前后端依赖安装与并行构建；测试 ECS 未执行 Vite 或 TypeScript 编译。
- 部署前完成 RDS MySQL、uploads 和运行配置逻辑备份；运行配置完整性、数据库连接与 SMTP 登录验证均通过。
- PM2 重载后的首次本机健康检查处于短暂启动窗口，脚本自动重试后成功；未重新执行部署，也未发生回滚或部分发布。
- 公网首页、`GET /api/health` 和数据库依赖只读接口均返回成功；PM2 中 `quiz-api` 为 `online`，Request ID 已在运行日志中匹配。
- npm 运行时依赖审计仍报告 1 项低风险问题，未阻断本次部署，需在后续依赖维护中处理。
- 脱敏 HTML 报告已保存到 Git 忽略目录，本地及远端时间戳临时目录均已自动清理。

---

## 2026-08-03 测试环境全量部署（诊断测试优化）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `all` 范围部署。前后端从确认提交在本地隔离工作树中并行构建，测试 ECS 仅接收并校验构建产物，随后完成部署前备份、迁移检查、运行时同步和服务重载。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `7e2101d` |
| 上线范围 | 后端 + Prisma 迁移检查 + 前端 |
| 部署前备份 | `backup_before_deploy_20260803_202527.txt` |
| Prisma 迁移 | 共 31 条，数据库已是最新状态；无待应用迁移，未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 目标环境、运行时环境和预期数据库双重门禁通过；构建产物的 Commit 与 SHA-256 校验通过。
- 本地隔离工作树完成前后端依赖安装与并行构建；测试 ECS 未执行 Vite 或 TypeScript 编译。
- 部署前完成 RDS MySQL、uploads 和运行配置逻辑备份；运行配置完整性、数据库连接与 SMTP 登录验证均通过。
- PM2 重载后的首次本机健康检查处于短暂启动窗口，脚本自动重试后成功；未重新执行部署，也未发生回滚或部分发布。
- 公网首页、`GET /api/health` 和数据库依赖只读接口均返回成功；PM2 中 `quiz-api` 为 `online`，Request ID 已在运行日志中匹配。
- 脱敏 HTML 报告已保存到 Git 忽略目录，本地及远端时间戳临时目录均已自动清理。

---

## 2026-08-03 测试环境全量部署（首页优化）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `all` 范围部署。前后端从确认提交在本地隔离工作树中并行构建，测试 ECS 仅接收并校验构建产物，随后完成数据库备份、迁移检查、运行时同步和服务重载。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `f1583da` |
| 上线范围 | 后端 + Prisma 迁移检查 + 前端 |
| 部署前备份 | `backup_before_deploy_20260803_154438.txt` |
| Prisma 迁移 | 共 31 条，数据库已是最新状态；无待应用迁移，未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 目标环境、运行时环境和预期数据库双重门禁通过；服务器仓库从既有提交快进到目标提交，构建产物的 Commit 与 SHA-256 校验通过。
- 本地隔离工作树完成前后端依赖安装与并行构建；测试 ECS 未执行 Vite 或 TypeScript 编译。
- 部署前完成 RDS MySQL、uploads 和运行配置逻辑备份；运行配置完整性、数据库连接和 SMTP 登录验证均通过。
- PM2 重载后的首次本机 API 健康检查处于短暂启动窗口，脚本自动重试后成功；未重新执行部署，也未造成回滚或部分发布。
- 公网首页、`GET /api/health` 和数据库依赖只读接口均返回成功；PM2 中 `quiz-api` 为 `online`，Request ID 已在实际运行日志中匹配。
- 脱敏 HTML 报告已保存到 Git 忽略目录，本地及远端时间戳临时目录均已自动清理。

---

## 2026-08-02 测试环境全量部署（首页、错题本与诊断报告界面更新）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `all` 范围部署。前后端均从确认提交在本地隔离工作树中构建，测试 ECS 仅接收并校验构建产物，随后完成运行时同步、数据库迁移检查和服务重载。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `ab892dc` |
| 上线范围 | 后端 + Prisma 迁移检查 + 前端 |
| 部署前备份 | `backup_before_deploy_20260802_175839.txt` |
| Prisma 迁移 | 共 31 条，数据库已是最新状态；未使用 `prisma db push` |
| 部署结果 | 成功 |

处理与验证记录：

- 首次尝试因本地工具短时限中断，在远端目标预检阶段安全停止，未执行 Git 更新、构建、迁移或服务重载；第二次使用完整时限重新执行并成功。
- 本地隔离工作树完成前后端依赖安装与并行构建，产物 Commit 和 SHA-256 校验通过；测试 ECS 未执行 Vite 或 TypeScript 编译。
- 部署前完成 RDS MySQL、uploads 和运行配置逻辑备份；运行环境、预期数据库、配置完整性、数据库连接和 SMTP 登录验证均通过。
- 公网首页和 `GET /api/health` 返回成功，数据库依赖只读接口 `GET /api/payment/config` 返回成功。
- PM2 中 `quiz-api` 状态为 `online`；本机 API 与 Nginx 反向代理健康检查通过，请求 ID 已在实际运行日志中匹配。
- 脱敏 HTML 报告已保存到 Git 忽略的 `.private/deployment-reports/`，本地及远端时间戳临时目录均已清理。

---

## 2026-08-02 测试环境全量部署（首页与试题库更新）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `all` 范围部署。前后端均从确认提交在本地隔离工作树中构建，测试 ECS 仅接收并校验构建产物，然后完成运行时同步、数据库迁移和服务重载。

| 项目 | 结果 |
|------|------|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `fa9f4e9` |
| 上线范围 | 后端 + Prisma 迁移 + 前端 |
| 部署前备份 | `backup_before_deploy_20260802_110029.txt` |
| Prisma 迁移 | 新增 3 条，部署后共 30 条且数据库已同步 |
| 部署结果 | 成功 |

处理记录：

- 部署前已恢复 `deploy` 私钥访问，并启用 SSH 密钥登录基线：禁止 root、密码和键盘交互认证；该变更只影响服务器访问策略，不改变本地构建、服务器激活的部署架构。
- 首次尝试在 SSH 握手阶段停止，未发生构建或服务器变更；调整安全组访问策略后恢复连接。
- 后续两次尝试分别暴露了 Windows PowerShell 将 npm 普通警告和远程 Git bundle 成功诊断误判为异常的问题，均在数据库备份、迁移和运行版本切换前停止。部署 Skill 已通过 `b10d379` 和 `fa9f4e9` 修正为仅依据原生命令退出码判断成败，同时继续保留诊断输出。
- 最终尝试完成本地前后端并行构建、Commit 与 SHA-256 产物校验、增量 Git bundle 更新和临时证据清理；测试 ECS 未执行 Vite 或 TypeScript 编译。
- 部署前完成 RDS MySQL、uploads 和运行配置逻辑备份。成功应用 `20260801120000_set_question_bank_limit_25`、`20260801130000_add_question_quality_tier`、`20260801131000_add_question_attempt_payload`，未使用 `prisma db push`。
- 运行配置完整性、测试环境与数据库双重门禁、数据库连接和 SMTP 登录均通过，原有敏感配置未被覆盖。

验证结果：

- 公网首页、`GET /api/health` 和数据库依赖只读接口 `GET /api/payment/config` 均返回成功。
- PM2 中 `quiz-api` 状态为 `online`；本机 API 与 Nginx 反向代理健康检查通过，健康请求的 Request ID 已在实际运行日志中匹配。
- 部署报告保存于 Git 忽略的 `.private/deployment-reports/`，本地和远程时间戳临时目录均已自动清理。

---

## 2026-08-01 TMUA 2023 真题与关联作答记录清理

本次在 `test` 环境对 `examType=TMUA`、`year=2023` 的目标数据执行精确清理。操作前先只读解析目标集合并创建 RDS 逻辑备份；删除时校验试卷 ID 未发生漂移，全部变更在单个 Prisma 事务中完成，未修改数据库结构。

| 项目 | 结果 |
|------|------|
| 目标试卷 | `TMUA真卷-2023年`（`TMUA-2023`） |
| 清理前备份 | `backup_before_tmua_2023_cleanup_20260801_200128.txt` |
| 删除内容 | 1 套试卷、40 道题、2 次考试记录、80 条答题记录、1 个诊断报告任务、1 份诊断报告 |
| 无需删除 | 题目考纲映射、解析任务、旧版诊断会话、孤立导入批次均为 0 |
| 清理后核验 | TMUA 2023 目标及关联记录均为 0 |
| 隔离性核验 | 11 个用户仍保留；8 套非目标试卷仍保留，其中 TMUA 其他年份 2 套 |

处理说明：

- 备份脚本仅新增本次数据库备份和 SHA-256 校验清单，没有执行历史备份清理。
- 关联考试记录既包含直接引用目标试卷的记录，也包含答题明细引用目标题目的记录，避免题目删除后遗留不完整作答。
- 用户账号、会员权益、备考偏好、其他考试类型及 TMUA 其他年份数据均不在本次事务范围内。

---

## 2026-08-01 测试环境全量部署（本地构建产物流程）

本次使用 `quiztestdemo-deploy` Skill 对 `test` 环境执行 `all` 范围部署。前端与后端均在本地隔离工作树中从指定提交构建，服务器只接收经校验的构建产物，不在测试 ECS 上执行 Vite、TypeScript 或前端依赖安装。

| 项目 | 值 |
|------|-----|
| 部署环境 | `test` |
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `609d1ed` |
| Commit 说明 | 修正测试运行目录中的 Prisma Client 生成路径 |
| 上线范围 | 后端 + 数据库迁移检查 + 前端 |
| 应用迁移 | 无；27 条既有 migration 均已应用 |
| 部署结果 | 成功 |

处理记录：

- 部署前完成测试环境 RDS 逻辑备份、上传目录与运行配置归档；部署脚本仅保留本地忽略的详细报告与服务端既有保留期内备份。
- 本地临时隔离工作树完成后端 Prisma Client 生成、TypeScript 构建，以及前端测试模式构建；上传前已校验分支、Commit、构建范围和 SHA-256 清单。
- 服务器端复用运行时依赖并在运行时目录生成 Linux Prisma Client；运行配置完整性、数据库连接和 SMTP 登录校验均通过，未覆盖既有敏感配置。
- Prisma schema/migration guard 与 `prisma migrate deploy` 通过，无待应用迁移；部署过程未使用 `prisma db push`。
- 后端运行产物和前端静态资源同步后，PM2 已完成安全重载；本次流程涉及的本地与远端临时部署目录均已清理。

验证结果：

- 公网首页、`GET /api/health` 和数据库依赖只读接口均返回成功；本机 API 与 Nginx 反向代理健康检查同样通过。
- PM2 中 `quiz-api` 状态为 `online`；健康检查响应的 Request ID 已在运行日志中匹配。
- 本地详细部署证据保存于 Git 忽略的 `.private/deployment-reports/`，不包含可公开的运行时凭据。

---

## 2026-08-01 测试业务数据重置（保留注册账号）

本次在 `test` 环境执行受保护的业务数据清理，用于重新开始联调测试。执行脚本仅允许测试运行环境调用，并要求显式确认参数；未使用 `prisma db push`，未改动数据库结构或服务器运行时配置。

| 项目 | 结果 |
|------|------|
| 执行分支 / Commit | `20260626-feature-development` / `534f1df` |
| 清理前备份 | `backup_before_test_business_data_reset_20260801-163439.txt` |
| 保留范围 | 11 个 `User` 的账号、邮箱、密码哈希、角色与邮箱验证状态 |
| 清理范围 | 题库、试卷、考纲、答题与考试记录、报告、错题本、诊断会话、用户会话、验证码、会员权益、备考偏好、订单/退款/支付通知及相关运营记录 |
| 保留系统配置 | 套餐定义、权益配置、支付配置 |
| 清理后核验 | 除 `User` 外的上述业务表计数均为 `0`；本机与公网 `/api/health` 均返回成功 |

处理说明：

- 所有既有登录会话已清除，保留的用户需重新登录；账号密码本身未变更。
- 题库和考纲数据已清空，相关页面将按项目既有空状态显示，直到重新导入题目与试卷。
- 清理前完成 RDS 逻辑备份、上传目录与运行配置归档，并生成校验清单；备份保留策略仍由服务器既有任务管理。

---

## 2026-08-01 新测试 ECS 首次全量部署与运维基线

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围部署，将测试环境迁移至新的低成本 ECS，并将后端、前端、RDS 运行配置和运维基线一并完成验证。

| 项目 | 值 |
|------|-----|
| 部署环境 | `test` |
| 部署分支 | `20260626-feature-development` |
| 应用发布 Commit | `2081c6f` |
| 最终仓库 Commit | `d9cc514` |
| 上线范围 | 后端 + 数据库迁移检查 + 前端 + 运维基线 |
| 最终备份清单 | `backup_before_deploy_20260801_015825.txt` |
| 应用迁移 | 无；27 条既有 migration 均已应用 |
| 部署结果 | 成功 |

处理记录：

- 新 ECS 完成 Node.js、PM2、Nginx、MySQL 客户端和 2 GiB swap 初始化；运行时配置以独立测试环境文件写入，未复制或覆盖其他环境的敏感配置。
- 新服务器无法稳定访问 Git 远端时，按 Skill 的受校验增量 bundle 兜底流程完成快进更新；部署前后均验证目标分支和 Commit，未改变 `origin` 配置。
- 后端依赖、Prisma schema/migration guard、运行时数据库与 SMTP 校验全部通过；部署前完成 RDS MySQL、uploads 与运行配置逻辑备份，并生成校验清单。`prisma migrate deploy` 确认无待应用迁移，未使用 `prisma db push`。
- 完成 Prisma Client 生成、后端 TypeScript 构建、运行目录同步、PM2 发布，以及测试模式前端构建与静态文件同步。工作区中未提交的支付相关本地改动未纳入本次部署。
- 已安装版本化运维基线：RDS 逻辑备份和健康检查定时器、PM2 日志轮转、受限 Nginx 管理权限，以及 PM2 开机恢复服务；现有运行进程保持在线，开机恢复将在后续重启时生效。

验证结果：

- 公网首页、`GET /api/health` 和数据库依赖只读接口 `GET /api/papers/syllabus?examType=ESAT` 均返回 `200 OK`，健康接口返回 `success: true`。
- PM2 中 `quiz-api` 状态为 `online`；本机 API 与 Nginx 反向代理均健康，健康检查 Request ID 已在应用运行日志中精确匹配。
- 运维基线、PM2 保存状态、备份文件完整性与定时器状态均已核验；根分区使用率约 18%，2 GiB swap 已启用。
- 测试环境当前按 HTTP 公网地址验收，未配置测试域名和 HTTPS；这不影响联调，但不应作为生产访问方式复用。

---

## 2026-07-31 全量上线（首页、练习册与试题库）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布首页改版、练习本创建与历史练习闭环、题库练习交卷解析流程、后台题目预览交互，以及 TMUA 试题库导入字段统一为 `paper1` / `paper2` 等改动。

| 项目 | 值 |
|------|-----|
| 部署环境 | `test` |
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `a61932d` |
| Commit 说明 | 试题库上传 TMUA 的 part 改成 paper |
| 上线范围 | 后端 + 数据库迁移 + 前端 |
| 最终备份清单 | `backup_before_deploy_20260731_013045.txt` |
| 应用迁移 | `20260730224500_add_practice_notebooks`、`20260730231500_restore_question_bank_limit_100` |
| 部署结果 | 成功 |

处理记录：

- 测试机仓库无未提交改动，从 `2964c73` 快进到 `a61932d`，依次纳入首页改版、练习册与 TMUA 题库导入字段调整三次提交，未执行强制重置。
- 测试环境和预期数据库双重门禁通过；迁移前完成 RDS MySQL、uploads 和运行配置备份，并生成备份清单与 SHA-256 校验信息。
- Prisma schema 与 migration guard 通过；成功应用两条练习本及免费题库额度相关 migration，部署后 27 条 migration 均已应用。
- 运行配置完整性检查共核对 53 个键，运行时校验确认测试模式、数据库连接、SMTP 登录和银商测试配置正常。
- 完成 Prisma Client 生成、后端 TypeScript 构建、运行目录同步与 PM2 重载；前端使用测试模式完成构建及静态资源同步。
- 部署过程未使用 `prisma db push`，未覆盖服务器既有敏感配置。

验证结果：

- 公网首页、`GET /api/health` 和数据库依赖只读接口 `GET /api/papers/syllabus?examType=TMUA` 均返回 `200 OK`，健康接口返回 `success: true`。
- PM2 中 `quiz-api` 状态为 `online`，端口 `80` 与 `3001` 正常监听；健康检查 Request ID 已在 PM2 实际运行日志中精确匹配。
- 服务器根分区使用率为 15%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计仍有 1 个 low 风险；前端依赖审计存在 9 个已知风险（1 low、1 moderate、6 high、1 critical），并有 Vite 插件 peer dependency 与大体积 chunk 警告。本次未执行可能引入破坏性变更的自动升级，需另行评估并回归。

---

## 2026-07-30 全量上线（诊断测试流程与全局考试类型）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布导航栏全局考试类型联动、诊断测试列表与分数趋势真实数据、诊断答题页布局和流程弹窗等近期改动。

| 项目 | 值 |
|------|-----|
| 部署环境 | `test` |
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `2964c73` |
| Commit 说明 | 诊断报告精调 |
| 上线范围 | 后端 + 数据库迁移检查 + 前端 |
| 最终备份清单 | `backup_before_deploy_20260730_010012.txt` |
| 应用迁移 | 无；25 条既有 migration 均已应用 |
| 部署结果 | 成功 |

处理记录：

- 测试机仓库由 `db824f8` 快进到 `2964c73`，工作区保持干净，未执行强制重置。
- 首轮运行配置检查发现新增键 `CHINAUMS_EXPECTED_MID` 尚未同步，部署在迁移、构建和重载前安全停止；确认该键仅生产必填后，在测试环境以空值补齐，并保留修改前的运行配置备份。
- 配置修正前后均完成 RDS MySQL、uploads 和运行配置备份；最终备份清单及 SHA-256 校验信息以 `backup_before_deploy_20260730_010012.txt` 为准。
- 测试环境与预期 RDS 数据库双重门禁通过；运行配置完整性检查共核对 53 个键，运行时校验确认测试模式、数据库连接、SMTP 登录和银商测试配置正常。
- Prisma schema 与 migration guard 通过；测试环境未配置 `SHADOW_DATABASE_URL`，按既定门禁使用 `prisma validate + migrate deploy`，本次无待应用 migration。
- 完成 Prisma Client 生成、后端 TypeScript 构建、运行目录同步与 PM2 重载；前端使用测试模式完成构建及静态资源同步。
- 部署过程未使用 `prisma db push`，未覆盖服务器既有敏感配置。

验证结果：

- 公网首页、`GET /api/health` 和数据库依赖只读接口 `GET /api/papers/syllabus?examType=TMUA` 均返回 `200 OK`，健康接口返回 `success: true`。
- PM2 中 `quiz-api` 状态为 `online`，端口 `80` 与 `3001` 正常监听；健康检查 Request ID 已在 PM2 实际运行日志中精确匹配。
- 服务器根分区使用率为 15%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计仍有 1 个 low 风险；前端依赖审计存在 9 个已知风险（1 low、1 moderate、6 high、1 critical），并有 Vite 插件 peer dependency 与大体积 chunk 警告。本次未执行可能引入破坏性变更的自动升级，需另行评估并回归。

---

## 2026-07-29 全量上线（试题质量等级）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布试题库 `qualityTier` 字段的导入校验与保存，并在上传包题目列表中以“优秀 / 标准”展示质量等级。

| 项目 | 值 |
|------|-----|
| 部署环境 | `test` |
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `db824f8` |
| Commit 说明 | 试题库题目上传增加 qualityTier 字段 |
| 上线范围 | 后端 + 数据库迁移检查 + 前端 |
| 备份清单 | `backup_before_deploy_20260729_152101.txt` |
| 应用迁移 | 无；25 条既有 migration 均已应用 |
| 部署结果 | 成功 |

处理记录：

- 测试机仓库无未提交改动，从 `d294907` 快进到 `db824f8`，未执行强制重置。
- 测试环境和预期 RDS 数据库双重门禁通过；迁移检查前完成 RDS MySQL、uploads 和运行配置备份，并生成备份清单与 SHA-256 校验信息。
- Prisma schema 与 migration guard 通过；本次无待应用 migration，部署后 25 条 migration 均已应用。
- 运行配置完整性检查通过，共核对 52 个键；运行时校验确认测试模式、数据库连接和 SMTP 登录正常。
- 完成 Prisma Client 生成、后端 TypeScript 构建、运行目录同步与 PM2 重载；前端使用测试模式完成构建及静态资源同步。
- 部署过程未使用 `prisma db push`，未覆盖服务器运行配置。

验证结果：

- 公网首页、`GET /api/health` 和数据库依赖只读接口 `GET /api/payment/config` 均返回 `200 OK`，健康接口返回 `success: true`。
- PM2 中 `quiz-api` 状态为 `online`，端口 `80` 与 `3001` 正常监听；健康检查 Request ID 已在 PM2 实际运行日志中精确匹配。
- 服务器根分区使用率为 15%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计仍有 1 个 low 风险；前端依赖审计存在 9 个已知风险（1 low、1 moderate、6 high、1 critical），并有 Vite 插件 peer dependency 与大体积 chunk 警告。本次未执行可能引入破坏性变更的自动升级，需另行评估并回归。

---

## 2026-07-28 全量上线（协议、试题库与错题本完善）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布协议与政策入口及同意版本记录、试题库独立题目与批次导入、题库练习进度和额度事务、错题本逐题解析与相关后台管理页面。

| 项目 | 值 |
|------|-----|
| 部署环境 | `test` |
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `d294907` |
| Commit 说明 | 试题库、错题本逻辑完善 |
| 上线范围 | 后端 + 数据库迁移 + 前端 |
| 备份清单 | `backup_before_deploy_20260728_205405.txt` |
| 应用迁移 | `20260727190000_add_user_legal_acceptance`、`20260727210000_atomic_question_bank_practice`、`20260728120000_standalone_question_library`、`20260728130000_add_question_import_batch_filename` |
| 部署结果 | 成功 |

处理记录：

- 测试机仓库无未提交改动，从 `aca346d` 快进到 `d294907`，未执行强制重置。
- 测试环境和预期 RDS 数据库双重门禁通过；迁移前完成 RDS MySQL、uploads 和运行配置备份，并生成备份清单与 SHA-256 校验信息。
- Prisma schema 与 migration guard 通过；测试环境未配置 `SHADOW_DATABASE_URL`，按既定门禁使用 `prisma validate + migrate deploy`，成功应用四条 migration，部署后 25 条 migration 均已应用。
- 运行配置完整性检查通过，共核对 52 个键；运行时校验确认测试模式、数据库连接和 SMTP 登录正常。
- 完成 Prisma Client 生成、后端 TypeScript 构建、运行目录同步与 PM2 重载；前端使用测试模式完成构建及静态资源同步。
- 部署过程未使用 `prisma db push`，未覆盖服务器运行配置。

验证结果：

- 公网首页、`GET /api/health` 和数据库依赖只读接口 `GET /api/payment/config` 均返回 `200 OK`，健康接口返回 `success: true`。
- PM2 中 `quiz-api` 状态为 `online`，端口 `80` 与 `3001` 正常监听；健康检查 Request ID 已在 PM2 实际运行日志中精确匹配。
- 服务器根分区使用率为 15%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计仍有 1 个 low 风险；前端依赖审计存在 9 个已知风险（1 low、1 moderate、6 high、1 critical），并有 Vite 插件 peer dependency 与大体积 chunk 警告。本次未执行可能引入破坏性变更的自动升级，需另行评估并回归。

---

## 2026-07-26 全量上线（诊断试卷访问级别与会员权益）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布诊断真题卷 `free / member` 访问级别、按考试类型判断会员权益、锁定卡片与历史报告入口、免费卷不限次重测、用户会员取消逻辑，以及旧付款状态字段移除。

| 项目 | 值 |
|------|-----|
| 部署环境 | `test` |
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `aca346d` |
| Commit 说明 | 增加免费试卷、会员试卷限制 |
| 上线范围 | 后端 + 数据库迁移 + 前端 |
| 备份清单 | `backup_before_deploy_20260726_154705.txt` |
| 应用迁移 | `20260726120000_add_paper_access_tier`、`20260726133000_remove_user_payment_status` |
| 部署结果 | 成功 |

处理记录：

- 测试机仓库无未提交改动，从上一版本快进到 `aca346d`，未执行强制重置。
- 测试环境和预期 RDS 数据库双重门禁通过；数据库变更前完成 RDS MySQL、uploads 和运行配置备份，并生成备份清单与校验信息。
- Prisma schema 与 migration guard 通过；测试环境未配置 `SHADOW_DATABASE_URL`，按既定门禁使用 `prisma validate + migrate deploy`，成功应用两条 migration，部署后 21 条 migration 均已应用。
- 运行配置完整性检查通过，共核对 52 个键；运行时校验确认测试模式、数据库连接和 SMTP 登录正常。
- 完成 Prisma Client 生成、后端 TypeScript 构建、运行目录同步与 PM2 重载；前端使用测试模式完成构建及静态资源同步。
- 部署过程未使用 `prisma db push`，未覆盖服务器运行配置。

验证结果：

- 公网首页、`GET /api/health` 和数据库依赖只读接口 `GET /api/papers/question-bank/summary?examType=ESAT` 均返回 `200 OK`，健康接口返回 `success: true`。
- PM2 中 `quiz-api` 状态为 `online`，端口 `80` 与 `3001` 正常监听。
- 健康检查响应中的 Request ID 已在 PM2 实际运行日志中精确匹配。
- 服务器根分区使用率为 15%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计仍有 1 个 low 风险；前端构建出现 Vite 开发工具 peer dependency 和大体积 chunk 警告，本次构建成功，后续可单独安排依赖与代码拆分优化。

---

## 2026-07-26 全量上线（历史诊断报告查询）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布诊断测试重复测试、历次诊断报告查询、报告时间展示及历史答题数据兼容逻辑。

| 项目 | 值 |
|------|-----|
| 部署环境 | `test` |
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `7b71c3a` |
| Commit 说明 | 增加历史诊断测试报告查询 |
| 上线范围 | 后端 + 数据库迁移 + 前端 |
| 备份清单 | `backup_before_deploy_20260726_000403.txt` |
| 应用迁移 | `20260725090000_diagnostic_report_history` |
| 部署结果 | 成功 |

处理记录：

- 部署前确认本地、远端分支均指向 `7b71c3a`，测试机仓库从上一版本快进更新，未执行强制重置。
- 环境类型与目标数据库门禁通过；部署脚本保留服务器运行配置，并在数据库变更前完成 RDS MySQL、uploads 和运行配置备份。
- 运行配置完整性检查通过；运行时校验确认测试模式、数据库连接和 SMTP 登录正常。
- Prisma migration guard 通过，成功应用 `20260725090000_diagnostic_report_history`，部署后共 19 条 migration，数据库结构处于最新状态。
- 完成 Prisma Client 生成、后端 TypeScript 构建、运行目录同步与 PM2 重载；完成前端测试模式构建及静态资源同步。
- 部署过程未使用 `prisma db push`，未覆盖服务器运行配置。

验证结果：

- 公网首页、`GET /api/health` 和数据库依赖只读接口 `GET /api/papers/question-bank/summary?examType=ESAT` 均返回 `200 OK`；健康接口返回 `success: true`。
- PM2 中 `quiz-api` 状态为 `online`，端口 `80` 与 `3001` 正常监听。
- 健康检查响应中的 Request ID 已在 PM2 实际运行日志中精确匹配。
- 服务器根分区使用率为 15%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计存在 1 个 low 风险；前端依赖审计存在 9 个已知风险（1 low、1 moderate、6 high、1 critical）。本次未执行可能引入破坏性变更的自动升级，需另行评估依赖升级并完成回归测试。

---

## 2026-07-25 全量上线（前端接口请求规范与全局错误处理）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布前端统一 API 请求层、标准 `ApiError`、登录会话失效处理、动态模块加载异常处理，以及全部旧页面错误读取方式迁移。

| 项目 | 值 |
|------|-----|
| 部署环境 | `test` |
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `34f8cd5` |
| Commit 说明 | 前端接口请求规范改造 |
| 上线范围 | 后端 + 数据库迁移检查 + 前端 |
| RDS 逻辑备份 | `/opt/quiz/backups/mysql/${QUIZ_TEST_DATABASE}_before_deploy_20260725_222151.sql.gz` |
| uploads 备份 | `/opt/quiz/backups/uploads/uploads_before_deploy_20260725_222151.tar.gz` |
| 运行配置备份 | `/opt/quiz/backups/config/config_before_deploy_20260725_222151.tar.gz` |
| 备份清单 | `/opt/quiz/backups/manifests/backup_before_deploy_20260725_222151.txt` |
| 应用迁移 | 无；18 条既有 migration 均已应用 |

处理记录：

- 本地工作区干净，目标分支已跟踪同名远端分支；测试机仓库最终确认位于 `34f8cd5`，未执行强制重置。
- 第一次远端执行在 Git 更新阶段因本地 PowerShell 将 Git 的标准错误流进度信息误判为异常而中断。随后只读确认远端无残留部署进程、仓库干净且原 PM2 服务在线，再从环境门禁开始完整重试。
- 测试环境和预期 RDS 数据库双重门禁通过；部署脚本未修改或覆盖服务器运行配置。
- Prisma schema 与 migration guard 通过；测试环境未配置 `SHADOW_DATABASE_URL`，按既定门禁使用 `prisma validate + migrate deploy`，确认无待应用迁移。
- 数据库操作前完成 RDS MySQL、uploads 和运行配置备份，RDS 压缩逻辑备份约 1.6 MiB，并生成 SHA-256 校验信息和备份清单。
- 运行配置键检查通过，共核对 52 个键；`API_ENV_FILE=/opt/quiz/api/.env npm run validate:runtime` 验证测试运行模式、RDS 连接和 SMTP 登录均正常。
- 完成 Prisma Client 生成、后端 TypeScript 构建、运行目录同步与 PM2 重载；前端使用 `build-only:test` 完成 Vite 测试构建并同步静态资源。
- 部署过程中未使用 `prisma db push`，未依赖旧 `/opt/quiz/data/prod.db`。

验证结果：

- `http://127.0.0.1:3001/api/health` 与 `http://127.0.0.1/api/health` 均正常。
- `${QUIZ_TEST_PUBLIC_URL}/`、`${QUIZ_TEST_PUBLIC_URL}/api/health` 和数据库依赖只读接口 `GET /api/papers/syllabus?examType=ESAT` 均返回 `200 OK`。
- 健康检查 Request ID 已在 PM2 实际运行日志中精确匹配。
- PM2 中 `quiz-api` 状态为 `online`，端口 `80` 与 `3001` 正常监听。
- Prisma 显示 18 条 migration 均已应用，RDS MySQL schema 为最新状态。
- 服务器根分区使用率为 15%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计存在 1 个 low 风险；前端依赖审计存在 9 个已知风险（1 low、1 moderate、6 high、1 critical）。本次未执行可能引入破坏性变更的自动升级，需另行评估并回归。

---

## 2026-07-25 全量上线（诊断测试计时、返回拦截与文档改版）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布 ESAT/TMUA 分段诊断的暂停恢复、到时锁定与竞态保护、诊断首页和报告展示调整，以及页面返回和浏览器历史返回共用“确认返回”弹窗等改进。

| 项目 | 值 |
|------|-----|
| 部署环境 | `test` |
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `f9fb7f5` |
| Commit 说明 | 诊断测试bug修复，文档改版 |
| 上线范围 | 后端 + 数据库迁移检查 + 前端 |
| RDS 逻辑备份 | `/opt/quiz/backups/mysql/${QUIZ_TEST_DATABASE}_before_deploy_20260725_185609.sql.gz` |
| uploads 备份 | `/opt/quiz/backups/uploads/uploads_before_deploy_20260725_185609.tar.gz` |
| 运行配置备份 | `/opt/quiz/backups/config/config_before_deploy_20260725_185609.tar.gz` |
| 备份清单 | `/opt/quiz/backups/manifests/backup_before_deploy_20260725_185609.txt` |
| 应用迁移 | 无；18 条既有 migration 均已应用 |

处理记录：

- 本地与 Git 远端分支均确认指向 `f9fb7f5`；测试机仓库无未提交改动，并从上一版本快进到目标提交，未执行强制重置。
- 测试环境和预期 RDS 数据库双重门禁通过；部署脚本未修改或覆盖服务器运行配置。
- Prisma schema 与 migration guard 通过；测试环境未配置 `SHADOW_DATABASE_URL`，按既定门禁使用 `prisma validate + migrate deploy`，确认无待应用迁移。
- 数据库操作前完成 RDS MySQL、uploads 和运行配置备份，RDS 压缩逻辑备份约 1.6 MiB，并生成 SHA-256 校验信息和备份清单。
- 运行配置键检查通过，共核对 52 个键；`API_ENV_FILE=/opt/quiz/api/.env npm run validate:runtime` 验证测试运行模式、RDS 连接和 SMTP 登录均正常。
- 完成 Prisma Client 生成、后端 TypeScript 构建、运行目录同步与 PM2 重载；前端使用 `build-only:test` 完成 Vite 测试构建并同步静态资源。
- 部署过程中未使用 `prisma db push`，未依赖旧 `/opt/quiz/data/prod.db`。

验证结果：

- `http://127.0.0.1:3001/api/health` 与 `http://127.0.0.1/api/health` 均正常。
- `${QUIZ_TEST_PUBLIC_URL}/`、`${QUIZ_TEST_PUBLIC_URL}/api/health` 和数据库依赖只读接口 `GET /api/papers/question-bank/summary?examType=ESAT` 均返回 `200 OK`；只读接口返回 ESAT 共 40 题。
- 健康检查 Request ID 已在 PM2 实际运行日志中精确匹配。
- PM2 中 `quiz-api` 状态为 `online`，端口 `80` 与 `3001` 正常监听。
- Prisma 显示 18 条 migration 均已应用，RDS MySQL schema 为最新状态。
- 服务器根分区使用率为 15%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计存在 1 个 low 风险；前端依赖审计存在 9 个已知风险（1 low、1 moderate、6 high、1 critical）。本次未执行可能引入破坏性变更的自动升级，需另行评估并回归。

---

## 2026-07-24 全量上线（部署 Skill 与安全配置完善）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布部署配置脱敏、测试与生产环境隔离、部署前运行配置校验及报告安全存储等改进。

| 项目 | 值 |
|------|-----|
| 部署环境 | `test` |
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `e5681bd` |
| Commit 说明 | 完善部署skill |
| 上线范围 | 后端 + 数据库迁移检查 + 前端 |
| RDS 逻辑备份 | `/opt/quiz/backups/mysql/${QUIZ_TEST_DATABASE}_before_deploy_20260724_154450.sql.gz` |
| uploads 备份 | `/opt/quiz/backups/uploads/uploads_before_deploy_20260724_154450.tar.gz` |
| 运行配置备份 | `/opt/quiz/backups/config/config_before_deploy_20260724_154450.tar.gz` |
| 备份清单 | `/opt/quiz/backups/manifests/backup_before_deploy_20260724_154450.txt` |
| 应用迁移 | 无；18 条既有 migration 均已应用 |

处理记录：

- 测试机仓库无未提交改动，目标远端分支存在并更新到 `e5681bd`；未执行强制重置。
- 本地首次采集 SSH 输出时，PowerShell 将 Git 写入标准错误流的正常提示误判为异常；确认远端未残留部署进程、仓库干净且 PM2 原服务在线后，从构建和迁移前的安全状态重新执行部署。
- Prisma schema 与 migration guard 通过；测试环境未配置 `SHADOW_DATABASE_URL`，按既定门禁使用 `prisma validate + migrate deploy`，确认无待应用迁移。
- 数据库操作前完成 RDS MySQL、uploads 和运行配置备份，RDS 压缩逻辑备份约 1.6 MiB，并生成 SHA-256 校验信息和备份清单。
- 运行配置键检查通过，共核对 52 个键；`API_ENV_FILE=/opt/quiz/api/.env npm run validate:runtime` 验证测试运行模式、RDS 连接和 SMTP 登录均正常。
- 完成 Prisma Client 生成、后端 TypeScript 构建、运行目录同步与 PM2 重载；前端使用 `build-only:test` 完成 Vite 测试构建并同步静态资源。
- 部署过程中未使用 `prisma db push`，未覆盖 `/opt/quiz/api/.env`，未依赖旧 `/opt/quiz/data/prod.db`。

验证结果：

- `http://127.0.0.1:3001/api/health` 与 `http://127.0.0.1/api/health` 均正常。
- `${QUIZ_TEST_PUBLIC_URL}/`、`${QUIZ_TEST_PUBLIC_URL}/api/health` 和数据库依赖只读接口 `GET /api/papers/question-bank/summary?examType=ESAT` 均返回 `200 OK`；只读接口返回 ESAT 共 40 题。
- 健康检查 Request ID `5d2a85f1-d4ce-4379-95e7-662b8b185829` 已在 `/var/log/quiz/api-out.log` 中精确匹配。
- PM2 中 `quiz-api` 状态为 `online`，端口 `80` 与 `3001` 正常监听。
- Prisma 显示 18 条 migration 均已应用，RDS MySQL schema 为最新状态。
- 服务器根分区使用率为 15%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计存在 1 个 low 风险；前端依赖审计存在 9 个已知风险（1 low、1 moderate、6 high、1 critical）。本次未执行可能引入破坏性变更的自动升级，需另行评估并回归。

---

## 2026-07-22 全量上线（TMUA 分卷诊断与试卷管理）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布 TMUA Paper 1/2 标准试卷上传、真题库展示、分卷在线作答流程，以及真题库和试题库的真实删除操作；同时停用会误删数学比较符号的上传文本安全清洗，结构与业务校验保持启用。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `74d0e2f` |
| Commit 说明 | 增加tmua试卷上传 |
| 上线范围 | 后端 + 数据库迁移检查 + 前端 |
| RDS 逻辑备份 | `/opt/quiz/backups/mysql/${QUIZ_TEST_DATABASE}_before_deploy_20260722_213026.sql.gz` |
| uploads 备份 | `/opt/quiz/backups/uploads/uploads_before_deploy_20260722_213026.tar.gz` |
| 运行配置备份 | `/opt/quiz/backups/config/config_before_deploy_20260722_213026.tar.gz` |
| 备份清单 | `/opt/quiz/backups/manifests/backup_before_deploy_20260722_213026.txt` |
| 应用迁移 | 无；18 条既有 migration 均已应用 |

处理记录：

- 测试机仓库无未提交改动，从 `551b234` 快进到 `74d0e2f`；未执行强制重置。
- Prisma schema 校验与 migration guard 通过；测试环境未配置 `SHADOW_DATABASE_URL`，按既定门禁使用 `prisma validate + migrate deploy`，确认无待应用迁移。
- 数据库操作前完成 RDS MySQL、uploads 和运行配置备份，RDS 压缩逻辑备份为 782 KiB，并生成 SHA-256 校验信息和备份清单。
- 运行配置键检查通过，共核对 52 个键；`API_ENV_FILE=/opt/quiz/api/.env npm run validate:runtime` 验证测试运行模式、RDS 连接和 SMTP 登录均正常。
- 完成 Prisma Client 生成、后端 TypeScript 构建、运行目录同步与 PM2 重载；完成前端依赖安装、Vite 生产构建和静态资源同步。
- 部署过程中未使用 `prisma db push`，未覆盖 `/opt/quiz/api/.env`，未依赖旧 `/opt/quiz/data/prod.db`。

验证结果：

- `http://127.0.0.1:3001/api/health` 与 `http://127.0.0.1/api/health` 均正常。
- `${QUIZ_TEST_PUBLIC_URL}/`、`${QUIZ_TEST_PUBLIC_URL}/api/health` 和数据库依赖只读接口 `GET /api/papers/question-bank/summary?examType=TMUA` 均返回 `200 OK`。
- 健康检查 Request ID `68f50499-4248-4581-92e8-ecf591fd5268` 已在 `/var/log/quiz/api-out.log` 中精确匹配。
- PM2 中 `quiz-api` 状态为 `online`，端口 `80` 与 `3001` 正常监听。
- Prisma 显示 18 条 migration 均已应用，RDS MySQL schema 为最新状态。
- 服务器根分区使用率为 15%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计存在 1 个 low 风险；前端依赖审计存在 8 个已知风险（1 low、1 moderate、5 high、1 critical）。本次未执行可能引入破坏性变更的自动升级，需另行评估并回归。

---

## 2026-07-21 全量上线（ESAT 三模块诊断测试）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布 ESAT 历年真题等效组合卷导入、三模块顺序作答、科目间三分钟可跳过休息、诊断重测正式流程，以及真题库和诊断列表的模块展示。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `551b234` |
| Commit 说明 | 诊断测试全链路改版 |
| 上线范围 | 后端 + 数据库迁移 + 前端 |
| RDS 逻辑备份 | `/opt/quiz/backups/mysql/${QUIZ_TEST_DATABASE}_before_deploy_20260721_192548.sql.gz` |
| uploads 备份 | `/opt/quiz/backups/uploads/uploads_before_deploy_20260721_192548.tar.gz` |
| 运行配置备份 | `/opt/quiz/backups/config/config_before_deploy_20260721_192548.tar.gz` |
| 备份清单 | `/opt/quiz/backups/manifests/backup_before_deploy_20260721_192548.txt` |
| 应用迁移 | `20260719011000_modular_diagnostic_exams`、`20260719013000_active_diagnostic_attempt` |

处理记录：

- 部署前本地后端编译、模块诊断回归测试、Prisma schema 校验、迁移状态检查和前端生产构建均通过。
- 测试机仓库无未提交改动，从 `1def7aa` 快进到 `551b234`；未执行强制重置。
- 在迁移前完成 RDS MySQL、uploads 和运行配置备份，生成 SHA-256 校验信息和备份清单。
- 运行配置键检查通过，共核对 52 个键；`API_ENV_FILE=/opt/quiz/api/.env npm run validate:runtime` 验证测试运行模式、数据库连接和 SMTP 登录均正常。
- Prisma migration guard 通过；测试环境未配置 `SHADOW_DATABASE_URL`，按既定规则使用 `prisma validate + migrate deploy` 作为迁移门禁，两条新迁移均成功应用。
- 完成 Prisma Client 生成、后端 TypeScript 构建与 PM2 重载，以及前端 Vite 构建和静态资源同步。
- 部署过程中未使用 `prisma db push`，未覆盖 `/opt/quiz/api/.env`，未依赖旧 `/opt/quiz/data/prod.db`。

验证结果：

- `http://127.0.0.1:3001/api/health` 与 `http://127.0.0.1/api/health` 均正常。
- `${QUIZ_TEST_PUBLIC_URL}/`、`${QUIZ_TEST_PUBLIC_URL}/api/health` 和数据库依赖只读接口 `GET /api/payment/config` 均返回 `200 OK`。
- 健康检查 Request ID `ac307e0b-6c88-42b5-af75-a8ba171c4d12` 已在 `/var/log/quiz/api-out.log` 中精确匹配。
- PM2 中 `quiz-api` 状态为 `online`，端口 `80` 与 `3001` 正常监听。
- Prisma 显示 18 条 migration 均已应用，RDS MySQL schema 为最新状态。
- 服务器根分区使用率为 14%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计存在 1 个 low 风险；前端依赖审计存在 7 个已知风险（1 low、1 moderate、4 high、1 critical）。本次未执行可能引入破坏性变更的自动升级，需另行评估并回归。

---

## 2026-07-18 全量上线（学生行为分析与营收成本）

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布操作日志后续完善、学生用户行为分析，以及营收成本的分类与成本项管理；同时将本地营收成本数据合并到测试环境。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `1def7aa` |
| Commit 说明 | 更新营收成本 |
| 上线范围 | 后端 + 数据库迁移 + 前端 + 营收成本数据同步 |
| RDS 逻辑备份 | `/opt/quiz/backups/mysql/${QUIZ_TEST_DATABASE}_before_deploy_20260718_181605.sql.gz` |
| uploads 备份 | `/opt/quiz/backups/uploads/uploads_before_deploy_20260718_181605.tar.gz` |
| 运行配置备份 | `/opt/quiz/backups/config/config_before_deploy_20260718_181605.tar.gz` |
| 备份清单 | `/opt/quiz/backups/manifests/backup_before_deploy_20260718_181605.txt` |
| 应用迁移 | `20260718170000_add_revenue_cost_category` |

处理记录：

- 部署前完成 Prisma schema/migration guard、RDS MySQL、uploads 与运行配置备份，并生成 SHA-256 校验清单。
- `API_ENV_FILE=/opt/quiz/api/.env npm run validate:runtime` 验证通过，测试运行模式、数据库连接和 SMTP 登录正常。
- 成功应用营收成本分类迁移，完成 Prisma Client 生成、后端 TypeScript 构建、PM2 重载、前端生产构建和静态资源同步。
- 本地 `RevenueCost` 共 17 条记录按 ID 合并到测试库，17 条全部匹配；采用新增或更新方式，不删除测试环境独有记录。同步后测试库共 17 条。
- 同步后的研发工具成本包括 deepseek 4 条、claude 3 条、codex 5 条；技术基础设施成本包括数据库租赁 2 条、域名与证书 1 条、服务器租赁 2 条。

验证结果：

- `http://127.0.0.1:3001/api/health` 与 `http://127.0.0.1/api/health` 均正常。
- `${QUIZ_TEST_PUBLIC_URL}/` 与 `${QUIZ_TEST_PUBLIC_URL}/api/health` 均返回 `200 OK`。
- 数据库依赖接口 `GET /api/payment/config` 返回 `200`，且响应 `success: true`。
- Request ID `ea2150c7-0e2b-49b1-bb2b-21af682cc3b7` 已在 `/var/log/quiz/api-out.log` 中精确定位。
- PM2 中 `quiz-api` 状态为 `online`，监听 `80` 与 `3001` 端口。
- Prisma 显示 16 条 migration 均已应用，RDS MySQL 结构为最新状态。
- 服务器根分区使用率为 14%，可用内存约 2.8 GiB，部署后运行状态正常。
- 后端依赖审计为 0 个漏洞；前端依赖审计仍有 6 个已知漏洞（1 low、2 moderate、2 high、1 critical），未在本次部署中执行破坏性自动升级，需另行评估升级与回归。

---

## 2026-07-17 全量上线（操作审计与 Request ID 链路）

本次使用改进后的 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布操作审计日志、角色筛选与详情、IP 规范化，以及响应头、运行日志和操作日志共用 Request ID 的排查链路。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `59bab06` |
| Commit 说明 | 增加后端运维日志 |
| 上线范围 | 后端 + 数据库迁移 + 前端 |
| RDS 逻辑备份 | `/opt/quiz/backups/mysql/${QUIZ_TEST_DATABASE}_before_deploy_20260717_122920.sql.gz` |
| 备份清单 | `/opt/quiz/backups/manifests/backup_before_deploy_20260717_122920.txt` |
| 应用迁移 | `20260717120000_add_operation_logs`、`20260717153000_add_operation_log_request_id` |

处理记录：

- 服务器仓库存在上次部署留下的未提交文件；逐文件与目标提交及其历史 Blob 比对后，确认不存在服务器独有代码，并保存为可恢复的 `quiz-deploy-pre-20260717-122352-known-git-history` stash，未使用 `reset --hard`。
- 部署前执行 Prisma schema/migration guard，随后完成 RDS MySQL、uploads 和运行配置备份并生成 SHA-256 清单。
- 首次配置演进检查发现运行环境缺少 10 个非密钥默认配置，部署在迁移前安全中止；Skill 增加安全默认值白名单后再次备份，只追加缺失键且不覆盖现有值。
- `API_ENV_FILE=/opt/quiz/api/.env npm run validate:runtime` 验证通过，确认测试运行模式、RDS 连通性和 SMTP 登录正常。
- 成功应用操作审计表及 Request ID 两条迁移，完成 Prisma Client 生成、后端构建、PM2 重载、前端生产构建和静态资源同步。
- Skill 新增 PM2 日志路径动态发现与 Request ID 自检，不再假定日志位于 `~/.pm2/logs`；当前实际日志路径为 `/var/log/quiz/api-out.log` 和 `/var/log/quiz/api-error.log`。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/` 返回 `200 OK`。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 返回 `200` 和 `X-Request-ID`。
- Request ID 已在 `/var/log/quiz/api-out.log` 的 `http.request.completed` JSON 日志中精确定位。
- 数据库依赖接口 `GET /api/payment/config` 返回 `200`。
- PM2 中 `quiz-api` 状态为 `online`，监听 `80` 和 `3001` 端口。
- Prisma 显示 15 条 migration 均已应用，RDS MySQL 结构为最新状态。
- 前端依赖审计报告 6 个已知漏洞（1 low、2 moderate、2 high、1 critical），未在本次部署中执行破坏性自动升级，需另行评估升级与回归。

---

## 2026-07-14 全量上线

本次按 `all` 范围部署当前阶段功能，包含认证与会话、诊断报告、会员权益、支付联调、个人中心、试题库与错题本样式调整。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `8610479` |
| Commit 说明 | 支付方案联调 |
| 上线范围 | 后端 + 前端 |
| RDS 逻辑备份 | `/opt/quiz/backups/mysql/${QUIZ_TEST_DATABASE}_before_deploy_20260714_193919.sql.gz` |
| 备份清单 | `/opt/quiz/backups/manifests/backup_before_deploy_20260714_193919.txt` |

处理记录：

- 部署前执行 Prisma schema 与 migration guard，检查通过后再进行备份和迁移。
- 完成 RDS MySQL、uploads 和运行配置备份，并生成 SHA-256 校验清单。
- 成功应用诊断报告流水线、答题状态、考试截止时间、认证会话、支付订单与通知、免费权益额度等 7 条 migration。
- 后端完成 TypeScript 构建、运行目录同步和 PM2 重载；前端完成 Vite 构建并同步至 `/opt/quiz/web/dist`。
- 首次执行因 Windows 上传脚本缺少 Linux 可执行位，在备份和迁移前被安全检查中止；补齐临时脚本权限后重新执行成功。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/` 返回 `200 OK`。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 正常。
- PM2 中 `quiz-api` 状态为 `online`。
- Prisma 显示测试库 10 条 migration 均已应用，数据库结构为最新状态。

---

## 2026-07-04 全量上线

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布试卷类型标准化、用户管理样式与分页、试题库/真题库管理、登录用户名统一等功能。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `6d7257a` |
| Commit 说明 | 登录时填写邮箱替换成用户名 |
| 上线范围 | 后端 + 前端 |
| 数据库备份 | `/opt/quiz/backups/prod_before_deploy_20260704_174524.db` |
| 应用迁移 | `20260704000000_paper_type_source_values`、`20260704001000_paper_type_paper_values`、`20260704171500_rename_user_name_to_username` |

处理记录：

- 部署前执行 Prisma migration guard，确认 `schema.prisma` 与 migrations 已对齐。
- 执行 `npx prisma migrate deploy`，成功应用 3 条待执行 migration。
- `User.name` 已迁移为 `User.username`，生产登录凭据统一为用户名 + 密码。
- 后端完成 TypeScript 构建、运行目录同步并重载 PM2；前端完成 Vite 构建并同步到 `/opt/quiz/web/dist`。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/` 返回 `200 OK`。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 正常。
- PM2 中 `quiz-api` 状态为 `online`。
- Prisma 显示生产库结构已是最新。

---

## 2026-06-30 全量上线

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线，发布考纲管理相关功能。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260626-feature-development` |
| 上线 Commit | `2e0c8c1` |
| Commit 说明 | Add user exam preferences migration |
| 上线范围 | 后端 + 前端 |
| 数据库备份 | `/opt/quiz/backups/prod_before_deploy_20260630_161331.db` |
| 应用迁移 | `20260622000001_add_membership_entitlements`、`20260628000000_add_syllabus_library`、`20260630000000_add_user_exam_preferences` |

处理记录：

- 首次部署被 Prisma migration guard 拦截，原因是 `User.examPreferences` 字段缺少已提交 migration。
- 补充 migration `20260630000000_add_user_exam_preferences` 后推送到部署分支，并重新执行全量上线。
- 部署前执行 Prisma migration guard，确认 `schema.prisma` 与 migrations 已对齐。
- 执行 `npx prisma migrate deploy`，成功应用 3 条待执行 migration。
- 后端完成 TypeScript 构建、运行目录同步并重载 PM2；前端完成 Vite 构建并同步到 `/opt/quiz/web/dist`。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/` 返回 `200 OK`。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 正常。
- PM2 中 `quiz-api` 状态为 `online`。
- Prisma 显示生产库结构已是最新。

---

## 2026-06-22 后端修复上线

本次使用 `quiztestdemo-deploy` Skill 执行 `backend` 范围上线，用于修复线上 `Question.syllabusPoints` 字段缺失导致的接口 500 问题。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260519-feature-projectDeployment` |
| 上线 Commit | `5df4937` |
| Commit 说明 | 新增考纲树 migration |
| 上线范围 | 后端 |
| 数据库备份 | `/opt/quiz/backups/prod_before_deploy_20260622_134247.db` |
| 应用迁移 | `20260622000000_add_question_syllabus_points` |

处理记录：

- 部署前执行 Prisma migration guard，确认 `schema.prisma` 与 migrations 已对齐。
- 执行 `npx prisma migrate deploy`，成功为 `Question` 表增加 `syllabusPoints` 字段。
- 后端完成 TypeScript 构建、运行目录同步并重载 PM2。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/api/papers/question-bank/summary?code=110000` 返回 `200 OK`，不再出现 `Question.syllabusPoints` 缺失错误。
- PM2 中 `quiz-api` 状态为 `online`。
- Prisma 显示生产库结构已是最新。

---

## 2026-06-22 全量上线

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260519-feature-projectDeployment` |
| 上线 Commit | `a543ea2` |
| Commit 说明 | 错题本增加分页功能 |
| 上线范围 | 后端 + 前端 |
| 数据库备份 | `/opt/quiz/backups/prod_before_deploy_20260622_105550.db`、`/opt/quiz/backups/prod_before_deploy_20260622_105949.db` |

处理记录：

- 首次执行 `npx prisma migrate deploy` 时，发现线上库中已存在 `SyllabusNode` 表，但 migration `20260620000000_add_syllabus_node` 未被 Prisma 标记为完成。
- 经检查，线上 `SyllabusNode` 表和索引与 migration 内容一致，因此执行 `npx prisma migrate resolve --applied 20260620000000_add_syllabus_node` 修复迁移状态。
- 继续执行 `npx prisma migrate deploy`，成功应用 `20260620000001_add_question_table`、`20260620000002_add_answer_fk`。
- 后端完成 TypeScript 构建并重载 PM2；前端完成 Vite 构建并同步到 `/opt/quiz/web/dist`。

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/` 返回 `200 OK`。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 正常。
- PM2 中 `quiz-api` 状态为 `online`。
- Prisma 显示生产库结构已是最新。

---

## 2026-06-16 增量上线

本次使用 `quiztestdemo-deploy` Skill 执行 `all` 范围上线。

| 项目 | 值 |
|------|-----|
| 部署分支 | `20260519-feature-projectDeployment` |
| 上线 Commit | `bb7b269` |
| Commit 说明 | 修改试题syllabus字段和考纲对应关系 |
| 上线范围 | 后端 + 前端 |
| 数据库迁移 | `npx prisma migrate deploy`，无待执行 migration |
| 生产库备份 | `/opt/quiz/backups/prod_before_deploy_20260616_120504.db` |

验证结果：

- `http://127.0.0.1:3001/api/health` 正常。
- `http://127.0.0.1/api/health` 正常。
- `${QUIZ_TEST_PUBLIC_URL}/` 返回 `200 OK`。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 正常。
- PM2 中 `quiz-api` 状态为 `online`。

---

## 2026-06-15 大纲数据迁移

试题库页面依赖 `SyllabusNode` 大纲树数据。本次没有做全量数据库迁移，只迁移本地开发库中的大纲表。

处理过程：

- 线上生产库缺少 `SyllabusNode` 表，先执行 `npx prisma db push` 将 SQLite 结构同步到当前 Prisma schema。
- 从本地 `api/prisma/dev.db` 导出 `SyllabusNode` 表 98 条记录。
- 上传到服务器 `/opt/quiz/api/syllabus-import.json`。
- 在服务器通过 Prisma 按 `examType + code` 执行 upsert。
- 导入完成后删除临时 JSON 和临时导入脚本。

迁移结果：

| examType | 节点数 |
|----------|------:|
| ESAT | 98 |

根节点：`100000` — ESAT (工程与科学入学测试)

备份文件：

- 导入前备份：`/opt/quiz/backups/prod_before_syllabus_import_20260615.db`
- 导入后备份：`/opt/quiz/backups/prod_after_syllabus_import_20260615.db`

---

## 2026-06-15 用户数据迁移

本次没有做全量数据库迁移，只迁移本地开发库中的 `User` 表数据，目的是让线上具备可登录的管理员账号。

迁移方式：

- 从本地 `api/prisma/dev.db` 导出 `User` 表 4 条记录。
- 上传到服务器 `/opt/quiz/api/users-import.json`。
- 在服务器通过 Prisma 按 `email` 执行 upsert。
- 导入完成后删除临时 JSON 和临时导入脚本。

迁移结果：

| 角色 | 数量 |
|------|-----:|
| admin | 2 |
| student | 3 |

备份文件：

- 导入前备份：`/opt/quiz/backups/prod_before_user_import_.db`
- 导入后备份：`/opt/quiz/backups/prod_after_user_import_20260615.db`

---

## 2026-06-15 首次部署

本次已按最小可行上线方案完成公网 IP 部署。

| 项目 | 值 |
|------|-----|
| 公网 IP | `${QUIZ_TEST_SSH_HOST}` |
| 登录用户 | `deploy` |
| 部署分支 | `20260519-feature-projectDeployment` |
| Git 仓库 | `https://github.com/Shiyidan/OxBridgeQuiz.git` |
| 后端运行目录 | `/opt/quiz/api` |
| 前端静态目录 | `/opt/quiz/web/dist` |
| SQLite 生产库 | `/opt/quiz/data/prod.db` |
| PM2 进程 | `quiz-api` |
| Nginx 配置 | `/etc/nginx/sites-available/quiz` |

已完成：

- 安装 Node.js `v20.20.2`、npm `10.8.2`、Nginx `1.18.0`、PM2 `7.0.1`。
- 克隆 GitHub 仓库并切换到部署分支。
- 写入 `/opt/quiz/api/.env`，生成生产 `JWT_SECRET`。
- 执行 Prisma migration，创建 SQLite 生产库。
- 完成后端 TypeScript 构建和前端 Vite 构建。
- 使用 PM2 启动 API，并配置 `pm2-deploy` systemd 开机自启。
- 配置 Nginx：`/` 指向前端 SPA，`/api/` 反向代理到 `127.0.0.1:3001`。
- 启用 UFW，仅放行 `22`、`80`、`443`。

验证结果：

- `${QUIZ_TEST_PUBLIC_URL}/` 返回 `200 OK`。
- `${QUIZ_TEST_PUBLIC_URL}/api/health` 返回 `{ "status": "ok" }`。
- PM2 中 `quiz-api` 状态为 `online`。

待补充：

- 当前 `DASHSCOPE_API_KEY` 为空，AI 试卷解析能力暂不可用。
- 当前仍使用公网 IP + HTTP，正式对外使用前需完成域名、备案和 HTTPS。

### 2026-06-15 部署前核对

本次按当前代码仓库重新核对部署方案，结论如下：

- 当前后端真实目录是 `api/`，前端真实目录是 `quiz-web/`。
- 当前 Prisma 使用 SQLite，生产环境先用 `/opt/quiz/data/prod.db`。
- 当前没有 Redis 依赖和调用。
- 前端生产构建必须设置 `VITE_API_BASE`。
- 安全组不应开放 `3001`、`5432`、`6379`，API 统一通过 Nginx 反向代理访问。
- 备案和 HTTPS 完成前，可以先用公网 IP 做内部测试。

---

## 2026-05-22 环境初始化

### ECS 选型决策

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 实例规格 | 经济型 e（ecs.e-c1m2.large）2核4G | PDF 解析间歇突发、日常低负载 |
| 操作系统 | Ubuntu 22.04 LTS | Node.js/Nginx 生态最成熟 |
| 系统盘 | ESSD Entry 40G | 代码 + SQLite + 临时文件约 20G |
| 带宽 | 100Mbps 按流量计费 | 开发测试阶段突发快、空闲不花钱 |
| 预装应用 | 无（裸系统） | 避免面板/Docker 覆盖配置 |

### 已购实例信息

| 项目 | 值 |
|------|-----|
| 公网 IP | `${LEGACY_SSH_HOST}` |
| 私网 IP | `172.26.189.52` |
| 操作系统 | Ubuntu 22.04 64位 |
| 实例规格 | ecs.e-c1m2.large（2核4G） |
| 系统盘 | ESSD Entry 40G |
| 带宽 | 100Mbps 按流量 |
| 付费方式 | 按量付费 |

### 安全组配置

最终入方向规则（仅两条）：

| 方向 | 端口 | 来源 | 说明 |
|------|:---:|------|------|
| 入方向 | `22` | 0.0.0.0/0 | SSH 远程管理 |
| 入方向 | `80` | 0.0.0.0/0 | HTTP 对外访问 |

已删除的规则：3001（API 直连）、5432（PostgreSQL）、6379（Redis）、3389（Windows RDP）。

### SSH 登录操作

```bash
ssh root@${LEGACY_SSH_HOST}
passwd
adduser deploy
usermod -aG sudo deploy
```

### 下一步待执行

- [x] `sudo apt update && sudo apt upgrade -y` — 系统更新
- [x] 安装 Node.js 20 + PM2
- [x] 安装 Nginx
- [x] 克隆代码 + 构建 + 部署
- [x] 配置 Nginx → 启动服务
