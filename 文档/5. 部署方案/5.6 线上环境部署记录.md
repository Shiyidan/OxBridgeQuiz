# 线上环境部署记录

> **适用范围**：正式环境的公网验证、部署执行、运维变更与验收记录  
> **最后更新**：2026-08-08

本文档只记录已经执行的线上操作和验证结果。线上资源配置与待办事项参见 `5.5 线上环境部署方案.md`，详细备份规则参见 `5.7 备份策略.md`。

## 一、域名与 HTTPS 公网验证记录

### 1.1 2026-07-24 首次公共 DNS 生效验证

本次仅执行公网只读验证和文档更新，未连接生产服务器，未部署代码、修改运行配置或操作数据库。

验证结果：

- `acemock.cn` 根域名 A 记录已生效并解析到正式 ECS；`www.acemock.cn` 的 CNAME 已指向根域名，DNS TTL 为 600 秒。
- `http://acemock.cn/` 使用 `301` 跳转到 `https://acemock.cn/`，规范 HTTPS 首页返回 `200 OK`。
- `http://www.acemock.cn/` 使用 `301` 跳转到 `https://www.acemock.cn/`；当时 `https://www.acemock.cn/` 直接返回 `200 OK`，尚未继续永久重定向到规范主域名。
- 根域名和 `www` 的 HTTPS 请求均通过客户端证书校验，未出现证书名称或证书链错误。
- `${QUIZ_PROD_PUBLIC_URL}/api/health` 返回 `200 OK`，响应为标准健康状态。
- 数据库依赖只读接口 `GET /api/papers/question-bank/summary?examType=ESAT` 返回 `200 OK`；当时生产数据汇总为 0 题，后续仍需按生产数据迁移计划处理。
- API 响应已包含 CSP、`X-Content-Type-Options: nosniff` 和 `X-Frame-Options: DENY`。
- 当时本地前端源码已包含 ICP 备案号及工信部链接，但线上构建资源中尚未检出二者。

首次验证后的待办：

- 将 `www` 的 HTTP 与 HTTPS 请求统一使用 `301` 重定向到 `https://acemock.cn`，并保留原路径和查询参数。
- 发布并在真实浏览器中确认备案号可见、工信部链接可点击。
- 在真实浏览器中继续验证登录、Cookie、路由刷新及用户业务流程。

后续生产全量部署已经完成 `www` 规范域名重定向和 ICP 信息发布，结果见本文第二部分。

## 二、生产环境部署记录

### 2.1 2026-07-24 生产环境全量部署

本次使用 `quiztestdemo-deploy` 流程部署生产环境，部署范围为前后端全部，分支为 `20260626-feature-development`，部署提交为 `e5681bdffbfc7803111df94f99d688c9e6e6c6e6`。

处理记录：

- 部署前发现生产运行配置缺少 `API_RUNTIME_ENV`，已在备份原配置后补充为 `prod`。
- 生产服务器原 `/opt/quiz/repo` 不是 Git 工作树；由于服务器访问 GitHub 时出现 TLS 重置和超时，本次通过本地校验后的 Git bundle 初始化为 Git 仓库。旧源码已归档保留，部署结束后仓库 `origin` 已恢复为 GitHub 地址。
- 根据本次明确授权，生产环境暂时复用测试 SMTP 账号，但使用独立生成的生产 `EMAIL_CODE_SECRET`；支付、支付生命周期任务与支付对账任务均保持关闭。未将任何凭据写入仓库或部署文档。
- 生产配置通过必填项检查，运行时验证确认 `API_RUNTIME_ENV=prod`、前端规范地址正确、`${QUIZ_PROD_DATABASE}` 可连接且 SMTP 登录成功。
- 部署前备份清单为 `/opt/quiz/backups/manifests/backup_before_deploy_20260724_164117.txt`，包含生产 MySQL、上传文件和运行配置备份。
- Prisma 共应用 15 个待执行迁移，随后确认生产数据库结构已为最新状态。
- 后端 TypeScript 构建、Prisma Client 生成、PM2 重载和前端生产构建均成功。前端同步首次因目标目录属主导致元数据设置失败，调整为不覆盖目标属主、权限和目录时间后重新同步成功；部署脚本已同步修正。
- Nginx 实际配置文件为 `/etc/nginx/sites-available/quiztestdemo`。在获得明确授权后，使用现有生产 SSH 密钥完成 root 登录，执行带配置备份、`nginx -t` 检查和失败自动回滚的跳转脚本；语法检查和 Nginx 重载均成功。

验证结果：

- `/opt/quiz/repo` 位于目标分支和提交，生产运行环境与目标数据库匹配。
- PM2 进程 `quiz-api` 为 `online`，本机 API 与本机 Nginx 健康检查均成功。
- `https://acemock.cn/`、`https://acemock.cn/api/health` 和数据库依赖只读接口均返回 `200 OK`。
- Request ID 已能关联到 PM2 运行日志中的 `http.request.completed` 记录。
- 新生产前端资源已检出 `沪ICP备2026034877号` 和 `https://beian.miit.gov.cn/`。
- `http://www.acemock.cn/dns-check?source=codex` 和 `https://www.acemock.cn/dns-check?source=codex` 均返回 `301`，`Location` 为 `https://acemock.cn/dns-check?source=codex`，原路径和查询参数均得到保留。

回滚点：

- 部署前备份时间戳：`20260724_164117`。
- 旧源码归档：`/opt/quiz/backups/source/repo-before-git-bootstrap-20260724-163349.tgz`。
- 旧源码目录：`/opt/quiz/backups/source/repo-legacy-20260724-163349`。
- 生产运行配置备份：`/opt/quiz/backups/config/prod-env-before-config-merge-20260724-164054`。
- Nginx 配置应用前备份：`/opt/quiz/backups/config/nginx-quiztestdemo-before-www-redirect-20260724-160208.conf`。

### 2.2 2026-07-31 正式支付验证与全量部署

本次使用 `quiztestdemo-deploy` 流程部署生产环境，部署范围为前后端全部，分支为 `20260626-feature-development`，部署提交为 `91f9ad93c0f38660b3eaf7686c328795aae3f5a4`。

处理记录：

- 正式银联商务参数通过生产预检，商户号防误接、生产 API 域名、HTTPS 通知地址、请求签名和通知验签配置均有效；部署日志只记录脱敏标识。
- 正式支付仅向一个受控测试账号开放，白名单在服务端下单入口强制执行，不向浏览器暴露具体账号；支付验证完成后可清空白名单恢复正常购买。
- 支付通道已开启；支付生命周期自动处理和每日自动对账继续保持关闭，异常订单仍由管理员人工确认处理。
- 更新运行配置前先创建独立配置备份；代码部署前又完成生产 MySQL、上传文件和运行配置备份，备份清单为 `backup_before_deploy_20260731_162632.txt`。
- 服务器直接访问 GitHub 时连续出现 HTTP/2 与 TLS 传输中断，均发生在代码更新前。随后改用从已确认分支生成的完整 Git bundle，校验 SHA-256、完整历史和目标 Commit 后执行快进更新，未修改远程仓库地址。
- Prisma schema 与 migration guard 通过，共应用 9 条待执行迁移；部署后 27 条 migration 均已应用，未使用 `prisma db push`。
- 运行时检查确认生产模式、数据库连接、SMTP 登录、正式银联通道及单账号白名单均有效；后端构建、PM2 重载、前端生产构建和静态资源同步完成。

验证结果：

- 规范 HTTPS 首页、健康接口、数据库依赖只读接口和支付配置接口均返回 `200 OK`。
- 支付配置显示正式通道已就绪，价格策略保持为后台当前生效值。
- PM2 中 `quiz-api` 为 `online`，本机 API 与 Nginx 健康检查均成功，Request ID 已在实际运行日志中匹配。
- 生产仓库位于目标分支和目标 Commit，Prisma migration 状态为最新。
- 根分区使用率为 8%，可用内存约 2.6 GiB，部署后运行状态正常。

后续：

- 由白名单测试账号完成一次正式小额支付、异步通知、会员权益到账、订单查询和全额退款闭环。
- 正式验证完成前不开放其他账号下单；验证通过后清空支付购买白名单，并按产品决定是否启用生命周期任务与每日对账。

### 2.3 2026-07-31 支付体验修复与全量部署

本次使用 `quiztestdemo-deploy` 流程部署生产环境，部署范围为前后端全部，分支为 `20260626-feature-development`，部署提交为 `96616fa390005caaaf0f16727ae555ac04124266`。

处理记录：

- 银联商品详情改为支付页稳定支持的 ASCII 文案，月度与年度订单分别追加 `Monthly` 和 `Annual`，并增加配置校验以防中文乱码再次进入正式环境。
- 个人中心“开通会员”、升级和续费入口统一接入正式支付弹窗；支付完成后自动刷新会员权益与个人支付记录。
- 支付弹窗打开后直接创建订单并显示二维码；切换考试类型、套餐或支付渠道时先关闭旧订单，再自动生成与新选择一致的二维码。仅在生成失败时展示手动重试入口。
- 正式支付购买邮箱白名单已清空，所有已登录用户均可创建真实支付订单；支付生命周期自动处理和每日自动对账继续保持关闭。
- 更新支付运行配置前创建独立配置备份；代码部署前完成生产 MySQL、上传文件与运行配置备份，备份清单为 `backup_before_deploy_20260731_180657.txt`。
- 首次部署命令在 Git 更新完成后因本地 PowerShell 将远端标准错误误判为异常而中断；当时 PM2 仍运行旧进程且健康检查正常。修正日志采集方式后重新执行完整流程并成功完成。
- Prisma schema 与 migration guard 通过，27 条 migration 均已应用，本次无待执行迁移，未使用 `prisma db push`。

验证结果：

- 后端运行配置校验确认生产数据库、SMTP、正式银联通道和不受限购买模式均有效；商品描述配置为 `AceMock Membership`。
- 后端 TypeScript 构建、Prisma Client 生成、PM2 重载、前端生产构建及静态资源同步均成功。
- 规范 HTTPS 首页返回 `200 OK`，健康接口、数据库依赖的支付配置接口均返回标准成功响应，支付通道状态为可用。
- PM2 中 `quiz-api` 为 `online`，服务器本机 API 与 Nginx 健康检查成功，Request ID 已在实际运行日志中匹配。

剩余风险：

- 依赖审计仍报告前端高危及严重级别间接依赖、后端低危依赖；本次未执行可能引入破坏性升级的自动修复，需另行评估升级方案。
- 自动生成二维码会在用户打开弹窗时立即创建待支付订单；关闭弹窗或切换选项会主动关闭旧订单，后台仍应持续关注极端网络中断产生的过期待支付记录。

### 2.4 2026-08-03 首页、错题本与诊断报告更新全量部署

本次使用 `quiztestdemo-deploy` 流程部署生产环境，部署范围为前后端全部，分支为 `20260626-feature-development`，应用部署提交为 `e780ccdda6365d84759131882408634c0c84c8c8`。

处理记录：

- 首次从服务器直接拉取 GitHub 时发生 TLS 连接中断，流程在 Git 更新阶段退出，尚未开始备份、数据库迁移、构建、运行目录同步或 PM2 重载，因此未改变当时的生产运行状态。
- 按部署流程改用本地生成并校验的完整 Git bundle，服务器核对目标提交后从上一版本快进更新；远程仓库地址保持不变。
- 代码变更前完成生产 MySQL、上传文件和运行配置备份，备份清单为 `backup_before_deploy_20260803_155404.txt`。
- 运行配置必填项和目标环境检查通过；Prisma migration guard 通过，共应用 4 条迁移：`20260801120000_set_question_bank_limit_25`、`20260801130000_add_question_quality_tier`、`20260801131000_add_question_attempt_payload`、`20260802120000_wrong_question_summary`。部署后共 31 条 migration 已应用，未使用 `prisma db push`。
- 后端依赖安装、Prisma Client 生成、TypeScript 构建、运行目录同步和 PM2 重载成功；前端依赖安装、生产构建和静态资源同步成功。远端正式执行阶段约耗时 51 秒。

验证结果：

- 生产仓库位于目标分支和应用部署提交，数据库迁移状态为最新，PM2 中 `quiz-api` 为 `online`。
- 规范 HTTPS 首页、健康接口和依赖数据库的支付配置接口均返回标准成功响应；Request ID 已在实际 PM2 日志中匹配。
- 根分区使用率约 8%，可用内存约 2.6 GiB，部署后服务运行正常。

剩余风险：

- 生产支付通道状态为可用，但当前公开配置返回月度首购、月度续费和年度价格分别为 1、2、5 分，仍属于联调价格。部署流程未擅自修改既有业务配置；正式对外售卖前必须由产品与运维确认价格并重新验收下单金额。
- 依赖审计报告后端 1 个低危漏洞，前端 1 个低危、1 个中危、6 个高危和 1 个严重漏洞；本次未执行可能引入破坏性升级的 `npm audit fix --force`，需另行评估并安排依赖升级。
- 前端构建仍存在大体积 chunk 提示，当前不影响发布成功，但应在后续性能优化中拆分和复测。

### 2.5 2026-08-04 ESAT/TMUA 诊断报告与会员入口调整全量部署

本次使用 `quiztestdemo-deploy` 流程部署生产环境，部署范围为前后端全部，分支为 `20260626-feature-development`，部署提交为 `937dca8bd8de78b9064e63c9fc3bf29eff52364e`。

处理记录：

- ESAT 诊断报告补强科目整体评价、模块时间效率和七天启动路径，大模型分析字段支持逐项降级兜底，避免单个字段无效时丢弃整个模块结果。
- TMUA 诊断报告改为与 ESAT 一致的分析结构，同时展示 Paper 1 与 Paper 2 的诊断结论，并去除缺少官方数据支持的精确排名展示。
- 个人中心及项目内其他会员付费入口已暂时屏蔽跳转，点击后显示“开通会员功能上线中~”；原支付页跳转逻辑仍保留，便于后续重新开放。
- 代码更新前完成生产 MySQL、上传文件和运行配置备份，备份清单为 `backup_before_deploy_20260804_031726.txt`。
- Prisma schema 与 migration guard 通过，31 条 migration 均已应用，本次无待执行迁移，未使用 `prisma db push`。
- 后端依赖安装、Prisma Client 生成、TypeScript 构建、PM2 重载、前端生产构建与静态资源同步均成功，远端发布阶段约耗时 53 秒。

验证结果：

- 生产仓库位于目标分支和目标提交，数据库结构为最新状态。
- PM2 中 `quiz-api` 为 `online`，服务器本机 API 与 Nginx 健康检查均成功。
- 规范 HTTPS 首页返回 `200 OK`，健康接口与依赖数据库的支付配置接口均返回标准成功响应。
- Request ID 已在 PM2 实际运行日志中匹配到对应的 `http.request.completed` 记录。

剩余风险：

- 生产支付配置接口仍显示通道可用且当前价格为联调金额；由于前端付费入口已暂时屏蔽，普通用户无法从界面进入支付页。重新开放前仍需确认正式价格、通道配置和完整支付闭环。
- 依赖审计仍报告后端 1 个低危漏洞；本次未执行可能引入破坏性升级的自动修复。
- 前端构建仍存在大体积 chunk 提示，当前不影响发布成功，需在后续性能优化中拆分和复测。

### 2.6 2026-08-08 首页与诊断测试体验更新全量部署

本次使用 `quiztestdemo-deploy` 流程部署生产环境，部署范围为前后端全部，分支为 `20260807-quizDev`，部署提交为 `f94c1e1dd384eaff33d6e89fb1ed2abe38cb45cf`。

处理记录：

- 服务器直接访问 GitHub 时连续两次出现 TLS 传输中断，均发生在备份、迁移、构建和服务重载之前；随后改用本地生成并校验的完整 Git bundle，服务器核对目标提交后完成快进更新，远程仓库地址保持不变。
- 首次 bundle 预检发现生产运行配置缺少 `VISITOR_IP_HASH_SECRET`，流程在完成备份后、数据库迁移和服务变更前停止。随后使用受控配置合并脚本生成并写入该密钥，过程中未输出密钥值；配置备份为 `runtime-env-before-visitor-config-20260808_025304.tar.gz`。
- 最终部署前完成生产 MySQL、上传文件和运行配置备份，正式回滚清单为 `backup_before_deploy_20260808_025541.txt`；首次预检产生的额外备份清单为 `backup_before_deploy_20260808_025304.txt`。
- Prisma schema 与 migration guard 通过，应用迁移 `20260805120000_website_traffic_analytics`；部署后共 32 条 migration 均已应用，未使用 `prisma db push`。
- 后端依赖安装、Prisma Client 生成、TypeScript 构建、运行目录同步与 PM2 重载成功；前端依赖安装、Vite 生产构建和静态资源同步成功，正式远端执行阶段约耗时 53 秒。

验证结果：

- 生产仓库位于目标分支和目标提交，运行环境与目标生产数据库匹配，数据库迁移状态为最新。
- PM2 中 `quiz-api` 为 `online`，服务器本机 API 与 Nginx 健康检查均返回标准成功响应。
- 规范 HTTPS 首页返回 `200 OK`，公网健康接口与依赖数据库的支付配置只读接口均返回标准成功响应。
- Request ID 已在 PM2 实际运行日志中匹配到对应的 `http.request.completed` 记录。
- 根分区使用率约 8%，可用内存约 2.6 GiB，部署后服务运行正常。

剩余风险：

- 依赖审计报告后端 1 个低危和 1 个高危漏洞，前端 1 个低危、1 个中危和 2 个高危漏洞；本次未执行可能引入破坏性升级的自动修复，需另行评估依赖升级与回归测试。
- 前端构建仍存在大体积 chunk 提示，当前不影响发布成功，需在后续性能优化中拆分和复测。
- 生产支付配置接口仍显示联调价格；本次部署未擅自修改既有业务配置，正式开放付费入口前仍需确认价格与完整支付闭环。

## 三、生产运维变更记录

### 3.1 2026-07-24 生产安全与备份加固

本次不重新部署代码、不执行数据库迁移，也不修改业务数据；仅完善生产服务器访问控制、部署权限、逻辑备份、日志保留和本地健康检查。

处理记录：

- 新增 SSH 加固配置：禁止 root SSH 登录、禁止密码登录，仅保留公钥认证，将最大认证尝试次数降为 3 次，并将登录等待时间缩短为 30 秒。
- 启用 UFW，默认拒绝入站连接；保留 SSH、HTTP 和 HTTPS，其中 SSH 使用限速规则。公网验证确认应用端口 3001 已不可访问。
- 验证新的 `deploy` SSH 会话成功后才取消三分钟自动回滚；公网再次验证 root SSH 已被拒绝。
- `deploy` 不具有任意免密 sudo 权限，仅能调用 `/usr/local/sbin/quiz-nginx-apply`。该固定包装器会备份现有配置、执行 `nginx -t`，失败时恢复旧配置，成功时才重载 Nginx。
- 安装 `/opt/quiz/ops/backup-rds-runtime.sh`，通过 `quiz-rds-backup.timer` 每日 03:30 执行，随机延迟不超过 15 分钟，错过计划时间后会补跑，备份保留 14 天。
- 手动执行一次 `scheduled-verification` 备份，MySQL 压缩包、上传文件和运行配置均生成成功，清单内三项 SHA-256 校验全部通过。
- 新增 `quiz-healthcheck.timer`，每 5 分钟检查后端直连健康、Nginx 代理健康、PM2 进程状态和根磁盘使用率。
- 新增 `/etc/logrotate.d/quiz-pm2`，PM2 标准输出和错误日志每日轮转、压缩并保留 14 份；系统 `logrotate.timer` 已启用。
- 阿里云助手和安全防护客户端处于运行状态。ECS 内部无法读取阿里云账号侧的 RDS 具体备份策略和云监控告警规则，相关项目仍需在控制台核对。

验证结果：

- `deploy` 公钥 SSH 登录成功，root SSH 登录被拒绝。
- 任意 `sudo -n` 操作被拒绝，只有固定 Nginx 包装器可以免密执行；包装器验证和 Nginx 重载测试成功。
- 公网端口 3001 被阻断，正式 HTTPS 健康接口继续返回标准成功响应。
- `quiz-rds-backup.timer` 与 `quiz-healthcheck.timer` 均为 `enabled` 和 `active`。
- 手动健康检查、定时备份脚本、备份压缩包完整性、SHA-256 清单和 PM2 日志轮转策略均验证通过。

回滚与恢复：

- SSH/UFW 加固配置备份保存在 `/opt/quiz/backups/config/security-hardening-20260724_171213`。
- 部署脚本会继续在每次后端部署前创建独立逻辑备份；每日定时备份作为第二层保护。
- RDS 全量自动备份由阿里云平台强制开启，但仍需在控制台确认生产实例的最近成功备份集、日志备份和任意时间点恢复配置。
