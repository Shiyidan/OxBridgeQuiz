# 在线试卷系统 - 核心实现方案

## 方案概述

仅实现最核心的两个模块：
1. **PDF解析模块** - Python脚本将PDF转换为JSON
2. **前端答题模块** - Vue 3读取JSON渲染题目并支持答题

**简化策略**：
- 不做后端服务，前端直接读取本地JSON文件
- 不做数据库，用JSON文件作为数据源
- 样式简洁，功能优先

---

## 一、PDF解析模块

### 1.1 功能目标

输入：PDF试卷文件（如ENGAA_2023_S1_QuestionPaper.pdf）
输出：结构化JSON文件（包含题目文本、公式、图片路径等）

### 1.2 技术方案

```
PDF解析流程:

┌─────────────┐
│  输入PDF     │
└──────┬──────┘
       │
       ▼
┌──────────────────┐
│ 1. PyMuPDF提取    │  • 文本块（保留位置信息）
│    文本和图片     │  • 图片保存到本地
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 2. 题目区域识别   │  • 按题号分割（如 "1 ", "2 "）
│    和分割        │  • 或通过坐标范围
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 3. 内容分类      │  • 普通文本
│                 │  • 公式区域 → OCR识别为LaTeX（简化版可跳过）
│                 │  • 图形区域 → 保存图片路径
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 4. 输出JSON      │  • 按方案定义的格式
└──────────────────┘
```

### 1.3 全自动实现（MVP）

使用 **pix2tex** 进行自动公式识别，无需人工干预：

```python
# 解析器流程：
1. 提取PDF每页的文本内容
2. 提取PDF中的图片并保存
3. 识别公式区域（基于文本特征或图像检测）
4. 使用 pix2tex 将公式图片转换为 LaTeX
5. 按题号分割题目区域
6. 生成完整JSON（包含自动识别的公式）
```

**公式识别方案**：
- 使用 `pix2tex` 模型进行端到端公式OCR
- 支持行内公式和独立公式
- 自动识别希腊字母、分数、根号、上下标等

### 1.4 输出JSON格式

```json
{
  "title": "ENGAA 2023 Section 1",
  "year": 2023,
  "duration": 60,
  "totalQuestions": 6,
  "questions": [
    {
      "id": "q1",
      "number": 1,
      "content": [
        {"type": "text", "value": "The surface area of a solid sphere of radius R is equal to the total surface area of 10 solid closed cylinders of radius r and height 4r."},
        {"type": "break"},
        {"type": "text", "value": "Which of the following is an expression for R in terms of r?"},
        {"type": "break"},
        {"type": "text", "value": "(The surface area of a sphere of radius R is 4πR².)"}
      ],
      "options": [
        {"label": "A", "content": [{"type": "formula", "value": "R = 5r", "latex": "R = 5r"}]},
        {"label": "B", "content": [{"type": "formula", "value": "R = √12r", "latex": "R = \\sqrt{12r}"}]},
        {"label": "C", "content": [{"type": "formula", "value": "R = √(25r/2)", "latex": "R = \\sqrt{\\frac{25r}{2}}"}]},
        {"label": "D", "content": [{"type": "formula", "value": "R = √10r", "latex": "R = \\sqrt{10r}"}]},
        {"label": "E", "content": [{"type": "formula", "value": "R = √3(10r)", "latex": "R = \\sqrt[3]{10r}"}]},
        {"label": "F", "content": [{"type": "formula", "value": "R = √15r", "latex": "R = \\sqrt{15r}"}]}
      ],
      "correctAnswer": "C",
      "tags": ["geometry", "surface-area"]
    }
  ]
}
```

### 1.5 依赖

```txt
# requirements.txt
pymupdf==1.23.7
pillow==10.1.0
pix2tex==0.1.2
torch==2.1.1
torchvision==0.16.1
transformers==4.36.0
numpy==1.26.2
```

**注意**：pix2tex 依赖 PyTorch，首次运行时会自动下载模型（约几百MB）。

### 1.6 核心代码结构

```python
# parser/pdf_to_json.py

import fitz  # PyMuPDF
import json
import os
from pathlib import Path

def extract_pdf_content(pdf_path, output_dir):
    """提取PDF内容并生成JSON"""
    doc = fitz.open(pdf_path)
    
    questions = []
    image_dir = Path(output_dir) / "images"
    image_dir.mkdir(exist_ok=True)
    
    for page_num in range(len(doc)):
        page = doc[page_num]
        
        # 1. 提取文本
        text = page.get_text()
        
        # 2. 提取图片
        images = extract_images(page, image_dir, page_num)
        
        # 3. 按题号分割题目（简化版：按 "\n数字 " 分割）
        page_questions = split_questions(text, images)
        questions.extend(page_questions)
    
    # 4. 生成JSON
    result = {
        "title": extract_title(doc),
        "year": 2023,
        "duration": 60,
        "totalQuestions": len(questions),
        "questions": questions
    }
    
    # 5. 保存
    output_path = Path(output_dir) / "paper.json"
    with open(output_path, 'w', encoding='utf-8') as f:
        json.dump(result, f, ensure_ascii=False, indent=2)
    
    return output_path

def split_questions(text, images):
    """按题号分割题目（简化实现）"""
    # 实现按 "1 ", "2 " 等模式分割
    # 返回题目数组
    pass

def extract_images(page, image_dir, page_num):
    """提取页面中的图片"""
    # 保存图片并返回路径列表
    pass
```

---

## 二、前端答题模块

### 2.1 功能目标

- 加载JSON格式的试卷数据
- 渲染题目（支持文本、公式、图片）
- 支持选择答案
- 提交后显示得分和正确答案

### 2.2 项目结构

```
quiz-web/
├── index.html
├── package.json
├── vite.config.ts
└── src/
    ├── main.ts
    ├── App.vue
    ├── components/
    │   ├── QuestionRenderer.vue    # 题目渲染器
    │   ├── ContentBlock.vue        # 内容块渲染（文本/公式/图片）
    │   ├── OptionList.vue          # 选项列表
    │   └── ExamResult.vue          # 考试结果
    ├── views/
    │   ├── HomeView.vue            # 首页（选择试卷）
    │   └── ExamView.vue            # 答题页面
    ├── stores/
    │   └── exam.ts                 # Pinia store
    └── types/
        └── index.ts                # TypeScript类型定义
```

### 2.3 核心组件

#### 2.3.1 题目渲染器 QuestionRenderer.vue

```vue
<template>
  <div class="question-box">
    <!-- 题号和内容 -->
    <div class="question-header">
      <span class="question-number">{{ question.number }}.</span>
      <div class="question-content">
        <ContentBlock 
          v-for="(block, idx) in question.content" 
          :key="idx" 
          :block="block" 
        />
      </div>
    </div>
    
    <!-- 选项 -->
    <OptionList 
      :options="question.options"
      :selected="selectedAnswer"
      :disabled="submitted"
      @select="onSelect"
    />
    
    <!-- 答案反馈 -->
    <div v-if="submitted" class="answer-feedback">
      <span v-if="isCorrect" class="correct">✓ 正确</span>
      <span v-else class="wrong">✗ 错误，正确答案是 {{ question.correctAnswer }}</span>
    </div>
  </div>
</template>

<script setup lang="ts">
import type { Question } from '@/types';

interface Props {
  question: Question;
  selectedAnswer?: string;
  submitted: boolean;
}

const props = defineProps<Props>();
const emit = defineEmits<{
  (e: 'select', answer: string): void;
}>();

const isCorrect = computed(() => {
  return props.selectedAnswer === props.question.correctAnswer;
});

const onSelect = (label: string) => {
  if (!props.submitted) {
    emit('select', label);
  }
};
</script>
```

#### 2.3.2 内容块 ContentBlock.vue

```vue
<template>
  <span v-if="block.type === 'text'" class="text-block">{{ block.value }}</span>
  <br v-else-if="block.type === 'break'" />
  <FormulaBlock 
    v-else-if="block.type === 'formula'" 
    :latex="block.latex || block.value" 
  />
  <img 
    v-else-if="block.type === 'image'" 
    :src="block.src" 
    class="question-image"
  />
</template>

<script setup lang="ts">
import type { ContentBlock } from '@/types';

interface Props {
  block: ContentBlock;
}

defineProps<Props>();
</script>
```

#### 2.3.3 公式渲染 FormulaBlock.vue

```vue
<template>
  <span class="formula" v-html="rendered"></span>
</template>

<script setup lang="ts">
import { computed } from 'vue';
import katex from 'katex';
import 'katex/dist/katex.min.css';

interface Props {
  latex: string;
}

const props = defineProps<Props>();

const rendered = computed(() => {
  try {
    return katex.renderToString(props.latex, {
      throwOnError: false,
      displayMode: false
    });
  } catch {
    return `<span class="formula-error">${props.latex}</span>`;
  }
});
</script>
```

#### 2.3.4 选项列表 OptionList.vue

```vue
<template>
  <div class="options-list">
    <button
      v-for="option in options"
      :key="option.label"
      class="option-btn"
      :class="{
        'selected': selected === option.label,
        'correct': showCorrect && option.label === correctAnswer,
        'wrong': showCorrect && selected === option.label && selected !== correctAnswer
      }"
      :disabled="disabled"
      @click="$emit('select', option.label)"
    >
      <span class="option-label">{{ option.label }}.</span>
      <ContentBlock 
        v-for="(block, idx) in option.content" 
        :key="idx" 
        :block="block" 
      />
    </button>
  </div>
</template>

<script setup lang="ts">
import type { Option } from '@/types';

interface Props {
  options: Option[];
  selected?: string;
  correctAnswer?: string;
  disabled?: boolean;
  showCorrect?: boolean;
}

defineProps<Props>();
defineEmits<{
  (e: 'select', label: string): void;
}>();
</script>
```

### 2.4 数据流

```
┌─────────────────────────────────────────────┐
│                   数据流                      │
└─────────────────────────────────────────────┘

1. 准备阶段：
   PDF ──(parser)--> paper.json ──(copy)--> public/data/paper.json

2. 加载阶段：
   ExamView.vue ──(fetch)--> paper.json ──(store)--> Pinia store

3. 渲染阶段：
   Pinia store ──(props)--> QuestionRenderer ──(props)--> ContentBlock

4. 答题阶段：
   User ──(click)--> OptionList ──(emit)--> ExamView ──(commit)--> Store

5. 提交阶段：
   ExamView ──(计算得分)--> 显示结果
```

### 2.5 样式说明（简洁版）

```css
/* 基础样式 */
.question-box {
  border: 1px solid #ddd;
  border-radius: 8px;
  padding: 16px;
  margin-bottom: 16px;
}

.question-number {
  font-weight: bold;
  margin-right: 8px;
}

.option-btn {
  display: block;
  width: 100%;
  padding: 12px;
  margin: 8px 0;
  border: 1px solid #ccc;
  border-radius: 4px;
  background: white;
  text-align: left;
  cursor: pointer;
}

.option-btn:hover:not(:disabled) {
  background: #f5f5f5;
}

.option-btn.selected {
  border-color: #1890ff;
  background: #e6f7ff;
}

.option-btn.correct {
  border-color: #52c41a;
  background: #f6ffed;
}

.option-btn.wrong {
  border-color: #ff4d4f;
  background: #fff2f0;
}
```

---

## 三、实现步骤

### Step 1: 创建PDF解析器（Python）

```bash
mkdir parser
cd parser
pip install pymupdf pillow
```

编写 `pdf_to_json.py`，实现：
1. 读取PDF文件
2. 提取文本和图片
3. 生成JSON文件

### Step 2: 创建Vue 3项目

```bash
npm create vue@latest quiz-web
cd quiz-web
npm install
npm install katex pinia
npm install -D @types/katex
```

### Step 3: 生成测试数据

1. 用解析器处理ENGAA PDF（或手动创建JSON）
2. 将JSON放入 `public/data/paper.json`
3. 将提取的图片放入 `public/data/images/`

### Step 4: 实现前端组件

1. 定义TypeScript类型
2. 实现FormulaBlock.vue（公式渲染）
3. 实现ContentBlock.vue（内容块）
4. 实现OptionList.vue（选项列表）
5. 实现QuestionRenderer.vue（题目渲染）
6. 实现ExamView.vue（答题页面）

### Step 5: 测试运行

```bash
npm run dev
```

访问页面，加载JSON，测试答题流程。

---

## 四、依赖汇总

### Python解析器
```txt
pymupdf==1.23.7
pillow==10.1.0
```

### Vue前端
```json
{
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.5",
    "pinia": "^2.1.7",
    "katex": "^0.16.9"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.5.2",
    "typescript": "^5.3.3",
    "vite": "^5.0.8"
  }
}
```

---

## 五、核心挑战与策略

| 挑战 | 解决方案 |
|------|---------|
| PDF公式识别 | 使用 pix2tex 自动识别，将公式图片转为LaTeX |
| 题目分割边界 | 基于题号正则匹配 + 坐标分析 |
| 复杂图形渲染 | 直接展示提取的图片，不做SVG转换 |
| 图片位置排版 | 按流式布局展示，图片独占一行 |

## 六、pix2tex 公式识别说明

**什么是 pix2tex**：
- 基于深度学习的公式OCR工具
- 输入：公式图片
- 输出：LaTeX 代码
- 支持：数学公式、希腊字母、分数、上下标等

**使用方法**：
```python
from pix2tex.cli import LatexOCR

ocr = LatexOCR()
latex = ocr("formula_image.png")
print(latex)  # 输出: "R = \\sqrt{\\frac{25r}{2}}"
```

---

这个方案够简化吗？技术流程是否清晰？确认后我立即开始生成代码。
