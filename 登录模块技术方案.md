# 登录模块技术方案

> 基于现有技术架构与数据库规范，参考行业通用 JWT 鉴权方案，充分考虑安全性、可维护性和可扩展性。

---

## 一、模块划分

整个登录体系拆分为三个独立模块，各司其职：

```
┌─────────────────────────────────────────────────────────┐
│                      登录模块                            │
├────────────────────┬────────────────────────────────────┤
│   前端 Login 模块   │       后端 Auth 模块               │
│   (quiz-web)       │       (apps/api)                   │
├────────────────────┼────────────────────────────────────┤
│ • LoginView.vue    │ • routes/auth.ts   — 认证 API      │
│ • RegisterView.vue │ • middleware/auth.ts — 鉴权中间件   │
│ • stores/auth.ts   │ • middleware/admin.ts — 角色中间件  │
│ • utils/request.ts │ • services/jwt.ts  — JWT 工具      │
│ • router 守卫       │                                    │
├────────────────────┴────────────────────────────────────┤
│                   共用类型定义                            │
│   User, LoginRequest, RegisterRequest, AuthResponse     │
└─────────────────────────────────────────────────────────┘
```

| 模块 | 位置 | 职责 | 对外接口 |
|------|------|------|---------|
| **前端 Login 模块** | `quiz-web/src/views/auth/` `stores/auth.ts` `utils/request.ts` `router/index.ts` | 登录/注册页面、令牌管理、Axios 拦截器、路由守卫 | Pinia `useAuthStore`：`login()` `register()` `logout()` `isLoggedIn` `user` |
| **后端 Auth 模块** | `apps/api/src/routes/auth.ts` `middleware/auth.ts` `middleware/admin.ts` `services/jwt.ts` | 认证 API、JWT 签发/验证、鉴权中间件、角色控制 | REST API：`/api/auth/login` `/api/auth/register` `/api/auth/me` `/api/auth/logout`；中间件：`requireAuth()` `requireAdmin()` |
| **数据库** | `prisma/schema.prisma` → `User` 表 | 用户持久化、密码哈希 | Prisma Client（由 Auth 模块消费） |

**模块边界约定：**

- 前端 Login 模块**不直接操作数据库**，所有数据读写走 `/api/auth/*` 接口
- 后端 Auth 模块**不感知前端路由**，仅返回 401/403 状态码，由前端自行跳转
- 其他业务模块（试题、试卷）通过**引用中间件**获得鉴权能力，不与 Auth 模块内部耦合

---

## 二、需求概述

### 2.1 功能范围（MVP）

| 功能 | 说明 |
|------|------|
| 邮箱注册 | 邮箱 + 密码 + 姓名，注册后自动登录 |
| 邮箱登录 | 邮箱 + 密码，返回 JWT 令牌 |
| 退出登录 | 清除本地令牌和用户信息 |
| 路由守卫 | 未登录拦截到 `/login`，已登录跳过登录页 |
| 角色鉴权 | `student` / `admin`，管理后台仅 admin 可访问 |
| 获取当前用户 | `/api/auth/me` 返回用户信息，用于 NavBar 展示 |
| 用户管理 | 超管可在后台查看所有注册用户列表（用户名、邮箱、角色、权益），并编辑用户角色 |

### 2.2 本期不做

- 第三方登录（Google / 微信 OAuth）
- 邮箱验证 / 手机验证码
- 多因素认证（MFA）
- 密码重置邮件（仅前端留入口，后端后续补）
- 单设备登录限制（一个账号只能同时在一个终端登录，新设备登录后旧设备自动失效）

---

## 三、登录保持时长

JWT Token 有效期 **7 天**，后端签发时写入 `exp` 字段，每次请求后端自动校验。

### 3.1 时间轴示意

```
用户登录（t=0）
  └── JWT Token 签发，7 天后过期
      │
      ├── t=1天：用户做题、浏览，Token 有效
      │
      ├── t=3天：关闭页面，Token 存 localStorage
      │
      ├── t=5天：再次打开浏览器
      │   └── localStorage 有 Token → 直接恢复登录态
      │
      └── t=8天：超过 7 天未访问
          └── Token 过期 → 后端返回 401 → 前端跳 /login
```

### 3.2 关键时间节点

| 场景 | 失去登录态的时长 | 说明 |
|------|----------------|------|
| **持续使用中** | 7 天后 | 每次请求后端验证 JWT exp，过期即 401 |
| **关闭页面后回来** | **7 天内**自动恢复 | Token 存 localStorage，打开页面直接可用 |
| **完全不用** | **7 天后**需重新登录 | Token 到期，只能重新输入邮箱密码 |

### 3.3 超时由谁计算

**后端计算，前端只响应。**

```
超时判断流程：

  [前端发起 API 请求]
      │
      ▼
  [后端 Auth 中间件]
      │  jwt.verify(token, secret)
      │  JWT 自带的 exp 字段由 jsonwebtoken 库自动校验
      │
      ├── exp 未到 → 放行，执行接口逻辑
      │
      └── exp 已过 → jwt.verify() 抛出 TokenExpiredError
            │
            ▼
        后端返回 401 { code: 'TOKEN_EXPIRED' }
            │
            ▼
        [前端 Axios 响应拦截器] 捕获 401
            │
            └── 清除登录态 → 跳转 /login
```

**各端职责：**

| 端 | 做什么 | 不做什么 |
|----|-------|---------|
| **后端** | JWT 签发时写入 `exp` 过期时间；每次请求 `jwt.verify()` 自动校验；过期返回 401 | — |
| **前端** | 响应式处理 401：清除登录态 → 跳转 /login | 不自行计算时间、不设 `setTimeout` 模拟过期、不信任前端时钟 |
| **localStorage** | 存储 Token + user 信息 | 不参与超时判断逻辑 |

**为什么不用前端计时：**
- 前端时钟不可信（用户可修改系统时间）
- 多个标签页难以同步计时器
- 安全边界必须在服务端，前端只能做 UX 兜底

---

## 四、数据库设计

### 4.1 User 表

与 `数据库构建.md` 对齐，在现有 `schema.prisma` 中新增：

```prisma
model User {
  id             String   @id @default(uuid())
  name           String
  email          String   @unique
  password       String
  role           String   @default("student")   // student | admin
  paymentStatus  String   @default("free")      // free | paid | expired
  diagnosticUsed Boolean  @default(false)       // 是否已使用免费诊断机会
  avatar         String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  examRecords ExamRecord[]
}
```

> **注意**：`paymentStatus` 和 `diagnosticUsed` 现阶段先做字段和权限判断逻辑，付费流程（微信/支付宝接入）属于后续迭代。

**迁移步骤：**

1. 修改 `prisma/schema.prisma`，新增 User 模型
2. 执行 `npx prisma migrate dev --name add-user`
3. 同步更新 `数据库构建.md`（已对齐，无需额外修改）

### 4.2 索引策略

- `email` 字段设 `@unique`，自动创建唯一索引，登录查询走索引
- 所有查询通过 Prisma Client，禁止裸 SQL

### 4.3 DiagnosticSession（诊断测试会话）

游客做诊断测试时创建，用于关联匿名答题记录，注册后绑定到 User：

```prisma
model DiagnosticSession {
  id          String    @id @default(uuid())
  userId      String?                        // 外键 → User.id（注册后回填，初始为 null）
  answers     String                         // 用户答案 JSON：[{questionId, selectedAnswer, isCorrect}]
  totalQuestions Int                         // 总题数
  correctCount   Int                         // 答对题数
  status      String    @default("anonymous") // anonymous | linked
  createdAt   DateTime  @default(now())

  user User? @relation(fields: [userId], references: [id])

  @@index([userId])
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | UUID | ✅ | 主键，即返回给前端的 `sessionId` |
| `userId` | String? | | 注册后回填，关联到 User |
| `answers` | String | ✅ | 用户答案 JSON 数组 |
| `totalQuestions` | Int | ✅ | 总题数 |
| `correctCount` | Int | ✅ | 答对数量 |
| `status` | String | ✅ | `anonymous`=匿名 / `linked`=已关联用户 |

**关键逻辑**：
- 游客提交诊断 → 创建 `DiagnosticSession`（`userId = null`, `status = "anonymous"`）
- 游客注册时传入 `diagnosticSessionId` → 回填 `userId` → `status = "linked"` → 返回完整报告
- 游客登录时若传入 `diagnosticSessionId` → 同样回填关联
- 已登录用户不创建 DiagnosticSession，直接查 `ExamRecord` + `AnswerRecord` 生成报告

---

## 五、鉴权方案设计

### 5.1 方案选型：JWT 单令牌

| 维度 | JWT 单令牌 | Session + Cookie |
|------|-----------|------------------|
| 无状态 | ✅ 服务端不存状态 | ❌ 需 Redis/DB 存 session |
| 水平扩展 | ✅ 天然支持 | ❌ 需共享 session 存储 |
| 实现复杂度 | ✅ 简单 | 🟡 中等 |
| 适合本项目 | ✅ 当前阶段最优 | — |

**结论：采用 JWT 单令牌方案**，一个 Token 承载全部鉴权，7 天有效期。

### 5.2 令牌设计

| 属性 | 值 |
|------|-----|
| 有效期 | **7 天** |
| 存储位置（前端） | localStorage |
| 传输方式 | `Authorization: Bearer <token>` |
| Payload | `{ userId, email, role }` |
| 签名算法 | HS256 |

**为什么这样设计：**

- Token 存 localStorage，跨标签页共享 + 页面刷新恢复登录态
- 7 天覆盖一周使用周期，学生每周至少练一次题
- 单令牌无刷新逻辑，前后端代码量大幅减少
- XSS 窃取风险与双令牌 Refresh Token 存 localStorage 场景相同，无额外风险

### 5.3 鉴权流程

```
[用户请求受保护接口]
    ↓
[前端 Axios 拦截器] → 自动附带 Authorization: Bearer <token>
    ↓
[后端 authMiddleware] → jwt.verify(token)
    ├── 有效 → 放行
    └── 过期 → 返回 401
            ↓
        [前端拦截器] → 清除登录态 → 跳转 /login
```

### 5.4 密码安全

**总体方案：HTTPS 传输层加密 + 服务端 bcrypt 哈希存储。** 这是业界通用做法（Google、GitHub、Apple 等均采用此模式），不推荐客户端额外哈希。

**为什么不推荐客户端哈希（如 SHA + 盐）：**

- 客户端哈希后的值实质上变成新密码——数据库泄露后攻击者直接用哈希值即可登录，加盐白加
- 服务端无法校验密码复杂度（看不到原始明文）
- 未来升级哈希算法需要客户端同步更新

**完整链路：**

```
浏览器                              服务端                             数据库
  密码明文 ──[HTTPS/TLS 加密]──→   拿到明文                           
                                   │→ bcrypt(明文, salt=12) ──→   存哈希值
                                   │→ 登录时 bcrypt.compare()         │
                                   │   匹配则签发 JWT                 │
```

**各环节措施：**

| 环节 | 方案 | 说明 |
|------|------|------|
| 传输层 | **HTTPS（TLS 1.2+）** | 生产环境强制开启，密码在 TCP 层之上被加密，中间人无法窃听。开发期 localhost 可接受明文 |
| 存储层 | **bcrypt + 自动盐值** | salt rounds = 12，每次哈希自动生成随机盐值并入哈希结果中。即使两个用户密码相同，存储的哈希也不同 |
| 校验层 | **bcrypt.compare()** | 登录时取出哈希值，bcrypt 从哈希中提取盐值再计算比对，全程不还原明文 |

**密码规则（v1）：**
- 长度：8 ~ 32 位
- 必须同时包含字母和数字（`/[a-zA-Z]/` + `/[0-9]/`）
- 后续可扩展：特殊字符要求、禁止常见弱密码等

**其他措施：**
- **二次校验**：前端注册页设"确认密码"字段，两次输入不一致时阻止提交并提示；后端同样校验 `password === confirmPassword`
- **速率限制**：登录/注册接口每 IP 每分钟最多 5 次（`express-rate-limit`）
- **错误信息模糊**：登录失败统一返回"邮箱或密码错误"，不区分用户是否存在

---

## 六、权限管理

权限管理分为**三个层级**，覆盖从游客到付费用户的完整漏斗：

```
游客（未登录）
  │  可浏览首页、试题库、做诊断测试（仅部分报告）
  │  → 注册/登录转化点：查看完整诊断报告
  ↓
学生（已登录、未付费）
  │  可做题、查看个人中心、1 次免费诊断测试（完整报告）
  │  → 付费转化点：第 2 次诊断测试、高级功能
  ↓
付费用户（已登录、已付费）
  │  无限制诊断测试、全部功能
  ↓
管理员
     管理后台全部功能 + 学生所有权限
```

### 6.1 用户身份模型

| 身份 | 判断条件 | 权限等级 |
|------|---------|:-------:|
| 游客 | `!req.user`（无 JWT） | 0 |
| 学生（免费） | `req.user.role === 'student' && paymentStatus === 'free'` | 1 |
| 学生（付费） | `req.user.role === 'student' && paymentStatus === 'paid'` | 2 |
| 管理员 | `req.user.role === 'admin'` | 3 |

权限等级是递增的：高等级包含低等级的全部权限。

### 6.2 诊断测试权限流

这是整个漏斗的核心转化链路：

```
┌──────────────────────────────────────────────────────────────┐
│  第 1 步：游客进入诊断测试                                      │
│  GET /api/diagnostic/questions  公开接口，无需登录              │
│  返回一套诊断用试题（不限次数）                                   │
├──────────────────────────────────────────────────────────────┤
│  第 2 步：游客做题 & 提交                                       │
│  POST /api/diagnostic/submit   公开接口，无需登录               │
│  请求体：{ answers: [{questionId, selectedAnswer}] }           │
│  后端批改 → 计算得分                                            │
│  返回【部分报告】：总分、正确率、各题对错（不含解析和知识点分析）   │
├──────────────────────────────────────────────────────────────┤
│  第 3 步：转化提示                                              │
│  前端展示部分报告 + 遮罩层：                                     │
│  "注册/登录即可查看完整分析报告"                                  │
│  [立即注册] [已有账号？去登录]                                    │
├──────────────────────────────────────────────────────────────┤
│  第 4 步：注册/登录后回看完整报告                                 │
│  前端将 diagnosticSessionId 传给注册/登录接口                    │
│  后端：将诊断记录关联到 User → 返回完整报告                        │
│  标记 User.diagnosticUsed = true                                │
│  【完整报告】= 部分报告 + 逐题解析 + 知识点掌握度 + 薄弱项分析     │
├──────────────────────────────────────────────────────────────┤
│  第 5 步：后续访问                                              │
│  已登录 + 未付费 + diagnosticUsed = true                        │
│  → 再次进入诊断测试 → 提示"免费诊断次数已用完，升级付费解锁"       │
│  → [查看付费方案]                                                │
│                                                                │
│  已登录 + 已付费                                                │
│  → 无限制使用诊断测试                                            │
└──────────────────────────────────────────────────────────────┘
```

### 6.3 诊断报告的数据结构

```ts
// 部分报告（游客可看）
interface PartialReport {
  sessionId: string           // 用于注册后关联
  totalQuestions: number
  correctCount: number
  score: number               // 百分制
  accuracy: number            // 正确率 0-1
  items: {                    // 每题仅显示对错
    questionId: string
    order: number
    isCorrect: boolean
  }[]
}

// 完整报告（登录后可看，在部分报告基础上扩展）
interface FullReport extends PartialReport {
  items: (ReportItem & {
    explanation: string       // 题目解析
    knowledgePoints: string[] // 涉及知识点
  })[]
  knowledgeAnalysis: {        // 知识点掌握度分析
    point: string
    accuracy: number
    level: 'weak' | 'moderate' | 'strong'
  }[]
  suggestion: string           // 备考建议
}
```

### 6.4 前端路由权限表

| 路由 | 游客 | 学生(免费) | 学生(付费) | 管理员 | 拦截行为 |
|------|:---:|:--------:|:--------:|:-----:|---------|
| `/` 首页 | ✅ | ✅ | ✅ | ✅ | — |
| `/login` 登录/注册 | ✅ | ➡️跳首页 | ➡️跳首页 | ➡️跳首页 | 已登录 → `/` |
| `/question-bank` 试题库 | ✅ | ✅ | ✅ | ✅ | — |
| `/diagnostic` 诊断测试 | ✅ | ✅ | ✅ | ✅ | — |
| `/diagnostic/report` 诊断报告 | 🟡部分 | 🟡1次 | ✅ | ✅ | 游客仅部分；免费超1次 → 付费引导 |
| `/profile` 个人中心 | ❌ | ✅ | ✅ | ✅ | 未登录 → `/login` |
| `/practice` 在线答题 | ❌ | ✅ | ✅ | ✅ | 未登录 → `/login` |
| `/admin/*` 管理后台 | ❌ | ❌ | ❌ | ✅ | 非 admin → `/` |

### 6.5 后端 API 权限表

| 接口 | 游客 | 学生(免费) | 学生(付费) | 管理员 | 中间件 |
|------|:---:|:--------:|:--------:|:-----:|-------|
| `GET /api/health` | ✅ | ✅ | ✅ | ✅ | 无 |
| `GET /api/papers` | ✅ | ✅ | ✅ | ✅ | 无 |
| `GET /api/papers/:id` | ✅ | ✅ | ✅ | ✅ | 无 |
| `POST /api/auth/login` | ✅ | ✅ | ✅ | ✅ | 无 + 限速 |
| `POST /api/auth/register` | ✅ | ✅ | ✅ | ✅ | 无 + 限速 |
| `GET /api/auth/me` | ❌ | ✅ | ✅ | ✅ | `requireAuth` |
| `POST /api/auth/logout` | ❌ | ✅ | ✅ | ✅ | `requireAuth` |
| `GET /api/diagnostic/questions` | ✅ | ✅ | ✅ | ✅ | 无 |
| `POST /api/diagnostic/submit` | ✅ | ✅ | ✅ | ✅ | 无（内部判断鉴权） |
| `GET /api/diagnostic/report/:id` | 🟡部分 | 🟡查配额 | ✅ | ✅ | `requireAuth`（游客走 sessionId） |
| `GET /api/papers/:id/pdf` | ❌ | ✅ | ✅ | ✅ | `requireAuth` |
| `PUT /api/papers/:id` | ❌ | ❌ | ❌ | ✅ | `requireAdmin` |
| `DELETE /api/papers/:id` | ❌ | ❌ | ❌ | ✅ | `requireAdmin` |
| `PUT /api/papers/:id/publish` | ❌ | ❌ | ❌ | ✅ | `requireAdmin` |
| `POST /api/upload/paper` | ❌ | ❌ | ❌ | ✅ | `requireAdmin` |
| `GET /api/parse-tasks/:id` | ❌ | ❌ | ❌ | ✅ | `requireAdmin` |
| `POST /api/parse-tasks/:id/retry` | ❌ | ❌ | ❌ | ✅ | `requireAdmin` |

### 6.6 鉴权执行链路

以"游客做完诊断 → 注册 → 查看完整报告"为例：

```
1. 前端路由守卫
   router.beforeEach → to.path = '/diagnostic'
   → 公开路由，直接放行
   ✅ 游客进入诊断测试页

2. 游客做题并提交
   → POST /api/diagnostic/submit（无 Authorization header）
   → 后端未检测到 JWT → 以匿名模式处理
   → 批改答案 → 创建 DiagnosticSession（含 sessionId）
   → 返回 PartialReport + sessionId
   ✅ 前端展示部分报告 + 注册引导

3. 游客点击"立即注册"
   → /register 页面，填写信息提交
   → POST /api/auth/register + { diagnosticSessionId }
   → 后端：创建 User → 将 DiagnosticSession 关联到新 User
   → User.diagnosticUsed = true
   → 返回 { user, token, fullReport }
   ✅ 注册成功 → 直接展示完整报告

4. 已登录用户再次访问诊断测试
   → GET /api/diagnostic/questions
   → 后端：requireAuth 中间件提取 user
   → 查询 User.diagnosticUsed
   ├── 未使用（false）→ 返回试题
   └── 已使用（true）
       └── 检查 paymentStatus
           ├── free → 403 { code: 'DIAGNOSTIC_QUOTA_EXHAUSTED' }
           └── paid → 返回试题
```

### 6.7 安全原则

- **前端路由守卫仅是 UX 优化**，不能作为安全边界——后端每个需要鉴权的接口**必须**挂对应中间件
- **权限判断统一从 JWT 读取**，不信任前端传来的任何角色/付费状态字段
- 新增页面/接口时，**默认加鉴权**，确认公开后才可以去掉
- 管理后台接口挂 `requireAdmin`，学生角色即使猜出 API 路径也无法访问
- **诊断配额在后端校验**：`diagnosticUsed` 由服务端读写，前端不可篡改
- 匿名诊断结果通过 `sessionId` 关联，sessionId 为 UUID，不可遍历

---

## 七、后端实现

### 7.1 依赖新增

```bash
npm install bcryptjs jsonwebtoken express-rate-limit
npm install -D @types/bcryptjs @types/jsonwebtoken
```

### 7.2 新增文件清单

```
apps/api/src/
├── routes/
│   ├── auth.ts          # 新增：认证路由（login/register/me/logout）
│   └── diagnostic.ts    # 新增：诊断测试路由（questions/submit/report）
├── services/
│   ├── jwt.ts           # 新增：JWT 签发与验证工具
│   └── diagnostic.ts    # 新增：诊断批改与报告生成逻辑
├── middleware/
│   ├── auth.ts          # 新增：JWT 鉴权中间件（requireAuth）
│   └── admin.ts         # 新增：admin 角色中间件（requireAdmin）
└── index.ts             # 修改：挂载 /api/auth 和 /api/diagnostic 路由
```

### 7.3 路由设计

#### Auth 路由

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| POST | `/api/auth/register` | 无 | 注册新用户，可选传 `diagnosticSessionId` 关联诊断报告 |
| POST | `/api/auth/login` | 无 | 邮箱密码登录，可选传 `diagnosticSessionId` 关联诊断报告 |
| GET | `/api/auth/me` | requireAuth | 获取当前登录用户信息 |
| POST | `/api/auth/logout` | requireAuth | 前端清除 Token 即可，后端无状态 |

#### Diagnostic 路由

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| GET | `/api/diagnostic/questions` | 无 | 获取诊断测试题目（已登录会检查配额） |
| POST | `/api/diagnostic/submit` | 无 | 提交答案，游客返回部分报告，登录用户返回完整报告 |
| GET | `/api/diagnostic/report/:id` | 见逻辑 | `:id` 为 sessionId；游客仅返回部分、登录用户查配额后返回完整 |

### 7.4 核心实现要点

#### `services/jwt.ts` — JWT 工具

```ts
// 环境变量
const JWT_SECRET = process.env.JWT_SECRET || 'dev-secret-change-in-production'
const TOKEN_TTL = '7d'

// 签发 Token
export function signToken(user: { id: string; email: string; role: string }): string

// 验证 Token，返回 payload 或抛错
export function verifyToken(token: string): JwtPayload
```

#### `middleware/auth.ts` — 鉴权中间件

```ts
// 从 Authorization header 提取 Bearer token
// 调用 verifyToken 验证
// 将 { userId, email, role } 写入 req.user
// 失败 → 401 { code: 'UNAUTHORIZED', message: '请先登录' }
export function requireAuth(req, res, next): void
```

#### `middleware/admin.ts` — 角色中间件

```ts
// 依赖 requireAuth（已在 req.user 上）
// 检查 req.user.role === 'admin'
// 非 admin → 403 { code: 'FORBIDDEN', message: '无权限' }
export function requireAdmin(req, res, next): void
```

#### `routes/auth.ts` — 路由处理

**POST /api/auth/register**
```
输入：{ email, password, confirmPassword, name, diagnosticSessionId? }
校验：
  - 邮箱格式、name 非空
  - password 与 confirmPassword 完全一致
  - 密码规则：8~32 位，必须包含字母 + 数字
逻辑：
  1. 查邮箱是否已注册 → bcrypt 哈希 → 创建 User
  2. 如果传了 diagnosticSessionId：
     - 查 DiagnosticSession → 回填 userId → status = "linked"
     - 标记 User.diagnosticUsed = true
     - 生成 FullReport
  3. 签发 Token
返回：{ user: { id, name, email, role }, token, fullReport? }
失败：
  - 422 { code: 'VALIDATION_ERROR', message: '...' } — 校验不通过
  - 409 { code: 'EMAIL_EXISTS', message: '该邮箱已注册' }
```

**POST /api/auth/login**
```
输入：{ email, password, diagnosticSessionId? }
逻辑：
  1. 查邮箱 → bcrypt 比对
  2. 如果传了 diagnosticSessionId：
     - 查 DiagnosticSession → 回填 userId → status = "linked"
     - 标记 User.diagnosticUsed = true
     - 生成 FullReport
  3. 签发 Token
返回：{ user: { id, name, email, role }, token, fullReport? }
失败：401 { code: 'INVALID_CREDENTIALS', message: '邮箱或密码错误' }
```

**GET /api/auth/me**
```
鉴权：requireAuth
逻辑：根据 req.user.userId 查 User
返回：{ id, name, email, role, avatar }
```

**POST /api/auth/logout**
```
鉴权：requireAuth
逻辑：无服务端状态需要清理，仅返回成功（前端自行清除 Token）
返回：{ success: true }
```

#### `routes/diagnostic.ts` — 诊断测试路由

**GET /api/diagnostic/questions**
```
鉴权：可选（有 JWT 时提取 user 做配额判断）
逻辑：
  1. 从题库随机抽取一套诊断试题（固定 N 题，如 20 题）
  2. 如果已登录：
     - 查 User.diagnosticUsed 和 paymentStatus
     - 免费用户已使用 → 403 { code: 'DIAGNOSTIC_QUOTA_EXHAUSTED' }
     - 付费用户或未使用 → 返回试题
  3. 如果未登录：直接返回试题
返回：{ questions: Question[] }
```

**POST /api/diagnostic/submit**
```
鉴权：可选（通过 Authorization header 判断）
输入：{ answers: [{ questionId, selectedAnswer }] }
逻辑：
  1. 逐题比对正确答案 → 计算 isCorrect、score、accuracy
  2. 如果已登录：
     - 检查配额（同上）
     - 配额通过 → 标记 User.diagnosticUsed = true
     - 生成完整报告 → 返回 FullReport
  3. 如果未登录：
     - 创建 DiagnosticSession（userId=null, status="anonymous"）
     - 生成部分报告 → 返回 PartialReport + sessionId
返回：
  游客：{ report: PartialReport }
  登录用户：{ report: FullReport }
```

**GET /api/diagnostic/report/:id**
```
鉴权：登录用户通过 JWT，游客通过 sessionId 匹配
逻辑：
  1. 查 DiagnosticSession：
     - status="linked" → 返回完整报告
     - status="anonymous" → 返回部分报告
     - 不存在 → 404
  2. 如果已登录且 session 的 userId 匹配当前用户 → 可返回完整报告
返回：
  游客（仅限自己创建的 session）：{ report: PartialReport }
  登录用户：{ report: FullReport }
```

#### `services/diagnostic.ts` — 诊断业务逻辑

```ts
// 批改答案
export function scoreAnswers(
  answers: { questionId: string; selectedAnswer: string }[],
  questions: Question[]
): ScoreResult

// 生成部分报告（游客）
export function buildPartialReport(
  session: DiagnosticSession,
  scoreResult: ScoreResult
): PartialReport

// 生成完整报告（登录用户）
export function buildFullReport(
  partialReport: PartialReport,
  questions: Question[]
): FullReport

// 知识点分析（薄弱项识别）
export function analyzeKnowledgePoints(
  scoreResult: ScoreResult,
  questions: Question[]
): KnowledgeAnalysis[]
```

---

### 7.5 对现有路由的鉴权改造

| 路由 | 改造 |
|------|------|
| `GET /api/papers` | 无需鉴权（公开浏览） |
| `GET /api/papers/:id` | 无需鉴权 |
| `PUT /api/papers/:id` | 加 `requireAdmin` |
| `DELETE /api/papers/:id` | 加 `requireAdmin` |
| `PUT /api/papers/:id/publish` | 加 `requireAdmin` |
| `GET /api/papers/:id/pdf` | 加 `requireAuth` |
| `POST /api/upload/paper` | 加 `requireAdmin` |
| `GET /api/parse-tasks/:id` | 加 `requireAdmin` |
| `POST /api/parse-tasks/:id/retry` | 加 `requireAdmin` |
| `GET /api/health` | 无需鉴权 |

### 7.6 速率限制

`routes/auth.ts` 路由文件挂载 `express-rate-limit`：

```ts
import rateLimit from 'express-rate-limit'

const authLimiter = rateLimit({
  windowMs: 60 * 1000,      // 1 分钟窗口
  max: 5,                    // 最多 5 次
  message: { code: 'RATE_LIMITED', message: '请求过于频繁，请稍后再试' },
})

router.use(authLimiter)      // 整个 /api/auth/* 下所有路由均受限
```

---

## 八、前端实现

### 8.1 新增/修改文件清单

```
quiz-web/src/
├── utils/
│   └── request.ts          # 新增：Axios 实例 + 拦截器（token 注入 + 自动刷新）
├── stores/
│   ├── auth.ts             # 改造：真实 API 调用替代 mock
│   └── diagnostic.ts       # 新增：诊断测试状态（题目、答案、报告）
├── views/
│   ├── auth/
│   │   ├── LoginView.vue   # 改造：接入真实登录 API
│   │   └── RegisterView.vue # 新增：注册页
│   ├── diagnostic/
│   │   ├── DiagnosticView.vue     # 新增：诊断测试做题页
│   │   └── DiagnosticReport.vue   # 新增：诊断报告页（部分/完整）
├── router/
│   └── index.ts            # 改造：添加路由守卫 + 诊断路由
└── types/
    └── index.ts            # 改造：新增 User、Diagnostic 相关类型
```

### 8.2 Axios 封装（`utils/request.ts`）

```
核心职责：
1. baseURL 指向 http://localhost:3001/api
2. 请求拦截器：从 localStorage 取 token 注入 Authorization header
3. 响应拦截器：
   - 401 时 → 清除登录态 → 跳转 /login
```

### 8.3 Auth Store 改造（`stores/auth.ts`）

```ts
// 改造后
export const useAuthStore = defineStore('auth', () => {
  const user = ref<User | null>(null)
  const token = ref<string | null>(null)
  const isLoggedIn = computed(() => !!token.value && !!user.value)

  // 从 localStorage 恢复（页面刷新后）
  function initFromStorage(): void {
    const saved = localStorage.getItem('token')
    const savedUser = localStorage.getItem('user')
    if (saved && savedUser) {
      token.value = saved
      user.value = JSON.parse(savedUser)
    }
  }

  // 登录（调 API，存 token + user）
  async function login(email: string, password: string): Promise<void>

  // 注册（调 API，存 token + user）
  async function register(name: string, email: string, password: string): Promise<void>

  // 退出（清除本地数据）
  function logout(): void {
    token.value = null
    user.value = null
    localStorage.removeItem('token')
    localStorage.removeItem('user')
  }

  return { user, token, isLoggedIn, initFromStorage, login, register, logout }
})
```

**Token 存储策略：**

| 数据 | 存储位置 | 理由 |
|------|---------|------|
| `token` | Pinia state + localStorage | 跨标签页共享 + 页面刷新恢复登录态 |
| `user` | Pinia state + localStorage | 同 token |

App 启动时（`main.ts` 或 `App.vue` 的 `onMounted`）调用 `initFromStorage()`：如果 localStorage 有 token，直接恢复登录态，无需额外网络请求。

### 8.4 路由守卫（`router/index.ts`）

```ts
import { useAuthStore } from '@/stores/auth'

router.beforeEach((to, _from, next) => {
  const auth = useAuthStore()

  // 需要登录的页面
  const requiresAuth = to.path.startsWith('/profile')
    || to.path.startsWith('/practice')

  // 仅管理员可访问
  const requiresAdmin = to.path.startsWith('/admin')

  if (requiresAuth && !auth.isLoggedIn) {
    return next('/login')
  }

  if (requiresAdmin && auth.user?.role !== 'admin') {
    return next('/')
  }

  // 已登录用户访问 /login → 重定向首页
  if (to.path === '/login' && auth.isLoggedIn) {
    return next('/')
  }

  next()
})
```

**路由鉴权清单：**

| 路由 | 鉴权要求 |
|------|---------|
| `/` | 公开 |
| `/login` | 公开（已登录则跳首页） |
| `/question-bank` | 公开 |
| `/profile` | 需登录 |
| `/practice` | 需登录 |
| `/admin/*` | 需 admin 角色 |
| `/admin` 未匹配 | 重定向 `/admin/core-library`（无变化） |

### 8.5 登录页改造

在现有 `LoginView.vue` 基础上改动：

1. `handleSubmit` 从 `auth.login()` mock 改为 `await auth.login(form.email, form.password)`
2. 增加 loading 状态（按钮禁用 + spinner）
3. 增加错误提示（API 返回的错误信息显示在表单顶部）
4. "忘记密码"链接保留（暂不做功能）
5. "立即注册"链接改为 `router-link to="/register"`

### 8.6 注册页（新增 `RegisterView.vue`）

```
结构参考 LoginView.vue：
- 姓名输入框
- 邮箱输入框
- 密码输入框（含规则提示）
- 确认密码输入框
- 注册按钮（含 loading）
- 底部链接："已有账号？去登录"
```

**前端校验规则（按优先级执行）：**

| 字段 | 规则 | 错误提示 |
|------|------|---------|
| 姓名 | 非空，最多 50 字 | "请输入姓名" / "姓名不超过 50 字" |
| 邮箱 | 合法邮箱格式 | "请输入有效的邮箱地址" |
| 密码 | 8~32 位 | "密码长度需为 8-32 位" |
| 密码 | 必须包含字母 + 数字 | "密码需同时包含字母和数字" |
| 确认密码 | 与密码字段完全一致 | "两次输入的密码不一致" |

**交互细节：**
- 密码输入框下方展示规则提示文字（灰色小字）："8-32 位，需包含字母和数字"
- 确认密码输入时实时比对，不一致时输入框变红 + 显示错误文案
- 所有校验通过后才允许点击注册按钮，按钮未被激活时置灰
- 提交后若后端返回校验错误（422），在表单顶部展示错误摘要

### 8.7 NavBar 适配

当前 NavBar 从 `useAuthStore` 读取 `user` 展示头像/名称，改造后数据源不变，只需确保 `user` 字段包含 `name` 和可选 `avatar`。

**角色切换下拉**（现有 mock 功能）：保留 UI，但切换时需验证权限。如果当前用户 role 非 admin，不应出现"切换到管理员"选项。改为根据 `user.role` 展示当前角色标签。

---

## 九、实施步骤

| 步骤 | 内容 | 涉及文件 |
|------|------|---------|
| **Step 1** | 数据库：schema.prisma 新增 User + DiagnosticSession，执行 migrate | `schema.prisma` |
| **Step 2** | 后端 JWT 工具：sign + verify 单 Token | `services/jwt.ts`（新） |
| **Step 3** | 后端中间件：requireAuth + requireAdmin | `middleware/auth.ts`, `admin.ts`（新） |
| **Step 4** | 后端路由：/api/auth/* 4 个端点 + 速率限制 | `routes/auth.ts`（新） |
| **Step 5** | 后端诊断：diagnostic 路由 + 批改/报告逻辑 | `routes/diagnostic.ts`, `services/diagnostic.ts`（新） |
| **Step 6** | 后端改造：给现有管理类路由加鉴权中间件 | `index.ts`, `routes/*.ts` |
| **Step 7** | 前端 Axios 封装：拦截器（注入 Token + 401 跳转） | `utils/request.ts`（新） |
| **Step 8** | 前端 Auth Store 改造：真实 API + localStorage | `stores/auth.ts` |
| **Step 9** | 前端诊断 Store：题目加载、答案管理、报告状态 | `stores/diagnostic.ts`（新） |
| **Step 10** | 前端路由守卫：beforeEach 鉴权拦截 | `router/index.ts` |
| **Step 11** | 前端登录页改造 + 注册页新建 | `views/auth/LoginView.vue`, `RegisterView.vue`（新） |
| **Step 12** | 前端诊断页 + 报告页新建 | `views/diagnostic/`（新） |
| **Step 13** | 前端 NavBar 适配：角色展示改为真实数据 | `components/NavBar.vue` |
| **Step 14** | 联调测试：注册 → 登录 → 诊断 → 报告 → 配额 → 登出 | — |

---

## 十、安全清单

| 检查项 | 措施 |
|--------|------|
| 密码存储 | bcrypt hash（salt rounds = 12），永不存明文 |
| 密码传输 | 生产环境强制 HTTPS |
| 令牌签名 | 强随机密钥存环境变量，默认值仅开发用 |
| 暴力破解 | 登录/注册接口速率限制（1分钟5次） |
| XSS | 生产环境 CSP 头限制脚本来源，降低 XSS 风险 |
| CSRF | Bearer Token 方案天然免疫（不依赖 Cookie） |
| 令牌吊销 | Token 无状态，登出时前端清除 localStorage，服务端无需额外处理 |
| 错误信息模糊 | 登录失败统一返回"邮箱或密码错误"，不区分用户是否存在 |
| SQL 注入 | 全部查询走 Prisma ORM，无裸 SQL |

---

## 十一、环境变量

在 `apps/api/.env` 中新增：

```env
# JWT 密钥（生产环境必须替换为强随机字符串）
JWT_SECRET=dev-secret-change-in-production
```

密钥生成命令：

```bash
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

---

## 十二、依赖清单

| 包名 | 版本 | 用途 |
|------|------|------|
| `bcryptjs` | ^2.4.3 | 密码哈希与比对 |
| `jsonwebtoken` | ^9.0.0 | JWT 签发与验证 |
| `express-rate-limit` | ^7.0.0 | 登录/注册速率限制 |

前端无需新增依赖，axios 已在项目中使用（如有），否则用 fetch 亦可。

---

*文档版本 v1.0，2026-05-16*
