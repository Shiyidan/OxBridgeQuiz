# 项目组织架构

## 一、整体结构

```
QuizTestDemo/
├── apps/api/                 # 后端 API（Express + Prisma）
│   ├── src/
│   │   ├── index.ts          # 服务入口（端口 3001）
│   │   ├── routes/           # 路由层
│   │   │   ├── papers.ts     # 试卷 CRUD
│   │   │   ├── upload.ts     # PDF 上传 + 创建解析任务
│   │   │   └── parse.ts      # 解析任务查询 / 重试
│   │   └── services/         # 业务层
│   │       ├── prisma.ts     # Prisma 客户端单例
│   │       ├── qwenService.ts # Qwen-VL-Max 模型调用
│   │       └── parseService.ts # 解析任务编排（PDF→PNG→Qwen→入库）
│   ├── scripts/
│   │   └── pdf_pages_to_base64.py  # PyMuPDF：PDF 逐页转 Base64 PNG
│   ├── prisma/
│   │   ├── schema.prisma     # 数据模型定义（Paper / ParseTask）
│   │   └── dev.db            # SQLite 数据库文件
│   ├── uploads/              # 上传的 PDF 文件存储
│   └── .env                  # 环境变量（API Key / 端口 / DB 路径）
│
├── quiz-web/                 # 前端（Vue 3 + TypeScript）
│   ├── src/
│   │   ├── main.ts           # 应用入口（Vue + Pinia + Router）
│   │   ├── App.vue           # 根组件（仅 RouterView）
│   │   ├── router/index.ts   # 路由配置
│   │   ├── stores/           # 状态管理（Pinia）
│   │   │   ├── auth.ts       # 登录态（mock）
│   │   │   ├── exam.ts       # 答题状态
│   │   │   └── counter.ts    # 脚手架自带（未使用）
│   │   ├── types/index.ts    # TypeScript 类型定义
│   │   ├── components/       # 共享组件
│   │   └── views/            # 页面
│   │       ├── admin/        # 管理后台
│   ├── public/
│   │   ├── data/paper.json   # 前端 mock 数据
│   │   └── data/images/      # 试卷图片（PDF 逐页 PNG）
│   ├── 开发规范.md            # 注释/命名等开发规范
│   └── design.md             # 设计 token 规范（色板/间距/圆角等）
│
├── design.md                 # 样式开发规范（CSS 变量 / 组件规范）
├── 技术方案.md                # PDF→Qwen 技术方案文档
└── 项目架构.md                # 本文件
```

---

## 二、后端（apps/api）

### 2.1 文件职责

| 文件 | 职责 |
|------|------|
| `src/index.ts` | Express 服务启动，挂载 CORS / JSON 解析 / 静态文件 / 路由 |
| `routes/papers.ts` | 试卷 CRUD：列表分页、单卷详情、编辑、删除、发布、PDF 下载 |
| `routes/upload.ts` | PDF 上传（multer, 50MB 限制），创建 Paper + ParseTask，后台启动解析 |
| `routes/parse.ts` | 查询解析任务状态、重试失败任务 |
| `services/prisma.ts` | Prisma 客户端单例导出 |
| `services/qwenService.ts` | Qwen-VL-Max 多模态接口封装，图片→JSON 题目解析 |
| `services/parseService.ts` | 解析编排：调用 Python 脚本 PDF→PNG，逐页调 Qwen，去重排序，写入 Paper |
| `scripts/pdf_pages_to_base64.py` | PyMuPDF 渲染 PDF 每页为 PNG 并 Base64 编码，输出 JSON |
| `prisma/schema.prisma` | 数据模型：Paper（试卷）+ ParseTask（解析任务） |

### 2.2 API 路由表

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 健康检查 |
| GET | `/api/papers` | 试卷列表（分页） |
| GET | `/api/papers/:id` | 试卷详情（含题目数组） |
| PUT | `/api/papers/:id` | 编辑试卷信息 |
| DELETE | `/api/papers/:id` | 删除试卷 + PDF |
| PUT | `/api/papers/:id/publish` | 发布试卷 |
| GET | `/api/papers/:id/pdf` | 下载原始 PDF |
| POST | `/api/upload/paper` | 上传 PDF，触发解析 |
| GET | `/api/parse-tasks/:id` | 查询解析进度 |
| POST | `/api/parse-tasks/:id/retry` | 重试解析 |

### 2.3 解析流程

```
用户上传 PDF
  → multer 保存到 uploads/
  → 创建 Paper 记录 + ParseTask 记录
  → 后台异步 startParseTask()
    → Python 脚本 PDF → PNG Base64（逐页）
    → Qwen-VL-Max 逐页识别 → JSON
    → 去重排序 → 写入 Paper.questions
    → 更新 ParseTask 状态为 completed
```

---

## 三、前端（quiz-web）

### 3.1 技术栈

Vue 3（Composition API）+ TypeScript + Pinia + Vue Router + KaTeX + SCSS

### 3.2 目录划分

#### `src/components/` — 共享组件

| 组件 | 说明 |
|------|------|
| `NavBar.vue` | 全局导航栏（首页/登录/试题库共用，含登录态切换和用户下拉卡片） |
| `QuestionCard.vue` | 题目渲染卡片（练习页、试卷预览共用） |
| `FormulaBlock.vue` | KaTeX 公式渲染块（行内 + 居中模式，模块级缓存） |
| `LatexText.vue` | LaTeX 文本混合渲染器（切分 $...$ / $$...$$ → FormulaBlock） |
| `PricingSection.vue` | 定价卡片区（首页使用，Free / Pro 双卡对比） |

#### `src/views/` — 前台页面

| 页面 | 路由 | 说明 |
|------|------|------|
| `HomeView.vue` | `/` | 首页（Hero + 定价 + 功能介绍） |
| `LoginView.vue` | `/login` | 登录页（静态 mock，点击即登录） |
| `QuestionBankView.vue` | `/question-bank` | 试题库（知识点 + 难度筛选 → 进入练习） |
| `PracticeView.vue` | `/practice` | 在线答题（题目切换 + 选项选择 + 提交） |

#### `src/views/admin/` — 管理后台

**布局组件：**

| 组件 | 说明 |
|------|------|
| `AdminLayout.vue` | 管理后台整体布局（左侧导航 + 右侧 RouterView） |
| `CoreLibraryLayout.vue` | 核心资料库子路由壳（仅 RouterView） |
| `ExamBankLayout.vue` | 真题库子路由壳（仅 RouterView） |

**侧边栏 Tab 页：**

| 页面 | 路由 | 说明 |
|------|------|------|
| `RevenueView.vue` | `/admin/revenue` | 营收与数据（占位） |
| `StaffView.vue` | `/admin/staff` | 员工管理（占位） |
| `UserManageView.vue` | `/admin/users` | 用户管理（占位） |
| `PaymentView.vue` | `/admin/payment` | 付费策略与订阅（占位） |

**核心资料库页面：**

| 页面 | 路由 | 说明 |
|------|------|------|
| `CoreLibraryView.vue` | `/admin/core-library` | 核心资料库首页（4 卡片入口） |
| `QuestionBankAdmin.vue` | `/admin/core-library/questions` | 试题库管理 |
| `TextbookAdmin.vue` | `/admin/core-library/textbooks` | 教材库管理（占位） |
| `SyllabusAdmin.vue` | `/admin/core-library/syllabus` | 大纲库管理（占位） |

**真题库页面：**

| 页面 | 路由 | 说明 |
|------|------|------|
| `ExamBankAdmin.vue` | `/admin/core-library/exams` | 真题库列表（套卷 + 状态下拉） |
| `PaperUpload.vue` | `/admin/core-library/exams/upload` | 试卷解析录入（PDF 拖拽 → Qwen 解析） |
| `PaperPreview.vue` | `/admin/core-library/exams/:id` | 试卷详情预览（复用 QuestionCard） |

#### `src/stores/` — 状态管理

| Store | 说明 |
|-------|------|
| `auth.ts` | 登录态（mock user，isLoggedIn 判断） |
| `exam.ts` | 答题状态（题目加载、答案存写、提交、计分） |

#### `src/types/` — 类型定义

| 类型 | 说明 |
|------|------|
| `Question` | 题目（id / title / options / answer / images） |
| `Paper` | 试卷（title / year / questions[]） |
| `ExamState` | 考试状态（paper / answers / submitted） |

---

## 四、路由层级

```
/                          → 首页
/login                     → 登录页
/question-bank             → 试题库
/practice                  → 在线答题

/admin                     → 重定向 → /admin/core-library
├── /admin/revenue         → 营收与数据
├── /admin/staff           → 员工管理
├── /admin/users           → 用户管理
├── /admin/payment         → 付费策略与订阅
└── /admin/core-library
    ├── /                  → 核心资料库首页
    ├── /questions         → 试题库
    ├── /textbooks         → 教材库
    ├── /syllabus          → 大纲库
    └── /exams
        ├── /              → 真题库列表
        ├── /upload        → 试卷解析录入
        └── /:id           → 试卷详情预览
```
