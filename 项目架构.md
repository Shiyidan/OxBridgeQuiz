# 项目组织架构

## 一、整体结构

```text
QuizTestDemo/
├── api/                              # 后端 API（Express + Prisma + JWT）
│   ├── src/
│   │   ├── index.ts                  # 服务入口（端口 3001）
│   │   ├── config.ts                 # 环境变量统一读取
│   │   ├── routes/                   # 路由层
│   │   │   ├── auth.ts               # 注册 / 登录 / 当前用户 / 登出
│   │   │   ├── papers.ts             # 试卷 CRUD + 发布 + PDF 下载
│   │   │   ├── upload.ts             # PDF 上传 + 创建解析任务
│   │   │   ├── parse.ts              # 解析任务查询 / 重试
│   │   │   ├── diagnostic.ts         # 诊断测评（出题 / 提交 / 报告）
│   │   │   ├── exam.ts               # 考试 / 交卷 / 答题结果 / 错题本
│   │   │   └── admin.ts              # 后台管理（用户列表 / 角色变更）
│   │   ├── middleware/               # 中间件
│   │   │   ├── auth.ts               # requireAuth（JWT 校验）+ optionalAuth
│   │   │   └── admin.ts              # requireAdmin（管理员权限）
│   │   ├── services/                 # 业务层
│   │   │   ├── prisma.ts             # Prisma 客户端单例
│   │   │   ├── jwt.ts                # JWT 签发 + 校验
│   │   │   ├── diagnostic.ts         # 评分算法 + 报告生成
│   │   │   ├── qwenService.ts        # Qwen-VL-Max 模型调用
│   │   │   └── parseService.ts       # 解析任务编排（PDF→PNG→Qwen→入库）
│   │   └── utils/
│   │       └── response.ts           # 统一响应格式 success() / fail()
│   ├── prisma/
│   │   └── schema.prisma             # 数据模型定义（8 张表）
│   ├── .env                          # 环境变量（JWT Secret / 端口 / DB 路径）
│   ├── package.json
│   └── tsconfig.json
│
├── quiz-web/                         # 前端（Vue 3 + TypeScript）
│   ├── src/
│   │   ├── main.ts                   # 应用入口（Vue + Pinia + Router）
│   │   ├── App.vue                   # 根组件
│   │   ├── config.ts                 # API 地址配置
│   │   ├── router/index.ts           # 路由配置 + 导航守卫
│   │   ├── stores/                   # 状态管理（Pinia）
│   │   │   ├── auth.ts               # 登录态（login / register / logout）
│   │   │   └── exam.ts               # 答题状态
│   │   ├── utils/                    # 工具函数
│   │   │   ├── request.ts            # Axios 实例 + 拦截器（Token 注入 / 响应解包 / 401 跳转）
│   │   │   └── validation.ts         # 表单校验（邮箱 / 密码 / 姓名）
│   │   ├── types/index.ts            # TypeScript 类型定义
│   │   ├── components/               # 共享组件
│   │   │   ├── NavBar.vue            # 全局导航栏 + 用户下拉菜单
│   │   │   ├── QuestionCard.vue      # 题目渲染卡片（复用组件）
│   │   │   ├── FormulaBlock.vue      # KaTeX 公式渲染块
│   │   │   ├── LatexText.vue         # LaTeX 文本混合渲染器
│   │   │   ├── PricingSection.vue    # 定价卡片区
│   │   │   └── report/               # 报告相关组件
│   │   │       └── ExamQuestionAnalysis.vue
│   │   └── views/                    # 页面（按模块分目录）
│   │       ├── home/HomeView.vue     # 首页
│   │       ├── auth/                 # 登录 / 注册
│   │       ├── questionBank/         # 试题库模块（浏览 / 答题 / 交卷结果）
│   │       ├── assessment/           # 诊断测试模块（真题入口 / 成绩报告）
│   │       ├── mistakeNotebook/      # 错题本模块（错题列表 / 练习记录）
│   │       ├── profile/              # 个人中心
│   │       └── admin/                # 管理后台（14 个页面 + 3 个布局）
│   ├── public/data/                  # 静态数据
│   ├── package.json
│   └── vite.config.ts
│
├── 开发规范.md                        # 注释 / 数据库 / API 响应规范
├── 样式规范.md                        # 设计 token 规范
├── 技术方案.md                        # PDF→Qwen 技术方案文档
├── 数据库构建.md                      # 数据模型详细文档
├── 登录模块技术方案.md                 # 登录模块技术方案
├── 部署方案.md                        # 部署方案文档
├── todoList.md                       # 待办清单
├── 项目架构.md                        # 本文件
└── README.md
```

---

## 二、后端（api）

### 2.1 文件职责

| 文件 | 职责 |
|------|------|
| `src/index.ts` | Express 启动，挂载 CORS / JSON 解析 / 路由，禁用 ETag |
| `src/config.ts` | 从 .env 读取端口、JWT Secret、数据库路径、CORS 白名单 |
| `routes/auth.ts` | 注册（bcrypt 加密 + JWT 签发）、登录、获取当前用户、登出；含频率限制 |
| `routes/papers.ts` | 试卷 CRUD：列表分页、详情、编辑、删除、发布、PDF 下载 |
| `routes/upload.ts` | PDF 上传（multer, 50MB 限制），创建 Paper + ParseTask，后台启动解析 |
| `routes/parse.ts` | 查询解析任务状态、重试失败任务 |
| `routes/diagnostic.ts` | 诊断测评：随机出 20 题 → 提交批改 → 生成报告（游客部分/登录完整） |
| `routes/exam.ts` | 考试：交卷保存、答题结果详情、错题本、练习记录 |
| `routes/admin.ts` | 管理员：用户列表、角色变更 |
| `middleware/auth.ts` | requireAuth（Bearer Token 校验 + 401），optionalAuth（静默解析） |
| `middleware/admin.ts` | requireAdmin（role === 'admin' 校验 + 403） |
| `services/prisma.ts` | Prisma 客户端单例 |
| `services/jwt.ts` | JWT 签发（7 天有效期）+ 校验 |
| `services/diagnostic.ts` | 答案评分 + 部分报告 / 完整报告（知识分析 + 建议）生成 |
| `services/qwenService.ts` | Qwen-VL-Max 多模态接口封装，图片 → JSON 题目解析 |
| `services/parseService.ts` | 解析编排：信号量并发控制（5 并发） + 协调器汇合多页结果 + 去重排序写入 Paper |
| `utils/response.ts` | success() / fail() 工具函数，统一响应格式 |
| `prisma/schema.prisma` | 数据模型 8 表：Paper / ParseTask / User / DiagnosticSession / ExamRecord / AnswerRecord / SyllabusNode / RevenueCost |

### 2.2 API 路由表

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| GET | `/api/health` | 无 | 健康检查 |
| POST | `/api/auth/register` | 无（频率限制） | 注册 |
| POST | `/api/auth/login` | 无（频率限制） | 登录 |
| GET | `/api/auth/me` | requireAuth | 当前用户信息 |
| POST | `/api/auth/logout` | requireAuth | 登出 |
| GET | `/api/diagnostic/questions` | optionalAuth | 获取 20 道诊断题 |
| POST | `/api/diagnostic/submit` | optionalAuth | 提交答案，返回报告 |
| GET | `/api/diagnostic/report/:id` | optionalAuth | 查看历史报告 |
| POST | `/api/exams/submit` | requireAuth | 交卷（保存答题记录） |
| GET | `/api/exams/:id/result` | requireAuth | 答题结果详情 |
| GET | `/api/exams/error-book` | requireAuth | 错题本（所有错题记录） |
| GET | `/api/exams/practice-records` | requireAuth | 试题库练习记录 |
| GET | `/api/papers/assessment/papers` | requireAuth | 诊断测试套卷 + 历史报告列表 |
| GET | `/api/admin/users` | requireAuth + requireAdmin | 用户列表 |
| PUT | `/api/admin/users/:id/role` | requireAuth + requireAdmin | 修改用户角色 |
| GET | `/api/papers` | 无 | 试卷列表（分页） |
| GET | `/api/papers/:id` | 无 | 试卷详情（含题目数组） |
| PUT | `/api/papers/:id` | requireAuth + requireAdmin | 编辑试卷 |
| DELETE | `/api/papers/:id` | requireAuth + requireAdmin | 删除试卷 |
| PUT | `/api/papers/:id/publish` | requireAuth + requireAdmin | 发布试卷 |
| GET | `/api/papers/:id/pdf` | requireAuth | 下载原始 PDF |
| POST | `/api/upload/paper` | requireAuth + requireAdmin | 上传 PDF，触发解析 |
| GET | `/api/parse-tasks/:id` | requireAuth + requireAdmin | 查询解析进度 |
| POST | `/api/parse-tasks/:id/retry` | requireAuth + requireAdmin | 重试解析 |

### 2.3 数据模型

```
User                            DiagnosticSession
├── id (uuid)                   ├── id (uuid)
├── name                        ├── userId (FK → User, nullable)
├── email (unique)              ├── answers (JSON)
├── password (bcrypt)           ├── totalQuestions
├── role (student/admin)        ├── correctCount
├── paymentStatus (free/paid)   ├── status (anonymous/linked)
├── diagnosticUsed (bool)       └── createdAt
├── avatar
├── createdAt
└── updatedAt

Paper                           ParseTask
├── id (uuid)                   ├── id (uuid)
├── title                       ├── paperId (FK → Paper, cascade)
├── code                        ├── status (pending/completed/failed)
├── year                        ├── progress (0-100)
├── duration                    ├── error
├── totalQuestions              ├── result (JSON)
├── paperType (past/practice)   ├── createdAt
├── pdfUrl (nullable)           └── updatedAt
├── status (draft/published)
├── questions (JSON)            ExamRecord
├── createdAt                   ├── id (uuid)
└── updatedAt                   ├── userId (FK → User)
                                ├── paperId (FK → Paper)
                                ├── totalQuestions
                                ├── correctCount
                                ├── startedAt
                                ├── submittedAt
                                ├── status (in_progress/submitted)
                                └── answers → AnswerRecord[]

AnswerRecord                    SyllabusNode
├── id (uuid)                   ├── id (uuid)
├── examRecordId                ├── code
│   (FK → ExamRecord, cascade)  ├── label
├── questionId                  ├── examType (ESAT/TMUA)
├── selectedAnswer              ├── parentCode (nullable)
├── isCorrect                   ├── order
├── durationSeconds             └── createdAt
└── answeredAt

RevenueCost
├── id (uuid)
├── rechargeItem
├── amount
├── operator
├── occurredAt
├── reimbursementStatus
│   (unreimbursed/reimbursed)
├── remark (nullable)
├── createdAt
└── updatedAt
```

### 2.4 解析流程

```
用户上传 PDF/图片
  → 浏览器 pdf.js 逐页渲染为 JPEG Base64（或直接传原图）
  → 创建 Paper 记录 + ParseTask 记录
  → 返回 paperId + taskId（即时响应）
  → 前端逐页 POST /parse-tasks/:id/pages
    → 后端收到每页立即调 Qwen-VL-Max（信号量限制 5 并发）
    → 协调器串行汇合多页结果
    → 全部完成后去重排序 → 写入 Paper.questions
    → 更新 ParseTask 状态为 completed
```



---

## 三、前端（quiz-web）

### 3.1 技术栈

Vue 3（Composition API）+ TypeScript + Pinia + Vue Router + Axios + KaTeX + SCSS

### 3.2 目录划分

#### `src/components/` — 共享组件

| 组件 | 说明 |
|------|------|
| `NavBar.vue` | 全局导航栏，含登录态自适应（显示用户下拉 / 登录按钮） |
| `QuestionCard.vue` | 题目渲染卡片（试题库、练习页、试卷预览共用） |
| `FormulaBlock.vue` | KaTeX 公式渲染块（模块级缓存，行内 + 居中模式） |
| `LatexText.vue` | LaTeX 文本混合渲染器（切分 $...$ / $$...$$ → FormulaBlock） |
| `PricingSection.vue` | 定价卡片区（首页 Free / Pro 双卡对比） |

#### `src/views/` — 前台页面

| 页面 | 路由 | 说明 |
|------|------|------|
| `home/HomeView.vue` | `/` | 首页（Hero + 功能介绍 + 定价） |
| `auth/LoginView.vue` | `/login` | 登录页 |
| `auth/RegisterView.vue` | `/register` | 注册页 |
| `questionBank/QuestionBankView.vue` | `/question-bank` | 试题库（知识点 + 难度筛选） |
| `questionBank/PracticeView.vue` | `/practice` | 在线答题（支持题库练习 + 真题模拟） |
| `questionBank/ExamResultView.vue` | `/exam-result` | 交卷结果汇总 |
| `assessment/AssessmentHomeView.vue` | `/assessment` | 诊断测试入口（真题套卷 + 历史报告） |
| `assessment/ExamResultDetail.vue` | `/exam-result/:id` | 成绩报告详情（诊断报告 + 题库练习结果） |
| `mistakeNotebook/MistakeNotebookView.vue` | `/mistake-notebook` | 错题本（错题列表 + 试题库练习记录） |
| `profile/ProfileView.vue` | `/profile` | 个人中心 |

#### `src/views/admin/` — 管理后台

**布局组件：**

| 组件 | 说明 |
|------|------|
| `AdminLayout.vue` | 管理后台整体布局（左侧导航 + 右侧 RouterView） |
| `CoreLibraryLayout.vue` | 核心资料库子路由壳（仅 RouterView） |
| `ExamBankLayout.vue` | 真题库子路由壳（仅 RouterView） |

**侧边栏页面：**

| 页面 | 路由 | 说明 |
|------|------|------|
| `RevenueView.vue` | `/admin/revenue` | 营收与数据（占位） |
| `StaffView.vue` | `/admin/staff` | 员工管理（占位） |
| `UserManageView.vue` | `/admin/users` | 用户管理（列表 + 角色切换） |
| `PaymentView.vue` | `/admin/payment` | 付费策略（占位） |

**核心资料库：**

| 页面 | 路由 | 说明 |
|------|------|------|
| `CoreLibraryView.vue` | `/admin/core-library` | 首页（4 卡片入口） |
| `QuestionBankAdmin.vue` | `/admin/core-library/questions` | 试题库管理 |
| `TextbookAdmin.vue` | `/admin/core-library/textbooks` | 教材库（占位） |
| `SyllabusAdmin.vue` | `/admin/core-library/syllabus` | 大纲库（占位） |

**真题库：**

| 页面 | 路由 | 说明 |
|------|------|------|
| `ExamBankAdmin.vue` | `/admin/core-library/exams` | 真题列表 + 状态下拉切换 |
| `PaperUpload.vue` | `/admin/core-library/exams/upload` | 试卷解析录入（拖拽上传 + 轮询进度） |
| `PaperPreview.vue` | `/admin/core-library/exams/:id` | 试卷详情预览 |

#### `src/stores/` — 状态管理

| Store | 说明 |
|-------|------|
| `auth.ts` | 登录注册状态、Token 持久化、用户信息管理 |
| `exam.ts` | 答题状态（题目加载、答案存写、提交、计分） |

#### `src/utils/` — 工具

| 文件 | 说明 |
|------|------|
| `request.ts` | Axios 实例：请求注入 Token，响应解包 envelope + 401 跳转登录 |
| `validation.ts` | 邮箱 / 密码 / 姓名 / 确认密码 校验函数 |

---

## 四、路由层级

```
/                          → 首页
/login                     → 登录页
/register                  → 注册页
/profile                   → 个人中心
/question-bank             → 试题库
/practice                  → 在线答题
/assessment                → 诊断测试
/mistake-notebook          → 错题本
/exam-result               → 交卷结果
/exam-result/:id           → 成绩报告详情

/admin                     → 重定向 → /admin/core-library
├── /admin/revenue         → 营收与数据
├── /admin/staff           → 员工管理
├── /admin/users           → 用户管理
├── /admin/payment         → 付费策略与订阅
└── /admin/core-library
    ├── /                  → 核心资料库首页
    ├── /questions         → 试题库
    ├── /textbooks         → 教材库
    ├── /syllabus          → 大纲库
    └── /exams
        ├── /              → 真题库列表
        ├── /upload        → 试卷解析录入
        └── /:id           → 试卷详情预览
```

### 路由守卫

- `/profile`、`/practice`、`/assessment`、`/mistake-notebook` → 需登录，未登录跳 `/login`
- `/admin/*` → 需管理员角色，非管理员跳首页
- 已登录访问 `/login` 或 `/register` → 重定向首页
